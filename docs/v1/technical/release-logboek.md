# Release-logboek

Leidend document: [afrondingsplan](release-afronding.md). Elke wijziging hieronder hoort bij een stap daarvan. **Afgetekend** betekent dat het genoemde bewijs bestaat; het zegt niets over nog open hardwareproeven. Oude resultaten blijven historisch bewijs wanneer de kandidaat verandert.

## Opdracht en huidige stand

Derek heeft op 6 september de twee stappen vóór hardware opgedragen: resterende codepunten sluiten (planstap 2) en één testbare kandidaat maken (planstap 3). Daarna plaatst Derek **2.2.0** voor de laatste hardware-ronde. Eerdere interpretatie dat beide nodes al de uiteindelijke 2.2 draaien is hiermee vervallen. In deze fase geen hardware-deploy of flash.

| Planstap | Status | Bewijs / restant |
| --- | --- | --- |
| 1. Framework tegen visie | Afgetekend voor de uitgevoerde codepaden en lokale regressies | [Contract](framework-contract.md), [reparaties](../reviews/2026-09-06-framework/fixes/README.md). Board-/drivergrenzen gaan naar stap 2; hardwarebewijs blijft apart. |
| 2. Codepunten sluiten | Afgetekend voor code/host | HOP v1.0.1 geïntegreerd; NIC-, opslag- en overige driverreviews uitgevoerd, concrete fixes getest. L04–L08. |
| 3. Kandidaat bouwen | Afgetekend | Definitieve bron-/bundlehashes, zeven builds, hostgates en relevante QEMU-proeven slagen. L09–L10. |
| 4. Hardware | In uitvoering; bevindingen open | H0–H6 op 2.2.0; deelbewijs L12–L13. Geen volledige hardware-aftekening. |
| 5–6. Eindaftekening | Open | Volgt na hardware en eventuele gerichte reparaties. |
| Vervolg: fysieke netwerk-IRQ | Gepland na de docs, voor een volgende puntrelease | Geen blokkade voor de huidige afronding; besluit L21. |

## Logboek 6 september 2026

### L01 — bestaand bewijs afgetekend (planstap 1)

- Frameworkeigendom, start/stop, geheugenwissen, ringgrenzen, SMP-context, wekpad en adoptie zijn gerepareerd. Hosttests en zeven kernelbuilds zijn uitgevoerd; exacte scope en logs staan in het reparatieverslag.
- QEMU lifecycle, geforceerde yield, gedeelde buur, SMP, hergebruik en opeenvolgende flips slagen op de vastgelegde snapshots.
- Netwerkacceptatie: twee blijvende TCP-verbindingen over drie flips, ieder 24 rondes / 864 bytes. Echte agentacceptatie: dezelfde appbootmarker met oplopende teller. [Bewijs](../reviews/2026-09-06-framework/acceptance/README.md).
- Log-adoptiepatch: oorspronkelijke code faalt op ontbrekende logroute; patch slaagt in hostregressie met race detector en echte QEMU-agent. Best-effort logs herstellen; verliesloze logging tijdens flip is niet beloofd. Nog niet geïntegreerd in productie-dependency.

### L02 — uitvoering vóór hardware gestart (planstappen 2–3)

Drie afgebakende driverdoorlopen: NICs en IRQ-bedrading; NVMe/PCIe/RTKit; overige drivers en optionele GUI. Parallel integreert de hoofdtaak de bewezen agentfix en bereidt de kandidaatbouw voor. Alleen concrete correctness-fouten worden gerepareerd. Geen nieuwe functies of algemene infrastructuur.

Nieuwe bevindingen, besluiten, gewijzigde bestanden en testuitkomsten worden hieronder toegevoegd vóór het betreffende punt wordt afgetekend.

### L03 — fysieke netwerk-IRQ uitgesteld (scopebesluit Derek)

Derek: “zullen we de IRQ voor alle boards wel even bewaren, dat is echt een test voor later”. Alleen QEMU heeft nu de netwerk-IRQ aangesloten; de fysieke boards blijven voorlopig pollen. We voegen in deze ronde geen board-IRQ-drivers toe en blokkeren de kandidaat hier niet op. NIC-buffercorrectness en het bestaande logische wekprotocol blijven wel onderdeel van stappen 2–4. De visie blijft ongewijzigd, met deze expliciete tijdelijke uitzondering.

### L04 — agentlogfix geïntegreerd (planstap 2, afgetekend)

De geteste patch is gepubliceerd als `github.com/xinix00/hop v1.0.1`, commit `b565e2e` (alleen de log-adoptie en de regressietest). `metal/go.mod` en `go.sum` gebruiken deze gepubliceerde versie; downloaden is geslaagd. Geen lokale HOP-replace in de release. [Runner-/agenttests](../reviews/2026-09-06-framework/candidate/evidence/hop-tests.log) slagen; de gerichte adoptietest slaagt ook met `-race`. Het eerdere QEMU-bewijs testte dezelfde patch via een tijdelijke workspace. Stap 3 herhaalt de integratie met de gepubliceerde dependency en de uiteindelijke driverwijzigingen.

### L05 — concrete driverbevindingen in behandeling (planstap 2)

- NIC: RX-lengtes moeten binnen het werkelijke DMA-buffer blijven; bij tg3 werd een buffer al aan hardware teruggegeven vóór de kopie klaar was.
- Opslag: NVMe mag na onzekere completion dezelfde buffer/queue niet voor een nieuwe opdracht gebruiken. Apples RTKit-allocator overlapte het huidige NVMe-DMA-venster.
- Overige drivers: bestaande GIC-routering heeft een verkeerd registeroffset; enkele timeoutpaden geven commandogeheugen te vroeg opnieuw uit. Dit gaat over bestaande drivers, niet over de uitgestelde aansluiting van IRQ op extra boards.

De verantwoordelijke driverreviews leggen de exacte fixes, gerichte regressies en resterende hardwarevragen vast voordat deze punten worden afgetekend.

### L06 — NIC- en opslagcode afgetekend (planstap 2)

[NIC-review](release-nic-review.md): alle zeven Receive-paden zijn binnen hun DMA-buffers begrensd; tg3 kopieert vóór teruggeven, GEM weigert onvolledige frames, virtio valideert de volledige descriptor-ID. Zeven regressiefixtures falen tegen de oorspronkelijke drivercode en slagen met de fixes. MDIO en switchtests slagen. Fysieke IRQ-aansluiting blijft uitgesteld volgens L03.

[Opslagreview](release-storage-review.md): NVMe bewaart onzeker DMA-eigendom na timeout, controleert completion-ID en bounds, reset vóór hergebruik; Apples RTKit-gebied ligt buiten het volledige NVMe-cacheblok. Gerichte NVMe-/RTKit-/allocatorproeven slagen. Warme ANS-firmwareoverdracht en fysieke performance blijven hardwarebewijs.

### L07 — gezamenlijke gates uitgevoerd (planstap 3)

De [framework-hosttests](../reviews/2026-09-06-framework/candidate/evidence/framework-host.log) en [bestaande host-/TamaGo-buildgate](../reviews/2026-09-06-framework/candidate/evidence/host-build-gate.log) slagen met HOP v1.0.1 en de gezamenlijke fixes. De gate omvat kale/GUI-varianten, Apple met VHE/highram en RISC-V met embedvarianten. Een brede standaard-hostaanroep van alle drivers strandt uitsluitend op DVFS, dat TamaGo `runtime/goos` nodig heeft; dit pakket wordt door de targetbuilds getoetst. De toepasselijke hostpackages worden afzonderlijk met de GUI-tag uitgevoerd; de eerste foutlog blijft bewaard. Bundelbouw en kandidaat-QEMU volgen nog.

### L08 — overige drivercode afgetekend (planstap 2)

[Overige-driverreview](release-aux-driver-review.md) bevat de volledige inventaris, inclusief optionele GUI en standaard uitgeschakelde SMC. Bestaande GIC-routering, USB-eigendom na timeouts, HID-rollover, mailbox/PCC-hergebruik, SMC-request-ID en framebuffer-/consolegrenzen zijn hersteld. De [gezamenlijke toepasselijke driverhosttests](../reviews/2026-09-06-framework/candidate/evidence/driver-host-supported.log) slagen met `-tags gui`. Geen extra lock of worker. DVFS houdt zijn beleidsstatus pas bij na bevestigde klokwijziging; dit wordt op target gebouwd.

### L09 — kandidaatcontrole en laatste bronwijziging (planstap 3)

De eerste zeven bundels bouwden alle, zonder bronwijziging tijdens die bouw. Een controle daarna vond één late wijziging: `metal/driver/dvfs/dvfs.go`. Die eerste set is daarom tussentijds bewijs, geen definitieve kandidaat. De builder maakt een nieuwe set na die laatste wijziging. De framework-QEMU en netwerk-/agentacceptatie van dezelfde bron vóór deze uitsluitend Pi-DVFS-wijziging slagen; de nieuwe exacte virt-bundel wordt bovendien rechtstreeks als boot- en flipbeeld in de laatste agenttest gebruikt. Hiermee wordt niet alleen een herbouwde testkopie afgetekend.

### L10 — definitieve kandidaat afgetekend (planstap 3)

[Kandidaatverslag en reproduceerbare scripts](../reviews/2026-09-06-framework/candidate/README.md). Alle zeven bundels zijn opnieuw gebouwd na de laatste wijziging; bronhashes vóór/na en bij de afsluitende controle zijn gelijk. Alleen Pi 4/5-bundels veranderden door DVFS; de virt-bundel bleef gelijk. De lokale artifacts staan in `out-release/2.2.0-candidate`, met NOCFG als controlebuilds; geen fysieke node heeft deze ronde een nieuwe kernel gekregen.

De exacte virt-bundel met SHA-256 `493028fd806154fecef4c4776778518c770380e8221a117bf6451a316b89958d` is als ELF geboot en daarna via de echte agent geflipt. [Bewijs](../reviews/2026-09-06-framework/candidate/evidence/agent-exact/artifact-proof.json): `AGENT_LOG_FLIP_ASSERTIONS_PASSED`, taak `5DTQM9R2B7QV50J0`, bootmarker `30412336993` ongewijzigd, voortgang tot 3670, restart_count 0 en verse logs na de flip. De afzonderlijke netwerkproef hield twee TCP-verbindingen over drie flips; beide 24 rondes / 864 bytes. Framework-lifecycle/SMP/sharing/hergebruik slaagt eveneens.

De review- en bewijsbestanden zijn niet langer gitignored, zodat ze met de code kunnen worden opgenomen. De OS-werkboom is niet als release gepubliceerd; alleen de afgesproken HOP-dependencyfix is gepubliceerd. **De twee stappen vóór hardware zijn hiermee afgerond.** Volgende actie: Derek plaatst 2.2.0 met de juiste boardconfig, daarna H0–H6. Uitgestelde board-IRQ-aansluiting blijft buiten die aftekening; geen nieuwe reviewronde zonder concrete bevinding.

### L11 — volledige docs-overhaul toegevoegd (opdracht Derek, planstap 5)

Derek vraagt een totale overhaul van de verouderde docs nu het contract duidelijk is. Opgenomen als expliciete opdracht én eindvoorwaarde in stap 5: inventariseren, één ingang/leesvolgorde, herschrijven tegen contract/code/hardwarebewijs, dubbelen opruimen, historie apart houden en voorbeelden/links controleren. De twee stappen vóór hardware blijven afgetekend. Deze toevoeging wordt na de hardware-ronde uitgevoerd en is nog niet als afgerond gemarkeerd.

### L12 — hardwaredeelbewijs en open bevindingen (planstap 4)

[Bewaard hardwarebewijs](../reviews/2026-09-06-framework/hardware/) bevat de uitkomsten per node en de gebruikte hostscripts onder `scripts/` als tekst. Deze scripts gebruiken lokale fixturepaden.

- M4 en LicheeRV: twintig start/werk/stopcycli slagen, met terugkeer van gerapporteerde geheugen- en coreclaims naar de baseline (`h1-cycles.json`). De aparte hardwareproef voor schoon geheugen bij een nieuwe eigenaar ontbreekt nog; H1 is niet volledig afgetekend.
- Sharing zonder `core-class`: buur blijft lopen bij stop/vervanging; computechecksums en tien wake-observaties slagen. M4 dedicated SMP voert de tweewerkerproef uit. Niet alle fysieke idle-/wakegevallen zijn hiermee afgetekend.
- **Open plaatsingsfout:** `sharegroup` met `core-class=big` leidt tot onterechte plaatsingsweigering. De agent behandelt kooinummers hier als fysieke coreclaims. Bewijs: `h2-class-combination-failure.json`. Nog geen reparatie afgetekend; de geslaagde sharingproef zonder klasse sluit dit punt niet.
- H5 vóór flips: drie netwerk-contentproeven op beide nodes en drie private-filesysteemproeven op M4 slagen. LicheeRV heeft geen toepasselijk NVMe-volume. Dit is geen herhaling van de genoemde 4000 MB/s-benchmark en geen vergelijking met code vóór de fixes.
- **H4 niet geslaagd:** de hostsuite eindigt met `ConnectionResetError` op de blijvende appverbinding ([foutlog](../reviews/2026-09-06-framework/hardware/h4-failure.log)). De latere inventaris toont op LicheeRV alleen welcome; de eerdere testapps ontbreken. M4 meldt de drie testtaken nog als running. Derek heeft nadien bevestigd dat hij LicheeRV schoon heeft geflasht: alleen welcome is daar de bedoelde uitgangstoestand. De verdwenen testtaken zijn daarom geen foutbewijs. De verbindingsreset blijft een onbesliste meting en wordt opnieuw getoetst. Geen drie-flips-aftekening; herhaalde consolebacklog alleen bewijst geen reboot.

H4-herstel, H5 na flips, ontbrekende H1/H3-deelproeven en H6 blijven open. Fysieke netwerk-IRQ blijft uitgesteld volgens L03.

### L13 — extra hardwareadressen bevestigd (planstap 4)

Derek bevestigt Pi 4 `192.168.1.144`, Pi 5 `192.168.1.193` en Radxa `192.168.1.241`. Alle vijf nodes antwoorden met HTTP 200 op `/health`, `/capacity` en `/tasks`. De drie extra boards melden elk drie appcores en een draaiende welcome-app. Per node is de uitgangstoestand bewaard in `node-inventory-20260906.json`. Deze inventarisatie heeft geen taken gestopt en geen kernel geflipt; compatibiliteit en H1–H6 op de extra boards zijn nog niet afgetekend.

### L14 — schone LicheeRV bevestigd; hardwareproeven hervat

Derek bevestigt de schone flash van LicheeRV en vraagt verder te gaan. We herhalen de flipproef vanuit deze uitgangstoestand. De eerdere reset wordt niet zonder onafhankelijke reproductie aan een OS-fout toegeschreven.

### L15 — extra boards: lifecycle en sharing/SMP uitgevoerd

Na de inventarisatie zijn de welcome-jobs op Pi 4, Pi 5 en Radxa gestopt. Alle drie doorstaan twintig start/compute/stopcycli met nul gerapporteerde geheugen- en coreclaims na iedere stop. Daarna draait op ieder board één tweecore-SMP-app naast twee gedeelde bewoners op de resterende core. De computechecksums kloppen; vervangen van één gedeelde bewoner laat de andere met dezelfde bootmarker doorlopen. Bewijs onder [hardware/resume](../reviews/2026-09-06-framework/hardware/resume/), per board `h1-cycles.json`, `h2-smp-compute.json` en `h2-neighbor-survives.json`. Hardwaregeheugenwissen, de volledige wakeproef, flips en H6 op deze extra boards blijven open.

### L16 — herhaalde flips: onderscheid tussen meetfout en productgedrag

- M4: een verse console toont generatie 1 met alle drie oorspronkelijke bewoners. De oude consoleverbinding bleef hangen en miste die boottekst. De eerste meetmethode kon daardoor een geslaagde adoptie niet bevestigen.
- M4: generaties 2 en 3 slagen inclusief dezelfde TCP-socket, appbootmarkers, taakidentiteit en verse agentlogs (`resume/m4/h4-flips.json`). Bij generatie 4 liep de vernieuwde consolemeting tegen de lezerslimiet; geen volledige H4-aftekening.
- M4: een volgende afzonderlijke poging krijgt wel een echte reset op de app-TCP-verbinding. De nieuwe console toont generatie 5 met alle drie bewoners en herstelde taken (`confirmed/m4/console-after.log`, `after-tasks.json`, `h4-failure.json`). **Apps behouden en verbinding behouden zijn dus afzonderlijke uitkomsten; H4 blijft open.** De code activeert netwerkontvangst vóór opslaginitialisatie, slotpublicatie en NAT-herstel. Dat is een concrete verdachte voor een tijdelijke verkeerde aflevering aan de node-stack; de precieze resetoorzaak is nog niet met een pakkettrace bewezen.
- LicheeRV: de herhaling ná Dereks bevestigde schone flash eindigt met een timeout tijdens de bundeldownload; een latere verse console toont een nieuwe koude boot om 15:16:03 UTC en alleen welcome. Dit is nieuw bewijs uit de herhaling, niet de eerder door Derek verklaarde flash. Geen geslaagde adoptie aangetoond. Bundeldownload/geheugenpiek wordt onderzocht; OOM is nog geen bewezen oorzaak.

### L17 — stille console geeft losgekoppelde lezer weer vrij

Concrete bevinding uit L16: de bestaande leesgoroutine sluit de socket bij EOF, maar de uitvoerlus blijft bij een lege console doorlopen en houdt haar lezersclaim vast. `metal/kern/conport/conport.go` laat de bestaande lus nu ook eindigen op die sluiting, via één atomic-bool. Geen nieuwe mutex, goroutine of pollinglus. De gerichte regressie faalt tegen de oorspronkelijke code en slaagt met de wijziging, ook met de race detector (`resume/conport-before.log`, `resume/conport-after.log`). Deze fix is nog niet op hardware geflipt; productiecode, herbouw en hardwarebewijs blijven daarvan afzonderlijk herkenbaar.

De hardwarebewijsmap is expliciet uitgezonderd van gitignore, zodat deze nieuwe aftekeningen en mislukte proeven daadwerkelijk met de wijzigingen kunnen worden bewaard. H6 en eindaftekening volgen pas na de open correctness-punten; er is nog geen releaseklaarverklaring.

### L18 — console-targetbuilds en M4 I/O na flips

De consolefix bouwt als TamaGo-package voor arm64 en riscv64. Hostregressie met race detector slaagt; geen gewijzigde kernel is hiermee op een board geplaatst.

Op M4 slagen na de flips opnieuw drie 1 MiB-netwerkecho's en drie 16 MiB private-filesysteemproeven: bytes/hash kloppen, bestanden zijn verwijderd en de appbootmarker bleef gelijk. Bewijs: `hardware/m4/h5-*-after-flips.json`. Leestijden blijven circa 123 ms. De eerste schrijfronde na flips duurt circa 210 ms tegenover circa 9–10 ms vóór flips; de twee volgende liggen weer rond 9 ms. Dit verschil is nog niet verklaard en wordt niet als afgetekende performancepariteit gepresenteerd. Netwerkmetingen hebben ruime spreiding. Geen ruwe NVMe-/4000 MB/s-claim.

### L19 — netwerkclaims blijven geldig tijdens adoptie; gerichte hardwareherhaling gestart

De nieuwe kernel liet externe RX toe vóór de apppublicaties/NAT hersteld waren. Verkeer naar een overgedragen apppoort kon daardoor tijdelijk naar de node-stack gaan. De switch houdt nu die bestaande poortclaims vast tijdens adoptie: gepubliceerd appverkeer en antwoorden op overgedragen NAT-poorten worden in dat venster geclaimd en gedropt, zodat TCP kan herhalen in plaats van een node-reset te ontvangen. Na slot- en NAT-herstel wordt de tijdelijke verzameling vrijgegeven. Geen nieuwe mutex, worker of pakketqueue; de bestaande switchlock blijft de eigenaar.

De switch-/console-hosttests slagen. De gerichte nieuwe adoptietest slaagt met de race detector. Een brede race-aanroep liep op een bestaande `checkptr`-beperking van de host-ringfixture; dat is geen geslaagde brede race-aftekening. De wijziging plus consolefix zijn in drie afzonderlijke configvolle Apple-bundels gebouwd (`apple-g11` t/m `g13`); de hardwareherhaling loopt en wordt pas na resultaat afgetekend.

### L20 — M4: drie flips met de reparaties en beheer daarna geslaagd

Met beide reparaties slagen generaties **6, 7 en 8** op de M4. De hostsuite houdt exact dezelfde TCP-socket (`192.168.1.208:58262`) over alle drie sprongen; de bootmarkers, taak-ID's, starttijd en restart_count van de twee gedeelde bewoners en de SMP-app blijven gelijk, voortgang stijgt en verse agentlogs komen terug. Bewijs: [fixed/m4/h4-flips.json](../reviews/2026-09-06-framework/hardware/fixed/m4/h4-flips.json). De derde bundel had eerst een expliciet gemelde dial-timeout vóór downloaden/springen; die poging is herhaald terwijl dezelfde appverbinding open bleef. Een verse consoleverbinding bevestigde generatie 8 voor de actieve hostsuite, omdat de eerdere consoleverbinding opnieuw geen EOF gaf. Geen stil weggelaten downloadfout.

Daarna slagen twaalf opeenvolgende consoleverbindingen zonder de lezerslimiet te vullen. Dedicated SMP stoppen geeft aantoonbaar 192 MiB en twee coreclaims vrij; opnieuw starten levert weer twee werkende workers. Eén gedeelde bewoner vervangen laat de buur met dezelfde bootmarker doorlopen. [Beheerbewijs](../reviews/2026-09-06-framework/hardware/fixed/m4/h4-post-flip-management.json), `console-reader-reuse.json` en `fixed/post-fixed.log`.

De gerichte adoptietest faalt zonder de nieuwe ontvangstclaim en slaagt met de fix, ook met de race detector. De volledige bestaande host-/TamaGo-gate slaagt met de wijzigingen (`resume/updated-host-target-gate.log`). Bundlehashes en overeenkomst tussen gewijzigde productiebron en de gebouwde kopie staan in `fixed/flip-build-manifest.json` en `fixed/changed-source-proof.json`.

Dit sluit de getoetste M4-inbound-appverbinding en het beheerpad na drie flips. Het bewijst niet alle uitgaande NAT-scenario's of onbeperkte uptime. Open blijven onder meer LicheeRV-download/uitval (UART-toegang gevraagd), class-constrained sharing, hardwaregeheugenwissen, volledige wake-/I/O-aftekening, de overige boardflips, H6 en de docs-overhaul. De release is nog niet volledig afgetekend.

### L21 — fysieke netwerk-IRQ na de docs, als volgende puntrelease

Derek: “zet er maar op (IRQ) NA de docs :) het wordt gewoon een nieuwe punt release”. De huidige polling is volgens hem snel genoeg; de vervolgstap richt zich vooral op zuiniger draaien. Het afrondingsplan bevat nu dit expliciete vervolg na de docs-overhaul, inclusief boardtests en het bijwerken van de documentatie bij de IRQ-wijziging. De huidige hardware-/correctness-aftekening blijft op polling; fysieke IRQ is geen nieuwe releaseblokkade. Geen versienummer of reeds gemeten energiewinst toegekend. L03 blijft gelden voor de huidige ronde; L21 legt de vervolgvolgorde vast.

### L22 — mogelijkheden en boardbewijs zichtbaar maken in de docs

Derek vraagt een “HIER ZIJN WIJ”-tabel: laten zien wat HopOS al kan en fysieke IRQ als gepland vervolg benoemen. De [werkstand](release-status.md) bevat nu een mogelijkhedenoverzicht en een hardwarematrix, met concrete M4-flipaftekening. QEMU heeft netwerk-IRQ al; de fysieke boards nog niet. Open verificatie blijft onderscheiden van ontbrekende functionaliteit. De tabel wordt bijgewerkt tijdens punten 1–3 en opgenomen in de docs-overhaul; deze toevoeging vervangt of onderbreekt de lopende hardware-/correctness-opdracht niet.

### L23 — fixes bundelen als 2.2.1; LicheeRV daarna hertesten

Derek kiest een normale release **2.2.1** voor de gemaakte fixes. Daarna testen we de boards die de update niet via de oude downloader konden ophalen; momenteel alleen LicheeRV. De eerder voorbereide losse hardware-reviewimage is daarmee niet de gevraagde vervolgstap. De nieuwe downloadcode is hostgetoetst en als target gebouwd, maar blijft op LicheeRV expliciet **wacht op 2.2.1 + hardwarehertest**. Punten 1–3 worden ondertussen op M4, Pi 4, Pi 5 en Radxa doorgezet. Dit scopebesluit verklaart de resterende RV-hardwareaftekening; het is geen geslaagde flipclaim voor dat board.

### L24 — aanvullende geheugen- en wakeproeven; nieuwe kandidaat nog niet afgetekend

Op M4, Pi 4, Pi 5 en Radxa slagen drie geheugenhergebruikcycli: drie afzonderlijke 1 MiB-vensters binnen de eigen partitie zijn eerst nul, worden met niet-nulpatroon gevuld en zijn bij hergebruik van dezelfde fysieke partitie opnieuw nul. De gedeelde buur houdt zijn bootmarker. Dit bemonstert 3 MiB; het is geen hardwarecontrole van iedere byte. Op dezelfde vier boards slagen tien rondes waarin beide gedeelde testapps zonder eigen periodieke voortgangstaak slapen en weer antwoorden. Bewijs: `hardware/completion/<board>/h1-memory-canary.json` en `h3-quiet-shared-wakes.json`.

De kandidaat met classplaatsing, exact-length download, consolevrijgave en adoptieclaims bouwt voor alle vijf boards; de host-/targetgate inclusief Apple slaagt. Ook uitgaande NAT-allocatie wordt tijdens adoptie tegengehouden onder de bestaande lock, totdat de oude mappings terugstaan. Gerichte regressies slagen. De RISC-V-flipbundel neemt nu tevens de cage-startstub mee, via dezelfde buildhelper als de flashimage.

De eerste kandidaatflip behoudt taken en beide getoetste inbound-sockets op M4 (generatie 9), Pi 5 (1) en Radxa (1). De volgende class-sharing/SMP-proef vindt een integratiefout: de hoofdadapter `envSlots` geeft de optionele fysieke plaatsingsvraag niet door. Daardoor blijven SMP-jobs wachten naast het gedeelde pool. Reparatie en herhaling volgen; classplaatsing is nog niet afgetekend.

**Pi 4: nieuwe open flipbevinding.** Download en SHA-controle slagen; de laatste console meldt een geleende 130 MiB-partitie op 0x21200000. Daarna zijn beheer en netwerkconsole onbereikbaar. Er is geen bevestigde nieuwe kernelgeneratie. Een handmatige herstart is gevraagd; oorzaak wordt onderzocht. Dit staat los van het eerdere RV-downloadprobleem en corrigeert de verwachting in L23 dat alleen RV nog niet kon updaten. Bewijs: `completion/pi4/h4-lift-console.log` en `completion-failure.json`.

M4 eerste write na deze flip: 210,34 ms, daarna circa 9 ms. GC-pauze slechts 54 µs; de oude interne system-TCP-verbinding wordt vervangen. De transportstatistiek telt alleen levende verbindingen en mag daarom niet als monotone tellerdelta worden gelezen. Een afzonderlijke metadata-aanroep vóór de volgende bulk-write moet de hersteltijd lokaliseren. Nog geen ruwe 4000 MB/s-aftekening.

### L25 — adapter gerepareerd; twee meetuitkomsten gescheiden

`envSlots` geeft nu `CanPlaceDedicated` expliciet door. De bestaande adapter staat in een afzonderlijk bestand om de daadwerkelijke methodedoorgifte hostgetoetst te houden; die regressie is toegevoegd aan `tools/test.sh`. Gerichte race-test, volledige host-/targetgate en vijftien nieuwe boardbundels (reviewmarkers 31–33) slagen. De HOP-dependencywijziging wordt nog als normale gepinde dependency geïntegreerd; de tijdelijke build-replace is geen release-oplossing.

M4 adopteert generatie 10 en Radxa generatie 2 met beide class-shared bewoners. Beide apps antwoorden nadien met dezelfde bootmarkers. De hostsuite verwachtte ten onrechte generatie 1 doordat haar beginsnapshot de volle consolebuffer afkapte vóór de generatieregel; deze twee timeouts zijn daarom meetfouten, geen mislukte adoptie. De mislukte suite-uitkomst blijft bewaard onder `completion/adapter-fixed`; de vervolgmethode leest de buffer volledig en gebruikt de bevestigde generatie.

**Pi 5 wordt na zijn tweede flip onbereikbaar.** De eerste flip naar generatie 1 was bevestigd; de tweede overgang is niet bevestigd. De bewaarde console is daar bovendien afgekapt vóór het verzoek en lokaliseert de uitval niet. Een herstart en eventuele bestaande UART-toegang zijn gevraagd voor beide Pis. Geen speculatieve driverwijziging. Het Pi4-geheugenvenster overlapt volgens de inventaris geen bewoners; zonder UART kan de laatste netwerkconsole niet onderscheiden tussen een hang vóór de sprong en een nieuwe kernel zonder netwerk. `completion/pi4/flip-review.json` legt die grens vast.

### L26 — M4 laatste fixes: class/SMP, drie bidirectionele flips en I/O geslaagd

M4 slaagt met de gerepareerde adapter: eerst twee `core-class=big` gedeelde bewoners plaatsen, daarna dedicated SMP; buurvervanging, tien stille gedeelde wake-rondes en computechecksums slagen. Daarna slagen generaties **11, 12 en 13** met dezelfde twee inkomende app-TCP-sockets én twee door apps gestarte uitgaande TCP-verbindingen zonder reconnect. Bootmarkers, taak-ID's, starttijden en restart_count blijven gelijk; verse logs komen terug. Na de flips geeft SMP stoppen opnieuw 192 MiB en twee coreclaims vrij; herstart en buurvervanging slagen. Bewijs: `completion/adapter-fixed/m4/h4-three-final-flips.json`, `h4-final-management.json` en de egress-serverregistratie.

De afzonderlijke metadata-aanroep direct na generatie 12 duurt **200,47 ms**; de daaropvolgende eerste 16 MiB-write duurt **9,19 ms**. Bij de andere twee flips, zonder voorafgaande metadata-aanroep, duurt de eerste write circa 212 ms. Daarmee is de eenmalige hersteltijd gelokaliseerd buiten de bulk-write, bij het heropenen van de interne systemverbinding; content/hash en opruimen blijven correct. Dit bewijst geen ruwe 4000 MB/s en geen performancevergelijking met een andere OS-versie.

HOP-classplaatsing is gepubliceerd als **v1.0.2**, commit `8d60d9b`, na de volledige HOP-tests en runner-racetests; de OS-dependency wordt normaal op die tag gepind. Er is geen OS-release gepubliceerd.

De Radxa-herhaling wees dezelfde bundel correct af als “already running”: alleen een ingebakken configmarker maakt op DTB-configboards de ELF niet anders. De markerbundels 31–33 waren daar bytegelijk. Voor de begrensde herhaalproef komen afzonderlijke testbuilds met uitsluitend een extra boottekst, vastgelegd onder `completion/radxa-markers`. Dit is een fixturecorrectie, geen productfix of mislukte kernelovergang.

### L27 — Pi-relocatiecorrecties voor de hertest

In `board/raspi/cpuinit_body.h` gebruikt cache-invalidatie voortaan de door flip gepatchte `RamStart/RamSize`, in plaats van het vaste koude laadbereik. De twee vroege foutvectortabellen gebruiken het reeds bestaande Apple-patroon met absoluut doeladres; de korte branch kon een verder dan 128 MiB verplaatste kernel niet bereiken. Geen nieuw mechanisme of worker. Pi4/Pi5-targetbuilds en beide FLIPABI2-bundelbuilds met relocatiediff slagen; gegenereerde instructies zijn gecontroleerd (`completion/pi-relocation-fix`).

Dit zijn aantoonbare relocatiecorrecties, **geen bewezen oplossing van de hardware-uitval**. De cachefout kan door de voorafgaande scrub worden gemaskeerd; de foutsprong breekt diagnose zodra een exception optreedt, maar bewijst niet waardoor die exception ontstaat. Beide Pis blijven wachten op herstel/gerichte hardwarehertest.

### L28 — Radxa drie flips en normale releasebuild afgetekend; overdracht 2.2.1

Radxa slaagt voor class-shared plaatsing vóór SMP, buurvervanging, tien stille wake-rondes en drie flips naar generaties **3, 4 en 5**. De twee inkomende én twee uitgaande TCP-verbindingen blijven werken, met gelijke appbootmarkers/taakidentiteit en verse logs. Daarna slagen vrijgeven van 192 MiB/twee SMP-coreclaims, herstart en buurvervanging. Bewijs: `completion/radxa-markers/radxa/h4-three-final-flips.json` en `h4-final-management.json`. Netwerk-contentproeven vóór/na slagen; geen NVMe-volume op dit board.

De uiteindelijke productiebron, inclusief Pi-relocatiecorrecties, bouwt met de normaal gepinde **HOP v1.0.2**, zonder tijdelijke replace of testmarker, voor alle zeven fliptargets. De volledige host-/TamaGo-gate inclusief Apple en de echte adaptertest slaagt. Bronmanifest, bundlehashes en bouwlogs: `completion/release-221-check/`. Dit is buildbewijs; het plaatst de Pi-/RV-fixes niet stilzwijgend op hardware.

**Overdracht voor 2.2.1:** fixes en normale dependency-integratie staan in de OS-werkboom; de OS-release/tag zijn niet gepubliceerd. LicheeRV krijgt volgens Dereks besluit de nieuwe release en daarna de download-/flip-/class-hertest. Pi 4 en Pi 5 wachten op herstart en een gerichte flipronde, liefst met bestaande UART voor de ontbrekende overgangsdiagnose. H6 (dertig minuten gecombineerd gebruik), docs-overhaul en eindaftekening blijven daarna in het draaiboek; fysieke IRQ volgt pas na de docs in een volgende puntrelease. Geen volledige hardware- of releaseklaarverklaring.

### L29 — Derek herstart beide Pis; gerichte hertest hervat

Derek bevestigt de herstart. Beide Pis antwoorden weer met alleen welcome. Per board volgt eerst één flip vanuit de koude 2.2.0-uitgangstoestand met live gedeelde bewoners en SMP naar de kandidaat met de Pi-relocatiecorrecties en gepubliceerde HOP v1.0.2. Alleen bij geslaagde overgang volgen classplaatsing en drie herhaalflips. Bij uitval geen blinde herhaling. De afzonderlijke testbundels hebben uitsluitend een extra boottekst om byte-identieke updates te vermijden; bewijs onder `completion/pi-final/`. LicheeRV blijft volgens L23 wachten op de normale 2.2.1-installatie.

### L30 — Pi-hertest opnieuw mislukt; geen vervolgflips uitgevoerd

Na Dereks herstart zijn op beide Pis de SMP-app en twee gedeelde bewoners gestart. De gerichte overgang naar bundel 51 met Pi-relocatiecorrecties haalt de bundel volledig op en verifieert SHA-256. Daarna loopt de bestaande appverbinding vast; een nieuwe kernelgeneratie is niet bevestigd. De laatste ontvangen console meldt het geleende 130 MiB-venster (Pi4 0x21200000, Pi5 0x25a00000). Beheer en netwerkconsole vallen weg. De vervolgflips 52–54 en class-herhaling zijn **niet uitgevoerd**. Bewijs: `completion/pi-final/<board>/h4-lift-console.log`, `completion-failure.json` en `suite.log`.

De Pi-relocatiecorrecties sluiten deze uitval dus niet. De uitgaande kernel draait tijdens de download/plaatsing nog de geïnstalleerde versie; een nieuwe-kernelcorrectie is op zichzelf geen bewijs over dat uitgaande pad. De netwerkconsole vertelt niet of de sprong al gemaakt was. Volgende gerichte diagnostiek vereist UART-uitvoer tijdens één overgang, of een afzonderlijk afgesproken koude installatie van de kandidaat met diagnose vóór een volgende flip. Geen extra herhaalpogingen of speculatieve driverfixes.

Stand punten 1–3: bronfixes, normale dependency-integratie en alle buildgates gereed; M4/Radxa definitieve sharing/SMP-, bidirectionele drie-flips- en beheerproeven geslaagd; vier ARM-boards hebben het geheugenhergebruik-/stille-wake-deelbewijs. M4 eerste-I/O-vertraging verklaard als eenmalig intern transportherstel; geen 4000 MB/s-hermeting. Pi4/Pi5-flipcorrectness blijft open; RV wacht op Dereks 2.2.1-installatie. Het logboek geeft dus geen volledige aftekening of vrijgaveclaim voor alle boards.

### L31 — watchdogverwachting expliciet getoetst aan het beleid

Derek wijst terecht op het ontbrekende automatische herstel: “maar de watchdog hoort dat te solven”. Beide Pi-consoles bevestigen vóór de flip de BCM-watchdog met 12 seconden timeout én `HOPOS_CANARY_LIVE`; hij stond dus niet simpelweg uit in de uitgangstoestand.

De bron bevat wel een concreet beleidsgat: `armBootGuard` blijft onbeperkt voeden totdat `nodeCanary` overneemt, en die blijft vóór zijn eerste geslaagde probe eveneens onbeperkt voeden. Een opstart die intern nog schedult maar nooit netwerk/agent krijgt, hoeft daardoor niet te resetten. Correctie: één gedeelde termijn van twee minuten voor beide blinde flip-opstartpaden, zonder verlenging bij overdracht; daarna geen voeding meer. Bestaande workers, geen extra lock/worker. Intentional reboot moet bovendien de vroege voedingslus stoppen. Hostregressie en een gecontroleerde hardwareproef op M4 volgen.

Dit verklaart een ontbrekende watchdoggarantie, maar bewijst niet welk pad de Pis bereikten. De uiteindelijke self-dial controleert de interne agentroute en dekt volgens het bestaande contract geen uitsluitend defecte NIC-ontvangst. Die grens blijft zichtbaar; een timerfix wordt niet gepresenteerd als bewezen Pi-herstel.

### L32 — watchdog-herstelproef slaagt; normale proef vindt klokfout in de correctie

De M4 boot met de testkopie die bewust na `armBootGuard` blijft schedulen zonder agentopstart. Hij komt autonoom koud terug met welcome na **165,48 seconden** vanaf het flipverzoek. Dat past bij twee minuten voedingsbudget, de 30-secondenhardwaretimer en bootduur. De blackbox droeg na reset nul bytes over: de specifieke stall-/expiry-markers zijn dus niet teruggelezen. Dit is waargenomen herstel met bekende fault-injection; geen directe timingmeting van iedere watchdogwrite.

De daaropvolgende normale flip vindt een fout in de eerste deadline-implementatie: `time.Now` volgt op TamaGo de boardtijd, en SNTP verandert tijdens boot die offset. Daardoor meldt een gezonde nieuwe kernel onmiddellijk `HOPOS_BOOT_GUARD_EXPIRED`. Deze kandidaat wordt niet als correct afgetekend. De deadline gaat naar de bestaande ruwe hardwareteller (`dev.Counter` met `idle.CounterHz`); geen nieuwe klokarchitectuur. Normale boot en de herstelproef worden daarna herhaald. Bewijs van de fout blijft onder `completion/watchdog/normal-clock-jump-failure.*`.

### L33 — ruwe teller: normale flip met SNTP en alle gates geslaagd

De watchdogdeadline gebruikt nu de bestaande `dev.Counter()` en de frequentie uit `idle.CounterHz()`; `time.Now` speelt bij het budget geen rol meer. Twee voedingspaden blijven één deadline delen. De gerichte hosttests met race detector slagen. Een normale M4-flip naar generatie 1 slaagt inclusief SNTP, geadopteerde welcome-taak en `HOPOS_CANARY_LIVE`, zonder vervroegde expiry. Dezelfde code bouwt voor alle zeven targets en doorstaat de volledige host-/TamaGo-gate (`completion/release-221-counter-check`). De gecontroleerde vastloopproef wordt nogmaals uitgevoerd met deze definitieve tellerimplementatie.

De uitgebreide reviewmap is momenteel weer gitignored; de eerdere uitspraken over automatisch meepubliceren gelden daarom niet voor de huidige werkboom. Een compacte aftekening is apart bewaard in [release-evidence/2026-09-06-hardware.json](release-evidence/2026-09-06-hardware.json), met de definitieve [host-/targetgate](release-evidence/2026-09-06-host-target-gate.log). De volledige lokale scripts, consolelogs en mislukte proeven blijven onder de reviewmap beschikbaar.

### L34 — definitieve watchdog: normale boot én automatisch herstel op M4 geslaagd

Met de ruwe teller slaagt eerst de normale flip inclusief SNTP, residentbehoud en levende canary. Daarna volgt dezelfde gecontroleerde vastloop na `armBootGuard`, met de scheduler nog actief en zonder agentopstart. De M4 komt **autonoom koud terug na 167,13 seconden** vanaf het verzoek, met welcome. Geen handmatige reset tijdens deze proef. Ook deze keer draagt de blackbox nul bytes over; de afzonderlijke stall-/expiry-logregels zijn niet teruggelezen. Het gemeten herstel en de vooraf vastgelegde testinjectie blijven herkenbaar van die ontbrekende interne trace.

Bewijs: `completion/watchdog-counter/hardware-result.json`, de twee bundlehashes, bronmanifest en de normale-flipcontrole. Compact meegenomen in [de hardware-aftekening](release-evidence/2026-09-06-hardware.json). De hardwaregeteste watchdogbron heeft SHA-256 `3c0dcae70241465677a1e5e1ad1f581b13c71d923361379e0064d164f063fac8`, gelijk aan de productieboom. Daarna is de M4 teruggezet op de normale kandidaat: flip/adoptie en canary slagen opnieuw, welcome antwoordt met HTTP 200.

Dit sluit het onbeperkte blinde voeden in het getoetste flip-opstartpad. De Pi-uitval blijft open: geen bewijs dat beide Pis in ditzelfde pad bleven staan, en een puur defect NIC-ontvangstpad valt buiten de interne canary. RV-hertest na installatie van 2.2.1 blijft volgens afspraak uitgesteld. De zeven normale targetbuilds, volledige gate en gerichte racetests zijn geslaagd; geen OS-release/tag gepubliceerd.

### L35 — 2.2.1 geplaatst; RV-hertest en Pi5-UART-diagnose gestart

Derek bevestigt de geflashte LicheeRV en daarna de aangesloten Pi5-UART. De werkboom is inmiddels gecommit als 2.2.1 (HEAD `e76ab82`, frameworkcommit `9bf3c74`). RV antwoordt op 192.168.1.150 met alleen welcome en de nieuwe watchdogbeleidsregels. De hertest gebruikt verse builds van deze bron, gepinde HOP-dependency en de bestaande node-ID/MAC; drie configmarkers onderscheiden de RV-bundels zonder productiecode-instrumentatie.

RV: eerst class-sharing, aanvullende geheugencanary en stille wake; daarna drie flips met beide TCP-richtingen, verse logs en vrijgeven/hergebruik. Geen SMP op de ene apphart en geen NVMe-volume. Bewijs onder `completion/rv-221`. Pi5 wordt afzonderlijk met UART `/dev/cu.usbmodem21302` onderzocht; één gecontroleerde overgang, geen impliciete aftekening voor Pi4 op basis van vermoedelijke gelijkenis.

### L36 — Pi5-reeks geslaagd; Derek geeft Pi4 voorrang op RV

Pi5 doorstaat op 2.2.1 met UART de definitieve generaties **7, 8 en 9** met exact dezelfde twee inkomende app-TCP-sockets over alle drie overgangen. De twee class-shared bewoners op core 1 en de SMP-app op cores 2/3 behouden bootmarker en taakidentiteit; tweewerkerchecksums kloppen na iedere flip. Generatie 9 had eerst een expliciete dial-timeout vóór de sprong; de herhaalde aanvraag slaagt terwijl dezelfde sockets open blijven. Daarna slagen buurvervanging, compute op alle drie bewoners en verse agentlogs. Bewijs onder `completion/pi5-uart-221`. Dit sluit de uitgevoerde Pi5-reeks; de eerdere uitvaloorzaak is niet onomstotelijk vastgesteld.

RV: class-sharing, drie canarycycli en tien stille wake-rondes slagen. De eerste flip valt uit, gevolgd door autonome koude terugkeer. Een aparte diagnose met console vooraf en getelde serverwrites valt eveneens uit: de server kan 131072 bytes naar zijn socket schrijven, daarna BrokenPipe; koude terugkeer wordt na 89,30 seconden waargenomen. Geen volledig opgehaalde bundel of nieuwe generatie bevestigd. Grote allocatie/heapgroei is een hypothese, geen bewezen oorzaak. Bewijs onder `completion/rv-221`; verdere diagnose wacht.

Derek: “Pi 4 eerst, hebben we die reeks gehad”. Pi4 antwoordt op de huidige beheercontrole niet. Herstart en verplaatsing van de UART van Pi5 naar Pi4 gevraagd. De Pi5-UART wordt vrijgegeven. Eerst volgt de Pi4-reeks; de eerdere vraag om RV-UART heeft nu geen prioriteit.

### L37 — Pi4 opnieuw bereikbaar; UART-bedrading eerst bevestigen

Derek geeft de Pi4 vrij voor de reeks. Beheer antwoordt met welcome; daarna zijn één tweecore-SMP-app en twee gedeelde bewoners gestart en de computechecksums gecontroleerd. De koude console toont nog het oude watchdogbeleid. De twee voorbereide 2.2.1-testbundels staan klaar.

De geopende UART heeft vooralsnog slechts een oude/onvolledige regel opgeleverd, niet de verse Pi4-jobuitvoer. Derek noemt de mogelijke omwisseling van RX/TX; omwisselen gevraagd, capture blijft open. **Nog geen flip uitgevoerd in deze reeks:** eerst een bruikbare seriële trace bevestigen, zodat een eventuele uitval deze keer gelokaliseerd kan worden.

### L38 — Pi4: drie flips, residentbehoud en vrijgeven/hergebruik geslaagd

Derek stelt een termijn van vijftien minuten en vraagt de UART-reader opnieuw in te stellen. Reader opnieuw geopend op 115200 8N1; geen leesbare verse uitvoer. Pi4 antwoordt wel met alle drie bewoners. Op uitdrukkelijk verzoek doorgetest met een vooraf geopende netwerkconsole; de ontbrekende seriële trace is geen reden meer om de functionele proef uit te stellen.

Generaties **1, 2 en 3** slagen, ieder op de eerste aanvraag, met exact dezelfde twee inkomende TCP-sockets door de gehele reeks. Taakidentiteiten en bootmarkers van de twee gedeelde apps en de tweecore-SMP-app blijven gelijk; computechecksums kloppen na iedere overgang. Maximale gemeten echo-RTT per overgang: **5,220 / 8,014 / 4,323 seconden**. Dit bewijst verbindingbehoud, geen onderbrekingsvrije latencygarantie. Uitgaande TCP is in deze Pi4-reeks niet meegenomen. De koude uitgangskernel logde nog het oude watchdogbeleid; de aangenomen kandidaten zijn van bronrelease 2.2.1, met alleen een geïsoleerde bootmarker om de bundels te onderscheiden.

Daarna slagen buurvervanging zonder herstart van de buren, SMP-stop met vrijgave van **192 MiB en 2048 CPU-shares**, volledige stop tot nul claims, en opnieuw eerst twee class-shared bewoners en daarna SMP op de overgebleven twee cores. Alle computechecksums en een exacte nieuw aangevraagde stdout-marker slagen. De eerste logassertie in het testscript las alleen de eerste SSE-regel (een normale opstartregel); gecorrigeerd door tot de specifieke verse marker te lezen. Geen productiefix nodig.

Bewijs: `completion/pi4-221/h4-final-*`, `flip-live-console.log`, `postflip-neighbor.json`, `postflip-smp-release.json`, `all-released.json`, `final-class-first-smp.json`; samengevat in de publiceerbare hardware-aftekening. De eerdere uitvaloorzaak blijft onbekend. Deze Pi4-reeks is afgetekend; RV-diagnose loopt parallel binnen de afgesproken termijn. H6, docs-overhaul en fysieke IRQ blijven in hun afgesproken volgorde staan.

### L39 — RV-UART bewijst de download-OOM; watchdog herstelt autonoom

Derek sluit de LicheeRV aan op dezelfde USBmodem. De opnieuw geopende reader op `/dev/cu.usbmodem21302`, 115200 8N1, geeft nu een volledige verse boot en fouttrace. Een gerichte controle geeft een kleine ongeldige bundel met correcte SHA; die wordt schoon afgewezen. Aangekondigde lengtes van 1 en 4 MiB met direct EOF geven eveneens een gewone fout. Bij **6.150.136 aangekondigde bytes en nul verstuurde payloadbytes** valt de node uit. Ook vanuit een verse koude boot, zonder de voorafgaande allocatiereeks, reproduceert dit.

De UART meldt exact: `runtime: out of memory: cannot allocate 8388608-byte block (8028160 in use)`, gevolgd door `fatal error: out of memory`. De stack wijst naar `runtime.makeslice` → `kernflip.readBundle` op `fetch.go:81`. Daarmee is deze reproductie gelokaliseerd op de grote downloadallocatie, vóór bodyread, relocatie en sprong. De eerdere hypothese is nu bevestigd als OOM; dit is geen bewijs van een DWMAC-bulkfout. De hardwarewatchdog brengt de node autonoom koud terug met welcome; ook die boot staat op UART.

De eerste correctierichting (64 MiB) is vóór implementatie verworpen: het vaste RV-plan houdt het koude kernvenster buiten de pool, zodat na één lening van 66 MiB geen tweede dergelijke lening meer past. De kandidaat wordt **48 MiB**, op hetzelfde adres, met **206 MiB app-pool** (126 + 64 + 16 MiB). Twee flipvensters van elk 50 MiB passen in de grote regio; de normale plaatsbaarheidscontrole blijft nodig naast bewoners. Geen nieuw bufferbeheer, geen extra worker of mutex. Een nieuw koud flashimage wordt voorbereid omdat de lopende 32-MiB-kernel de kandidaat al bij downloaden niet kan bereiken. **Nog geen hardware-aftekening voor deze correctie.** Bewijs: `completion/rv-length-control`, `completion/rv-221/uart.log`.

### L40 — correctierichting ingetrokken: download hoort in het nieuwe venster

Derek corrigeert terecht het ontwerp: de download moet, net als app-plaatsing, in het gereserveerde nieuwe slot/poolgeheugen landen; het kernelvenster hoeft daarvoor niet groter. De voorbereide 48-MiB-wijziging en bijbehorende budgettest zijn ingetrokken. Het gebouwde kandidaatimage is niet geflasht en uit de aangeboden artifactmap verwijderd. LicheeRV blijft op 32 MiB. De UART-OOM is bewezen, maar de oplossing wordt nu gericht op directe download naar gereserveerd poolgeheugen, met correcte vrijgave bij iedere fout en behoud van hash-/bundelvalidatie vóór de sprong. Dit vervangt de correctierichting uit L39; nog geen fix-aftekening.

### L41 — v2.2.2: bestaande pool-/plaatsingsfuncties hergebruikt; hardware morgen

De download reserveert nu via `BorrowKernWindow` het normale nieuwe kernvenster, kiest via `StageAddr` de tijdelijke plek bovenin, scrubt via `Scrub` en streamt daarheen met een vaste **32-KiB-buffer**. SHA-256 en de bestaande FNV-inhoudssom worden tijdens de download berekend. De bestaande bundelparser leest via `ReaderAt`; `place.Build` blijft de plaatsingsmeetlat. De bestaande device-reader is uit de slotcode naar `dev.ReaderAt` verplaatst en wordt door beide paden gebruikt. Geen grotere kernel, geen tweede allocator, geen extra worker of mutex.

De tijdelijke bron blijft binnen de eigen lening en onder de handoff-staart. De platte bestemming mag de bron niet overlappen; alle bundel-, ELF-, ABI-, resident- en relocatiecontroles blijven vóór de sprong staan. De bestaande lifecycle-afbakening houdt de lening exclusief; iedere terugkerende fout geeft haar eenmaal terug. Het byte-slice-pad voor ingebakken testfixtures blijft beschikbaar via dezelfde parser en plaatsing. LicheeRV blijft **32 MiB kern / 222 MiB app-pool**.

Validatie: hosttests, gerichte racetests voor kernflip/plaatsing/layout, volledige host-/TamaGo-gate voor alle boards, echte RISC-V-flipbundel met dual-link-relocatiecontrole en koud kaartimage slagen. Nieuwe tests dekken begrensde parserreads, een virtuele 64-MiB-ELF zonder die in te lezen, kapotte/afgebroken metadata, late ongeldige relocaties, hash-/lengtefouten, vaste downloadschrijfgrootte en fouten bij reserveren/schrijven. Een bredere losse slots-racerun strandt in de bestaande ownership-fixture op `checkptr` bij `dev.Clear`; de gewone slots-suite in de gate slaagt. Dat is niet als geslaagde volledige slots-racerun geboekt. Tweede code-review vindt geen blokkerende fout in eigendom, overlap of vrijgave.

Derek maakt **v2.2.2** en vraagt het hierbij te houden; de laatste hardwaretest volgt morgen op die release. Geen verdere wijzigingen of hardwareproeven gestart. De eerder geslaagde ARM-reeksen gelden voor hun geteste kandidaten; de nieuwe downloader heeft nog geen hardware-aftekening. Morgen eerst LicheeRV koud installeren (de oude downloader kan zichzelf niet door zijn OOM heen vervangen), daarna foutpad/vrijgave en de drie-flips-reeks met resident-/TCP-behoud, beheer en hergebruik. Vervolgens de resterende release-hardwarecontrole en H6 volgens het draaiboek. Docs-overhaul blijft gepland; fysieke IRQ blijft uitgesteld tot daarna.

### L42 — docs eerst; LicheeRV en laatste hardwarecontrole morgen erachter

Derek zet de volgorde expliciet om: nu documenteren, LicheeRV achter de documentatieronde voor morgen. Het draaiboek is daarmee bijgewerkt, inclusief de verouderde dependency-, Pi- en update-status. De functionele wijzigingen voor v2.2.2 blijven vaststaan.

De documentatieronde heeft een afgebakend begin en eind: inventariseren wat actueel, dubbel of historisch is; één ingang en leesroute maken; visie/contract, architectuur/lifecycle, sharing/SMP, drivers/boards, bouwen/installeren/flippen en beheer/testbewijs onderling consistent maken; de “Hier staan we”-tabel opnemen; voorbeelden, versies en links controleren. Gereed wanneer een nieuwe lezer het beloofde gedrag, gebruik en bewijs kan vinden zonder tegenstrijdige uitleg. Hardware-aftekeningen blijven tijdens het schrijven open waar zij nog ontbreken.

Daarna morgen: release-identiteit vastleggen, LicheeRV koud op v2.2.2, downloadfouten/vrijgave en opeenvolgende flips met resident-/TCP-behoud controleren, resterende hardware-/duurproef afwerken en resultaten toevoegen. Pas dan de eindaftekening. Fysieke netwerk-IRQ blijft het afzonderlijke vervolg na de docs en deze afronding.

### L43 — English documentation in docs/v2; structure before prose

Derek requests the replacement documentation under `docs/v2/`, preserving the existing version for comparison. He clarifies that the documentation must be English and factual, with usage, architecture, the shared caller/network path, board capabilities, and measurements. Approximately 4000 MB/s M4 writes is retained as Derek's measurement, with unrecorded test conditions left unspecified.

The initial Dutch drafts were withdrawn from the new documentation directory and retained as temporary working material. `docs/v2/STRUCTURE.md` now defines the reading order, page responsibilities, architectural thread, and completion criteria. The new folder is explicitly included by `.gitignore`. No product code or existing documentation pages were replaced. The remaining work is to write and check the English pages against this structure; hardware tests remain scheduled after the docs for 7 September.

### L44 — English v2 documentation and website index complete

The new set is under `docs/v2/`, alongside the unchanged previous documentation. Its 13 public pages cover getting started, apps and placement, operations, architecture, lifecycle, the shared network/system-call path, kernel flip, configuration, boards/drivers, development, measurements and status. `index.md` is the website entry; `menu.md` uses the current website viewer's section/link syntax. README, structure and inventory are repository/editorial material outside that menu.

The actual viewer source in the local `gethop.org` checkout was inspected and used to render the new menu and all 13 pages: default `index`, all index routes found, no missing menu pages or broken local links. Across all 17 Markdown files, local links resolve; two JSON blocks parse and eighteen shell blocks pass syntax checking. The documented Go app example builds for ARM64 and RISC-V with the existing TamaGo compiler against the current metal source. No app was deployed and no hardware test was run. Commands and reference fields were checked against the existing source and pinned dependencies; the new docs identify slot ABI 10 and flip ABI 2 separately from release v2.2.2.

Measurements include Derek's approximately 4000 MB/s M4 write result with its provenance and unrecorded conditions, separate from the review's latency and correctness results. The new status page preserves the open hardware acceptance of the new downloader. The old topics are mapped in `docs/v2/INVENTORY.md`; old pages and historical evidence have not been removed or replaced. No product code changed in this documentation round.

The current website still reads `main/docs/menu.md`; it has not been switched or published by this work. The v2 index/menu are ready for that later selection or promotion. The documentation pass is complete as a parallel version; the remaining acceptance work is the v2.2.2 hardware round on 7 September, followed by final sign-off. Physical IRQ remains the separate later task.

### L45 — documentation promoted; LicheeRV sharing investigation remains open

Derek ends today's source review and schedules debugging and the outstanding flip test for tomorrow. He reports failure when two apps share the LicheeRV app hart. The source review has not established the cause; no product fix or hardware acceptance is claimed. The recent wakeAt arithmetic correction, shared slot/context addressing, and register save/restore did not reveal an explanation. Follow up the ownership of CtxWake during forced stop, the mixed-writer control-page cacheline containing CtrlShared and CtrlRXDoor, and per-connection system-service memory use. These are review leads, not established causes of this failure. No UART or new hardware test was performed after the request to review code first.

At Derek's request, the previous documentation tree (including this logbook, the plan and evidence) is moved to docs/v1/, and the English v2 pages are promoted to docs/. Index and menu now occupy the website's existing main/docs/ entry points. Relative Markdown links are adjusted for the move; the repository README points to current usage pages. Previously ignored local review material remains ignored. Publication still depends on committing and pushing these changes; no deployment was performed.

Tomorrow: identify the running release and the two app builds, reproduce the shared-core failure and capture its actual failure path, apply the smallest supported correction, then perform the v2.2.2 pool-download/flip checks. Sharing acceptance remains open alongside the outstanding flip acceptance. Physical IRQ remains the later task.

### L46 — two website follow-ups for release day

Derek plans publication for 7 September and asks to put the website's LicheeRV status and developer-reported approximately 4000 MB/s M4 result on tomorrow's TODO. Both are added to the release plan and the website TODO: complete LicheeRV sharing/FLIP verification, reproduce and document M4 write throughput, then update the public wording from the results. No new measurement or hardware acceptance is claimed today.

### L47 — LicheeRV UART attached; two welcome residents pass the first reproduction checks

On 7 September, UART at `/dev/cu.usbmodem21302` captures the running LicheeRV at 115200 baud. Initial state: one welcome job, one app hart, 222 MiB pool, 32 MiB kernel window; the agent identifies itself only as `hopos-dev`, so the precise installed kernel artifact is not established. The welcome page identifies its app build as `2026.09.06-1805`, Go 1.26.4, riscv64.

The existing welcome job was changed to sharegroup `rv-release-share`, then a second welcome job was added on port 18080. Each requests 64 MiB and 1024 CPU shares; they occupy slots 2 and 3 on the single app hart. Both report `core: shared` and serve HTTP. Ten quiet-wake rounds, three neighbor stop/replacement cycles and 600 requests across four concurrent HTTP clients pass. The surviving welcome task keeps its identity through replacements; both task identities remain unchanged during the traffic check. No fault or panic appears in the captured UART for these checks.

This does not reproduce Derek's reported failure and is not a fix sign-off. The exact failing app pair and operation have been requested. No product code changed. Local evidence: `../reviews/2026-09-07-licheerv-sharing/` (`uart.log`, original jobs/tasks/capacity, submitted job definitions, `welcome-check.json`, `traffic.json`, boot-console capture). The original welcome definition is retained for restoration. Sharing investigation and FLIP acceptance remain open.

### L48 — exact Stulp sequence reproduces kernel OOM; one-buffer candidate ready

Derek supplies the existing root jobs: cloudflared-lean (32 MiB), Stulp (48 MiB), then stulp-plugins (126 MiB), all in sharegroup `huis`. Welcome and the synthetic neighbor are removed; the three definitions are submitted unchanged with three-second intervals. Secret environment values are read only for submission and are not copied into review evidence. UART records cloudflared-lean and Stulp starting, then kernel OOM during the plugin download: `leantls.readRecord`, with a failed 4-MiB arena allocation and 12,091,392 bytes in use. Both system-service goroutines retain 0x110000-byte input buffers and each connection also owns a maximum read-response buffer. This establishes memory pressure during startup, not a switcher fault.

An attempted candidate installation then exposes the old flip downloader on the initially installed kernel: `kernflip.readBundle` allocates the whole 6-MB bundle and fails on an 8-MiB allocation. No candidate is installed. The watchdog recovers the node. Derek subsequently reports flashing v2.2.2. The same three-job sequence reproduces kernel OOM again, now in `leanelf.File.section`/`symtab` during plugin placement (4-MiB arena allocation, 12,156,928 bytes in use). The exact initial kernel release was not exposed by its `hopos-dev` identity; the second run is identified by Derek's v2.2.2 flash and the changed stack addresses. These are two observed allocation sites under pressure, not proof that TLS or ELF parsing alone caused the total memory demand.

The first candidate merely made two connection buffers lazy; Derek asks to establish why they exist before settling on that fix. That candidate is superseded and was not installed. The current candidate uses one reusable connection work buffer. Requests are decoded before a read response reuses their storage; DecodeReq copies the path and OpRead does not consume request Data. The response is sent before reading the next request. Allocation grows only to the current message/read size. No new lock, worker, ABI change or larger kernel window. Small response encoding and TCP buffers remain; this does not claim zero kernel-side memory.

Validation: full host/TamaGo gate passes for all existing target variants. Targeted race checks pass. Tests cover small log/storage-less calls without maximum buffer allocation, maximum-to-small frame transitions, and real hopfs sparse-file reads reusing request storage across large/small/zero reads while preserving response identity and data. Hardware acceptance of the candidate remains open.

Candidate image: `metal/out/hopos-licheerv-single-buffer.img`, SHA-256 `3b5d74535e6ca29266e9b4104af058465b51b1598de767c6e57121cbbd02510c`. It retains the 32-MiB cold kernel and 222-MiB app pool. The exact three-job set requests 206 MiB; a relocated kernel consumes additional app-pool space, so this workload must first be tested from the cold candidate. No card was written. Local UART, both reproduction records, manifests and build/gate logs are under `../reviews/2026-09-07-licheerv-sharing/`. Next: cold candidate, identical three-job sequence, verify application behavior and sustained progress, then a separate FLIP test with enough free staging space. Public status must reflect those outcomes before sign-off.


### L49 — single-buffer candidate passes the original startup and replacement reproduction

Derek writes the approved candidate to the card and reports it running. From that cold installation, welcome is removed and the three unchanged root job definitions are submitted three seconds apart: cloudflared-lean 32 MiB, Stulp 48 MiB, plugins 126 MiB, sharegroup `huis`. All three start and remain running on the one app hart. The plugins are then stopped and replaced three times; both neighbors retain their task IDs and the agent reports app claims returning from 206 MiB to 80 MiB and back. UART contains no kernel OOM in these runs. This closes the reproduced startup allocation failure on this candidate, without changing idling, sharing, the app ABI tail or the cold kernel window.

Application logs reveal a separate configuration issue: the unchanged cloudflared origin and plugin attach address point to `10.100.0.2`, whereas this placement puts Stulp at `10.100.0.4`. Process survival is therefore not evidence of successful plugin attachment or a working tunnel origin. Those endpoints need the actual service or its published node ports.

### L50 — revised RV downloader and four live flips pass; cold-window limitation measured

At Derek's request, the application trio is removed temporarily and a 48-MiB hardwarecheck resident is installed. The corrected 6.16-MB bundles download into pool staging and produce generations 1–4, preserving the same resident task ID and application boot identity. The exact same host socket carries verified echo payloads across generations 1–2; a new socket carries them across generations 3–4. The first request for generation 3 fails before downloading with a TCP dial timeout to the local artifact server; the resident and management remain available. Retrying succeeds. This is four successful swaps with one separately recorded rejected request, not a clean three-flip single-socket acceptance. Maximum observed echo RTT is approximately 3.45 seconds during the swaps.

A full-bundle wrong-hash request and an identical-bundle request are subsequently rejected. App claims remain unchanged; a fresh app starts, serves status and stops afterward. The next successful destination loan also establishes that the temporary loan was released. No download OOM occurs with the revised path.

The destination windows alternate between `0x8dc00000` and `0x8ba00000` (34 MiB allocator reservations, including the handoff). They do not return to the original `0x84000000` cold window: that remains a board-plan hole. Thus the original 206-MiB workload no longer fits after these flips; the nominal 222-MiB capacity field must not be read as current placeable memory. Reclaiming or reusing the cold window remains a separate memory-plan issue. No allocator change is included here.

A subsequent, explicitly requested cold-restoration image with `hopos.reboot=1` lands as generation 5 but fails to reset: its early reboot call precedes the LicheeRV watchdog probe. UART explicitly reports `HOPOS_REBOOT_UNAVAILABLE`; welcome being seeded does not establish a cold boot. The correction retries the existing reboot function after the existing board probe, before starting the canary. Its hardware result is recorded separately below; the four successful live flips above precede this additional change.

Public compact evidence: [RV startup and FLIP results](release-evidence/2026-09-07-licheerv.json). Local detailed records: `stulp-single-buffer.json`, `single-buffer-replacements.json`, `single-buffer-flips.json`, `flip-rejections.json`, `postflip-reuse.json`, artifact manifests and UART under `../reviews/2026-09-07-licheerv-sharing/`. No secret environment values are published.


### L51 — explicit cold restoration completed; kept separate from FLIP acceptance

The normal `hopos.reboot=1` option already existed. It was used only to restore capacity after the completed live-update tests, not to make those swaps pass. Derek questions this detour; the unresolved issue is reuse of the original cold window, and reboot is not its fix. A minimal retry of `rebootNow()` after `boardWarn()` allows the existing LicheeRV watchdog probe to complete before the request is retried. The full host/target gate passes with this change. The restoration bundle lands as generation 6, arms the watchdog after its probe, and subsequently cold-boots the flashed single-buffer image at `0x84000000..0x86000000`. Observed restoration time is 108.8 seconds including download/placement/reset/boot. This deliberately ends app continuity and is excluded from the four successful live-flip results.

The three original app definitions were restored unchanged, again with three-second submission intervals. All three report running with zero restarts. Their hardcoded service endpoints are left unchanged; the integration limitation in L49 remains. The retry is an additional uncommitted source change in `metal/cmd/hopos/main.go`, not part of the image on the SD card. Local evidence: `cold-restoration-after-probe.json`, `reboot-after-probe-artifact.json`, `gate-after-reboot-order.log`, and `stulp-restored-after-flips.json`. No commit or release was made.


### L52 — remove the explicit reboot path; online updates use FLIP

Derek rejects deliberate reboot as an update or capacity-restoration mechanism. Remove both boot-time `hopos.reboot` checks, `rebootNow`, the optional `wdHardware.Reboot` callback, its Apple wiring/helper, and the test dedicated to this removed feature. Remove the current configuration entry and mark the archived entry as historical. The late-probe retry from L51 is withdrawn with the rest of this path. Automatic watchdog recovery from faults and the bounded FLIP boot guard remain: they are not the explicit reboot command.

The L49–L51 hardware observations remain historical evidence, not the prescribed update procedure. No additional board reset or deployment is performed for this removal. The current cold-window allocation limitation remains open and must be resolved by memory ownership/reuse through FLIP. Validation of this removal is recorded below.

Validation: `sh tools/test.sh` passes the complete host and TamaGo target gate after removal (`../reviews/2026-09-07-licheerv-sharing/gate-remove-reboot.log`). Source search finds no remaining explicit reboot option, callback or helper in `metal/`, `image/` or `tools/`. `git diff --check` passes. Changes remain uncommitted and have not been deployed.


### L53 — LicheeRV releases the cold window; four FLIPs and full workload restoration pass

Derek specifies the ownership rule: a departed kernel relinquishes its window like a departed app, and a second flip may return to the original window to restore the board's memory geometry. The common partition allocator now rebuilds ownership from the reusable board regions and excludes only the active kernel. A board can declare its cold kernel region as reusable after departure; that region receives the same alignment, overlap and persistent-administration checks as pool regions. LicheeRV declares its existing 32-MiB window. Its fixed administration is outside that window. Other boards are not opted in blindly: some retain boot data or vectors inside their cold reservation.

The cold region is available to normal app allocation. Kernel borrowing prefers its original base only when the requested span is entirely free; an app there retains its ownership and forces a different destination. Borrowing, rollback, scrubbing, staging and adoption reuse the existing paths and lock. The LicheeRV reservation includes the existing 256-KiB handoff tail rather than growing the reservation for it. Runtime RAM after the transition is 31.75 MiB inside a 32-MiB reservation. Capacity reporting excludes the current kernel claim. Pool source ordering and boundaries are preserved for existing placement behavior; normal release still coalesces neighboring free claims. No new worker, mutex, reboot path or handoff fields.

Host regression tests verify a resident across allocator reconstruction, return to the cold window, rollback of a failed loan, the 32/48/126-MiB placement order after return, and an app occupying the cold window without being displaced by FLIP. Layout tests reject reusable kernel overlap with pools or persistent scratch and reject misalignment. The full host/TamaGo gate passes; targeted slots race tests pass.

Hardware starts from the already running single-buffer cold image. The three jobs are temporarily removed and a 48-MiB test resident is installed. Four live swaps pass on ONE continuously held host TCP socket, checking payload and application boot identity throughout; the task identity also remains unchanged. Generation 1 inherits the old downloader's 34-MiB loan at `0x8dc00000`. Generation 2 returns to the original `0x84000000` in a 32-MiB reservation. Generation 3 uses `0x86000000`; generation 4 returns to `0x84000000` again. No reboot is used. After stopping the test resident, all three unchanged user jobs start in the requested order, three seconds apart: 206 MiB used out of 222 MiB, including the 126-MiB plugins. This supersedes the cold-window limitation and paired-socket limitation in L50. The supplied application endpoint configuration remains a separate integration issue.

Evidence: [window reuse and four-flip results](release-evidence/2026-09-07-licheerv-window-reuse.json); local `window-reuse-hardware.json`, `window-reuse-artifacts.json`, `gate-kernel-reuse.log` and UART under `../reviews/2026-09-07-licheerv-sharing/`. The public record identifies both tested bundle hashes and source hashes. The running candidate includes reboot-option removal. No commit or published release is made.


L53 follow-up: two further plugin replacement cycles pass on the returned kernel. The third replacement stalls during artifact download at 2,676,544 of 10,642,201 bytes and exceeds the 60-second test wait; it is not recorded as a passed cycle. No UART OOM or node reset occurs. Cancellation removes the task and releases its 126-MiB claim. Submitting the identical definition again streams and places the image in 5.62 seconds; all three apps run again, and both neighbors retain their original IDs. This is a separate unresolved transport-progress observation, not a failure to reclaim the kernel window. Records: `window-reuse-replacements.json` and `reuse-plugin-download-retry.json`. Do not turn the successful FLIP/window result into a blanket transport acceptance.


### L54 — transport isolation and a reproduced false-idle timeout

Derek requests investigation of the isolated download stall. Both neighboring apps remain resident throughout the replacement tests. Ten downloads from the original GitHub RISC-V artifact and ten from a local plain-HTTP mirror complete. The actual RISC-V ELF is 10,642,201 bytes, SHA-256 `d4b37244d2b215f6c7329124801010c3ebaaef4707c067181a7b7c21d8da257a`. An initial size comparison accidentally selected the ARM artifact from the multi-architecture job; it does not establish a source change. The first ten-cycle script still selected the original RISC-V URL, so its evidence is classified as original-source coverage, not local HTTP coverage.

A controlled server sends 3 MiB and then goes silent while retaining its connection. The existing timeout aborts that download after approximately 62.8 seconds including setup, reports the actual 3,145,728-byte count, and permits claim release. Both neighbors remain unchanged. Thus permanent failure of the basic blocked-read close path is not reproduced. TCP capture is restricted to the authorized node; raw captures and job environment remain local, unpublished test evidence.

Source review finds a separate concrete defect in HOP v1.0.2 `internal/runner/hopos_stream.go`: its idle timer is refreshed only through batched UI progress reporting (at least 256 KiB), rather than every positive read. A deterministic virtual-time test fails against the old reader: it stops after 9 KiB even though 1 KiB arrives every second. Hardware confirms the contract violation: after a 3-MiB prefix the server sends 1 KiB each second, yet the old node reports “no data for 1m0s” after 3,206,144 bytes (61.6 seconds total). This establishes a false-idle bug. It does not prove the cause of the earlier isolated 2.7-MB observation, which did not recur in the ordinary download repetitions.

The minimal correction moves only the progress-report batching: every positive read now refreshes the existing idle timer; UI reports retain the same batching threshold. No extra timer, goroutine, mutex, retry policy or buffer is introduced. HOP v1.0.2 was checked out for review at `/Users/derek/Git/hop`, branch `fix/download-idle-progress`; code and regression test are uncommitted. `go test ./...` in HOP and targeted runner/HTTP race tests pass. The HopOS candidate gate also passes using an isolated build-time local HOP replacement; the production `metal/go.mod` still pins published HOP v1.0.2.

The candidate is installed with FLIPs 5 and 6, keeping cloudflared and Stulp task identities and the same open Stulp HTTP connection; generation 6 returns to the original kernel window. The test harness initially compared task lists by order, which is not stable; comparing the recorded identity sets confirms preservation. The hardware slow-stream and silence tests against this candidate are recorded next. Local records use the `transport-*` prefix under `../reviews/2026-09-07-licheerv-sharing/`.


### L55 — per-read idle tracking passes hardware; release dependency still to publish

The identical slow-stream fixture succeeds with the HOP correction: 3 MiB immediately, then 1 KiB every second for 70 seconds, followed by the remaining image. The plugins start after 81.54 seconds, whereas the previous kernel incorrectly aborted the same fixture at 61.62 seconds. A genuinely silent stream still aborts normally after 61.71 seconds including setup; its 126-MiB claim is released and both neighbors remain running. Ten subsequent downloads of the original RISC-V artifact from GitHub pass on the corrected candidate. Cloudflared and Stulp preserve the same IDs across the entire investigation, including both candidate FLIPs, both deliberate failure fixtures and all replacements. The final three jobs use the original definitions again.

This closes the reproduced false-idle defect with before/after evidence. The original isolated 2.7-MB event did not recur in 20 ordinary original-source downloads (10 before, 10 after) or 10 local downloads. No unproven TCP/NIC cause is assigned and no Lean changes were made. This is not an assertion that every transport fault is impossible.

Release handoff: the fix and regression test are in `/Users/derek/Git/hop`, branch `fix/download-idle-progress`, based on published v1.0.2. No commit/tag/push was made. Publish that HOP change, then update the production HopOS requirement from v1.0.2 to the actual published version and verify the release build. The tested kernel candidates used an isolated local replacement; `metal/go.mod` has deliberately not been pointed at a nonexistent tag or a local checkout. The running board has the tested candidate without a reboot.

Public evidence: [transport before/after results](release-evidence/2026-09-07-licheerv-transport.json). Local original-source/local cycles, controlled-server records, candidate manifests, gate output and UART use `transport-*`, `hop-transport-tests.log` and `gate-transport-candidate.log` under `../reviews/2026-09-07-licheerv-sharing/`. The packet capture covers only the authorized node and is not published.

### L56 — website cleanup is an explicit release acceptance item

Derek requests that temporary placeholders and investigation wording be removed from the public website before release. A statement that was accurate during debugging must not remain as a stale description of the product after the issue has been resolved. Add this check to both the canonical release plan and the website TODO. Describe verified capabilities and concrete remaining limitations on public pages; retain debugging history and superseded findings here. In particular, the LicheeRV framework status must reflect L53–L55. The separate application endpoint integration check remains open. This entry records the requested release check, not completion of the website cleanup or publication.


### L57 — requested application deployment on user-installed 2.2.3

Derek reports LicheeRV at 192.168.1.150 running release 2.2.3. Remove the welcome job and submit the unchanged root definitions for cloudflared-lean, Stulp and Stulp-plugins in that order, waiting three seconds after each submission before the next. All three reach running with zero reported restarts; claimed app memory is 206 MiB of 222 MiB. This verifies startup only; Derek will check Stulp application behavior. The API health check succeeds but does not independently expose the release version. Local evidence: `../reviews/2026-09-07-licheerv-sharing/v223-app-install.json`. No build, flash, FLIP or commit is performed.


### L58 — corrected application submission order

At Derek's request, delete all three jobs and wait until their tasks are removed. Submit the unchanged root job definitions in the corrected order: Stulp, Stulp-plugins, cloudflared-lean, waiting three seconds between requests. All three reach running with zero reported restarts. This supersedes the deployment order in L57; application behavior remains for Derek to check. Local evidence: `../reviews/2026-09-07-licheerv-sharing/v223-app-install-reordered.json`. No kernel update or reboot is performed.


### L59 — cage IDs and physical cores are independent; two RV FLIPs and ordered app deployment

Derek observes that the first shared app receives 10.100.0.3 despite cage 1 being free. HOP deliberately skipped IDs through the physical core count to protect an old SMP coupling: primary cage ID equalled primary core, and secondary CPU contexts occupied other cage context blocks. This was an allocation rule, not evidence of failed release. Derek requests independent cage/core assignment, a final simplification review, then removal of apps, two FLIPs, and unchanged Stulp → Stulp-plugins → cloudflared-lean submissions three seconds apart.

HOP now reserves one first-free cage per app, including SMP and adopted tasks. The node independently selects the physical dedicated core run or trusted group pool. Secondary CPU context IDs use the remaining values in the existing resident byte, and their contexts occupy previously unused space in the existing per-core administration blocks. No new memory region, worker, mutex or handoff field is introduced. Cage table rebuild preserves an unrelated secondary context. Start initializes the trusted sibling wake chain; dispatch, wake, stop/release and FLIP adoption use the actual physical assignment. A QEMU run exposed the remaining app-side assumption: existing apps send cage-relative virtual CPU requests. The kernel now validates and translates those requests, retaining compatibility with existing app binaries. The ARM switch code changes; its existing compatibility check prevents transferring old incompatible live contexts.

Simplification review removes shared-ID offsets, multi-cage task reservations, the optional physical-placement hint and its forwarding adapters, and obsolete cage/core overlap restrictions. Physical capacity and class checks remain in the existing node allocator. Node-reserved physical cores affect core placement and advertised classes, never cage IDs or IP addresses.

Validation: HOP full tests and runner/agent race tests pass. HopOS host tests and full TamaGo target matrix pass. Slots/stage2 race tests pass with checkptr disabled for the existing uintptr-based host MMIO fixtures; the initial default race invocation stopped at that fixture's pointer instrumentation, not a reported data race. Regression tests cover separate context storage, sibling wake routing, existing app CPU request translation, arbitrary cage/core adoption, physical overlap rejection, and complete core release. QEMU actual-agent acceptance places six shared apps in cages 1–6 on core 1 and a two-core SMP app in cage 7 on cores 2–3. Concurrent checksummed compute, three timer/wake rounds, live FLIP retaining all task/boot identities and the existing SMP TCP socket, adopted stop, neighbor preservation, cage-1 reuse and zero final claims pass.

Hardware: on Derek's user-installed 2.2.3 LicheeRV, remove apps and verify zero memory claims. Two uncommitted candidate bundles pass FLIP: generation 1 uses 0x82000000, generation 2 returns to the original 0x84000000 window. The first empty handoff caused the existing init-job configuration to seed welcome; that resident survived the second FLIP. The harness's expectation of an empty post-FLIP task list was corrected in the record rather than counted as a kernel failure. Remove welcome, then submit the unchanged root definitions in the requested order with three seconds between requests. All three run with zero reported restarts: Stulp cage 1 / 10.100.0.2, plugins cage 2 / .3, cloudflared cage 3 / .4. App claims are 206 MiB of 222 MiB. Derek checks full Stulp behavior.

The build uses an isolated local HOP replacement; production metal/go.mod is not pointed at a local checkout or unpublished tag. HOP publication, the actual dependency pin, and final release artifact signoff remain necessary. No commit, tag, flash or reboot was performed. No physical ARM SMP acceptance is claimed from QEMU. Public evidence: [cage/core separation](release-evidence/2026-09-07-cage-core-separation.json). Local records use the cage-/hop-cage- prefixes under ../reviews/2026-09-07-licheerv-sharing/.


### L60 — Derek accepts Stulp behavior on the LicheeRV candidate

Derek reports that Stulp works correctly again with no observed regression after the L59 deployment. Record this as operator acceptance of the supplied application setup, distinct from the automated startup, compute and FLIP evidence. The application integration item is closed for this tested candidate; this is not an exhaustive plugin-protocol certification. Remaining work is published HOP dependency integration, physical ARM acceptance of cage/core separation and the changed SMP switch, the agreed H5 I/O comparisons and H6 combined run, website/documentation cleanup, and final artifact signoff. IRQ remains the later point-release task.


### L61 — replace the global 16-GiB logical index limit with compact extents

Derek reports SQLite backup failure at offset 8,349,814,784: the global index has 4,194,278 of 4,194,304 entries and needs another 256. Derek clarifies that one roughly 8-GB database is being backed up; source plus backup plausibly accounts for the aggregate limit. The error itself establishes index exhaustion, not physical disk exhaustion. The old index represented every logical4KiB position, including sparse holes, with a uint32 entry; its July memory guard capped all logical file spans at16GiB regardless of the roughly400GB disk window. The earlier storage review missed this capacity restriction.

At Derek's request, replace per-block arrays with sorted sparse extents and a coalesced physical free-range list. Adjacent mappings merge only when both logical and physical addresses are contiguous. Sparse length changes need no index entries. Existing bump-first allocation and transfer batching remain. No new lock or background worker. Bound live file extents to262144 rather than total logical blocks; compact shortened file/free arrays when backing capacity exceeds twice their live length. This bounds fragmentation metadata, not all node heap allocations.

The same path now zeros the retained partial block when truncating, preventing discarded bytes reappearing on extension. Fresh block mappings become visible only after successful disk writes; failed fresh writes return the run without exposing previous-owner bytes. Successful earlier chunks remain valid if a later I/O fails. Disk-window and arithmetic bounds remain.

Before/after host evidence: old code fails a9GiB source plus9GiB backup at the16GiB aggregate boundary; new code succeeds with two file extents. A simulated400GiB device fills with one contiguous extent, refuses further physical allocation, releases fully and can be reused. Sparse endpoint writes, zero-I/O sparse truncate, randomized fragmented I/O versus an independent byte-array model, truncation/re-extension, injected write failures, metadata-budget rollback and two-sided merging at the budget pass. Existing1MiB batching tests, HopFS/slots host tests, HopFS race tests and the full host/TamaGo target gate pass. This is simulated capacity coverage, not a hardware throughput result.

A review M4 FLIP bundle and a private-root9GiB source/copy/readback SHA256 hardware fixture are built. The isolated build includes the pending local HOP dependency changes. No device has been modified for this HopFS change. Pending clarification: confirm the target (presumed192.168.1.122) and that temporary filesystem contents may be rebuilt. The current kernel initializes a fresh HopFS index after FLIP; this change does not add metadata transfer. Do not describe existing database files as preserved by that update.

Public record: [HopFS extent evidence](release-evidence/2026-09-07-hopfs-extents.json). Local before/after logs and fixture source are under ../reviews/2026-09-07-hopfs-extents/. No commits, tags or publication.


### L62 — M4 FLIP installed; Spin and cloudflared restored for Derek

Derek confirms his backup is complete and explicitly authorizes the one-time transition without filesystem migration or compatibility work. Save the original job definitions locally with restrictive permissions, stop their tasks, and verify released app claims. M4 accepts the extent-index candidate and adopts kernel generation2 at0x100dbc00000; the original configured NVMe window is detected (395188MiB, LBA19659256..120827418). No reboot or cold flash.

The private9GiB source/backup fixture starts but faults before reporting progress (vec4, ESR0x2000000, FAR0), repeating under the existing task restart policy. Its cause is not established; do not count this as physical storage acceptance or infer database corruption. The kernel remains reachable. Derek requests Spin and cloudflared back for his own test. Stop the fixture and its host harness, then submit the saved original Spin definition followed three seconds later by cloudflared. Both reach running with zero reported restarts. Existing HopFS contents were allowed to be discarded; no metadata migration was added. Original job definitions containing credentials remain private and are not included in public evidence.

The FLIP is accepted, host coverage remains green, and physical database/backup acceptance is pending Derek's check. Updated public [extent evidence](release-evidence/2026-09-07-hopfs-extents.json); local console and task records under ../reviews/2026-09-07-hopfs-extents/. No commit or publication.


### L63 — correction: running was not application acceptance

Derek reports that the restored services do not work. The initial L62 task-state snapshot was too early: Spin, cloudflared and the storage fixture faulted shortly after startup on the first M4 extent candidate (ARM vector 4, ESR 0x02000000). Stop the restart loops. Add minimal ARM fault-PC reporting using the dead context's existing resume word; normal ARM and VHE target builds pass. An empty FLIP to this diagnostic candidate reaches generation 3. Spin then responds with HTTP 200 and no restarts during observation; this behavioral change alone does not establish the original fault's cause. The changed switch-code publication path remains under investigation.

A separate restoration error is confirmed from cloudflared's logs: its configured origin is 10.100.0.3:80, while restoring Spin first assigned it 10.100.0.2. Preserve the original cloudflared-first, Spin-second deployment order. Cloudflared registers tunnel connections; this does not by itself prove origin reachability. Spin was subsequently deleted through the API, so check operator activity before replacing that task. Physical large-copy acceptance and sustained end-to-end service acceptance remain open. No reboot, commit or publication.


### L64 — original IP order restored; Derek confirms service operation

Derek confirms that the Spin deletion in L63 was his own restart to restore the correct order: cloudflared first (cage 1, 10.100.0.2), Spin second (cage 2, 10.100.0.3). IP addresses matter because the tunnel origin is configured explicitly. Read-only verification finds both original jobs running with zero restarts and Spin's local port 80 returning HTTP 200. Derek reports no issue. No additional app replacement or FLIP is performed after his confirmation. Future restoration must preserve this order.

This closes the immediate service-restoration request. It does not establish why the initial ARM candidate faulted before the diagnostic FLIP, or complete the physical large-copy test. Keep those acceptance items distinct. Local final snapshot: /tmp/hopos-hopfs-extents/restoration-check.json.


### L65 — complete board support checklist, including O6N

Derek requests the previously planned per-board support inventory and changes the post-release follow-up from network IRQ alone to the complete small contract. Inspect the current board interfaces, architecture idle paths, board startup/device hooks and build selectors. Publish the English [support checklist](../../support.md), link it from the main documentation index, board reference and status page, and mark the old support matrix as historical.

The inventory separates source wiring, active fallbacks, absent optional facilities and actual hardware acceptance. It covers core/memory ownership, idle/wake, clocks/DVFS, temperature, NIC/IRQ, PCIe/NVMe, framebuffer/input, watchdog, entropy and FLIP. O6N has its own rows and follow-up items: inherited UEFI support does not transfer Altra hardware evidence. Its image/probe route exists, but the standard target gate and FLIP bundle selector lack an explicit O6N target. Its new SCMI thermometer is present; the inspected startup does not yet call the existing clock helper. Correct the stale UEFI bus-0 storage description: the current Disk implementation scans the configured MCFG hierarchy.

Update the canonical release plan to select later work from the whole checklist; retain current correctness/acceptance obligations and keep optional peripherals outside the current compute-release promise. No hardware operations, builds, code changes, commit or publication. Documentation links and diff formatting checked.


### L66 — Orion O6N board profile: source complete, first hardware probe pending

Derek reports that the Orion O6N has arrived and asks for a board profile with idle, LAN (one port suffices), dynamic clocking where the firmware needs it, temperature, NVMe and framebuffer, with drivers taken from the Linux references. The board boots through Cix's EDK2 firmware in ACPI mode, so the profile is the generic UEFI path plus board knowledge, not a new boot chain. Research before code: the mainline `r8169` driver for the Realtek RTL8126A/RTL8125 family and the Cix EDK2 ACPI sources, mainline `sky1` device tree and community boot logs for the platform. Both dossiers are archived under [rtl8126-recept.md](../archief/rtl8126-recept.md) and [orion-o6-platform.md](../archief/orion-o6-platform.md).

Source changes. A new `rtl8126` polled NIC driver covers the RTL8126A (Orion O6) and the RTL8125B/D/CP/BP (Orion O6N revision) by chip XID, with legacy 16-byte descriptors, no offloads and no PHY firmware blob. The UEFI HOP profile now selects its NIC from a driver table (igb or rtl8126), discovers NVMe behind root ports through the MCFG hierarchy with a carve-resident DMA region, and excludes its own MADT index from the app-core list, because the Cix firmware boots on an A720 that is not the first MADT entry. The ACPI reader gains the GICC efficiency class and redistributor base, the GICR range and ITS, a 16550-aware SPCR decoder, and an AML scanner for `_CPC` packages. A minimal SCMI shared-memory client talks to the SCP over the mailbox channel the board's own DSDT uses for `_TMP`. The `o6n` profile (`-tags o6n`, `BOARD=o6n image/uefi-run.sh`) derives core classes from MADT efficiency classes or, when the firmware leaves them empty, from `_CPC` highest performance; mirrors the console to the UART2 debug header; reports the hottest SCMI CPU sensor on the heartbeat; and raises every DVFS domain to its `_CPC` maximum at startup, with `hopos.mhz=` and `hopos.clock=firmware` as configuration knobs. The SBSA watchdog refresh now also rewrites WOR, following a reported Cix refresh-frame defect. The pool builder subtracts every non-usable memory descriptor to tolerate the overlapping descriptors this firmware emits. Target gate, release recipe and FLIP bundle include the O6N build.

Same day, at Derek's request, the NIC interrupt joins the first probe so that the board is not flashed twice for it. The GICv3 adapter moves from Group 0 to Group 1: with TF-A on EL3 the distributor runs with `DS=0`, Group 0 is the secure world and its CPU-interface registers are unreachable from the non-secure OS. The adapter now sets the IGROUPR bit, enables non-secure Group 1, uses `ICC_IAR1`/`ICC_EOIR1`, no longer clears every distributor line at start, and finds the redistributor frame of the calling core by MPIDR in the MADT GICR range (128 KiB or 256 KiB frames). The UEFI profile gains a generic wiring step from the MADT; the O6N profile maps the NIC's root port to its INTx line from the mainline device tree (`GIC_SPI 436` for the x1_0 port, INTID 468; `hopos.nicirq=` overrides, 0 polls) and the Realtek driver gains interrupt enable and acknowledge. QEMU virt, which has no security extension, proved the Group 1 path: `15 irq/s` during an HTTP burst against the agent, zero when idle, health responses throughout. The probe waits up to fifteen seconds for the first interrupt and reports the claimed count on every heartbeat.

Not established. No O6N boot has happened yet; every register value comes from source, none from measurement. Open before any acceptance claim: EL2 entry and window selection, actual MADT order and class assignment, RTL chip variant and link, PCIe DMA translation, NVMe controller, SCMI channel response and sensor naming, `_CPC` register addresses on the shipped firmware, watchdog behaviour, Secure Boot enforcement, and whether INTID 468 is the line the root port actually raises (RX falls back to polling if not). The first physical step is the `hopos-o6n-probe.img` stick, which measures each of these and prints the result on UART2 and HDMI. The Altra profile shares the changed UEFI code and needs a rebuild check on its own hardware. No commit, tag or publication.


### L66 — Spin port collision is a configuration conflict

Derek reports task ZM5XRR09JK2SP43W repeatedly failing to allocate HTTP port 8080 after deleting and recreating Spin, and authorizes M4 FLIP if needed. Inspect HOP's initial/restart port allocation: it probes a wildcard TCP listener. The mini's management API itself listens on 8080; the new Spin job explicitly requests that occupied port. No leaked old-job port reservation is established. Preserve the current job privately, delete only the failed Spin job, wait for cleanup and recreate it with only HTTP changed to 80, preserving cloudflared and the intended cage order.

The corrected request clears port allocation but fails placement: its new 2048 MiB memory limit exceeds the running kernel's 768 MiB app-window cap. Ask whether to restore the earlier 512 MiB limit or address the larger memory requirement separately. Historical console messages about missing S3 replication configuration are superseded: the current job has all four required S3 fields configured. Do not change replication settings. No FLIP or source-code change is needed for the port collision; Spin is not yet restored while the memory choice is pending.


### L67 — one partition lifecycle, large mappings on ARM and RISC-V

Derek rejects the old ARM 768 MiB limit after Spin requests 2 GiB, and explicitly requires 5 GiB and the same generic ownership model on RISC-V. The physical mini has sufficient memory; the limit came from one stage-2 L2 table, plus an obsolete app scratch alias. Current app images already keep their control/rings in their own 2 MiB ABI tail and do not consume the node boot-scratch mapping. No RAM reduction or app rebuild is needed.

The shared allocator now reserves one visible partition plus architecture-reported translation storage, if needed, in one physical claim. The same release and adoption paths account for that complete claim, including overlap checks. PartSize in the existing handoff remains the visible partition size; the architecture's deterministic reservation rule restores the complete claim. Changing that rule in a later release requires corresponding handoff compatibility review. PoolLargest accounts for the address limit and translation reserve before admission. No second allocator, worker, lock or per-board lifecycle is added.

ARM uses 39-bit stage-2 IPA, eleven existing inline L2 pages and, only when necessary, another 2 MiB of trusted tables beyond app-visible RAM. The old scratch alias is removed; framebuffer grants stay separate. RISC-V retains PMP ownership and Sv39 translation; small mappings remain in the existing ABI slack, larger maps use the same reservation mechanism. Its hardware walker can read its own table reserve under PMP, while the visible app mapping remains unchanged. Table-copy bounds and full reserve clearing prevent overflow and old-owner data exposure. App ABI-tail geometry remains unchanged. Architectural address limits remain (ARM 512 GiB minus the canonical base; RISC-V positive Sv39), with contiguous physical capacity normally the tighter bound.

The changed ARM trampolines also expose a concrete instruction-publication requirement: copying code and cleaning data cache does not invalidate other cores' instruction caches. Publish the copied switch code with IC IALLUIS, DSB and ISB through one dedicated primitive, after existing data-cache maintenance. This closes the missing publication operation; it does not retrospectively prove the cause of L63's original fault.

Validation: host mapping tests cover 5 GiB, 20 GiB and the full ARM canonical address window, boundaries, neighbor isolation, rebuild and secondary-context preservation. Generic reservation/adoption/release tests include overflow table ownership. Host/target gate passes; the isolated gate skips its relative Apple checkout, so the Apple VHE/highram bundle is built separately against the actual workspace. Race tests pass with checkptr disabled for the existing uintptr MMIO fixtures. The RISC-V target is rebuilt after the final full-reserve scrub correction. The candidate uses published HOP v1.0.6 over HEAD 516d39b, with only this memory change, excluding concurrent O6N work.

Hardware on M4 192.168.1.122: empty FLIP installs generation 4. A 5 GiB app checks one byte per 4 KiB page across 4,829,741,056 bytes of Go heap. A 20 GiB request is correctly refused before download because the largest contiguous pool region is 19,688 MiB (19,686 MiB available after table reserve). An 18 GiB app checks 18,788,384,768 bytes of Go heap, exercising external table storage. These are mapping/ownership checks, not full-byte memory or disk throughput benchmarks.

A same-artifact FLIP is correctly rejected as already running; the first harness incorrectly matched the old generation marker and that apparent success is explicitly excluded. Correct the harness to require a new marker and generation. A marker-only second candidate then performs the actual live FLIP to generation 5 with cloudflared, Spin and the 18 GiB fixture alive: all three task IDs remain unchanged, and the fixture keeps its boot ID and page patterns. Remove the fixture and verify only the original services' 2,304 MiB remains claimed. Spin runs with the requested 2 GiB as cage 2, cloudflared as cage 1, both with zero restarts. Spin's root returns HTTP 404 from the application; no full S3/application integration claim is made.

Evidence: [large-partition acceptance](release-evidence/2026-09-09-large-partitions.json). Local scripts, fixture, build logs and console traces are under /tmp/hopos-arm-memory-20260909; private job definitions are not published. No flash, reboot, commit or publication. This closes the reproduced memory-window refusal and its M4 live-adoption test, not the separate HopFS large-copy, other-board final acceptance or remaining release tasks.


### L68 — Vitals capability reporting and cross-node TCP follow-up

Derek supplies a second review's observations: one reported core despite two-core benchmarks, Cachefly returning chunked data, storage-dependent tests failing on boards without storage, missing temperature on open nodes, idle wakes and Pi timer overshoot, and a large cross-node TCP transfer apparently hanging.

The current Vitals source already reports GOMAXPROCS rather than TamaGo's fixed NumCPU value; add a two-core endpoint regression. The deployed image has not been identified, so do not claim it contains this source. Temperature now queries an open API without a key and signs requests when configured. Match the exact node endpoint; never substitute another node's hottest sensor. Disk and SQLite explicitly skip the node's no-storage response, while unexpected I/O failures remain failures. The syscall comparison still measures HTTP when its storage phase is unavailable. Page, copied report and shell runner distinguish skipped tests.

Direct HTTP requests establish that Cachefly returns a seven-byte chunked alternative without a User-Agent, but the 104,857,600-byte object with a truthful HopOS-vitals User-Agent. Use that header and reject short benchmark responses. Do not weaken leanhttp's known-length download contract or count the seven-byte response as throughput.

At the Pi's 54 MHz counter frequency, the current event-stream setting wakes every 1.21363 ms; repeated 5 ms sleeps can span five events, about 6.068 ms. This explains the reported overshoot without establishing a timer correctness fault. CtrlWakes counts scheduler idle rounds, not physical NIC interrupts. SMP CtrlIdle lacks complete physical-core sleep accounting: summing scheduler sleep and clamping it to one wall-clock interval can falsely report 100%. Vitals now reports that SMP percentage and its inferred wake cost as unavailable, with a coverage note, while retaining scheduler rounds/s. Counter resets and changes in core count start a fresh sampling window. No timer, governor, extra polling loop or lock is added for these observations.

The earlier Pi4 debug log (rxlog144.txt, 6 September scratchpad) shows continued progress from 1,129,544 to 2,513,624 bytes over approximately nine seconds, with roughly 39,000 early RX-pump wakes per second and no zero-byte reads or TX-ring waits/drops. This is severe throughput collapse and wake churn, not proof of a permanent deadlock or a hard 1 MiB allocation limit. Lean's segment-output counter excludes bare ACKs, so its low value does not prove missing acknowledgements. A paired Lean-stack host fixture transfers 8 MiB byte-exact using 256 KiB writes, 64 KiB reads and a 1 MiB connection buffer cap, at MTU 1500 and 65535, three repetitions each. These checks do not cover the physical app-ring/doorbell/scheduler combination.

The Pi4, Pi5 and Radxa endpoints are presently unreachable on management and Vitals ports; no hardware state was changed. Keep cross-node TCP acceptance open: repeat the original transfer with matching deployed artifacts, capture both endpoints' progress and ring/doorbell state, fix only the demonstrated cause, then verify larger transfers and idle recovery. Do not describe the reporting fixes as a TCP fix. No FLIP, commit or publication.

Validation for L68: the complete Vitals host suite and race suite pass, including two-core reporting, authentication, storage absence versus I/O errors, short downloads and idle reset/coverage tests. Final ARM64 and RISC-V TamaGo builds pass with a temporary modfile resolving the current local metal tree; source go.mod/go.sum remain untouched. These are review builds, not published release artifacts. Local logs: /tmp/vitals-final-tests.log, /tmp/vitals-final-arm64-build.log and /tmp/vitals-final-riscv64-build.log. Final ELF SHA256: ARM64 8921bf87dce33885a184d64971bcf49641eb4795bfba99e3ba8aeb1b21803a2a; RISC-V e19afd3fc210e79c50d1d6c17ee71452453db5fb9c38e48775e207fa106140ab. Lean v1.1.1 host-transfer fixture and race evidence are archived under /tmp/hopos-lean-transfer-review-20260909; the Lean checkout is unchanged.


### L69 — consolidate the remaining acceptance list

At Derek's request, put the cross-node TCP test explicitly on the remaining-work checklist and return a single current list while he continues O6N bring-up. Add the dated summary at the top of the canonical plan: TCP reproduction/correction; final candidate and hashes; outstanding physical lifecycle acceptance; real HopFS capacity/integrity and performance; sustained operation and corrected Vitals checks; documentation/site cleanup and artifact signoff. Preserve completed evidence rather than reopening historical unchecked items. Keep the full board-capability checklist, including network IRQ and complete SMP idle accounting, as later follow-up. No hardware operations, tests, code changes or publication in this documentation-only update.


### L70 — Pi4/Pi5 cross-node TCP corrected and accepted

Derek makes Pi4 192.168.1.144 and Pi5 192.168.1.193 available on kernel 2.2.4 and asks to execute the TCP item. Both initially run Welcome only. Deploy a separate 128 MiB, two-core Vitals test app on port 8090, preserving Welcome. Pi5 briefly disappears because its power supply is insufficient; Derek corrects the supply. This operator-confirmed power event is separate from the subsequent app-level transport failures.

Baseline Vitals transfers reproduce the problem: Pi4 can reach 100% reported app CPU with HTTP and network-backed logs unresponsive while the node and Welcome remain usable. Short or diagnostic transfers sometimes pass; ordinary 64 MiB repetitions fail. Test-only images excluding selected statistics/GC operations are diagnostic controls, not proposed fixes.

Two concrete idle-path corrections are prepared. Ring.HeadPending rejects a sampled head older than a concurrently advanced tail, using bounded unsigned distance so counter wrap remains valid. Seven host snapshot cases pass. The TamaGo runtime's IdleMayReady previously treated a blocked note waiter holding a P as scheduler idle; it now also requires an unblocked M and a running P. RunIdleTimers shares this predicate. This excludes, among others, the stopped-world coordinator from servicing doors/timers inside its semaphore wait. Only runtime/idlehook_tamago.go is changed in that checkout; earlier local runtime changes are preserved. Three consecutive 64 MiB Pi5-to-Pi4 transfers with normal GC/statistics then pass at approximately 10.9–11.4 MB/s. The reverse direction remains slow and is not signed off.

A separate Lean v1.1.1 host fixture reproduces fast-retransmission amplification: one lost segment produces ten replays of the same 60 KiB flight without cumulative ACK progress. Queued duplicate ACKs reset the counter after the send cursor is rewound; its uint8 counter can also wrap. Saturate the existing duplicate counter at three until real ACK progress, retaining timeout recovery. The fixture then performs one replay, and stale-ACK/window-update/partial-progress/RTO regressions pass along with the full Lean host suite and focused race checks. Hardware improves in some respects but still has severe loss/retransmission overhead, so this alone does not close TCP acceptance.

The existing Lean design explicitly omitted sender congestion control on the assumption that this side sends only small heartbeats/API responses. App-to-app bulk invalidates that assumption. Investigate an ACK-driven send limit for physical routes, preserving the existing trusted memory-link path. The Pi5 GEM source has 64 RX descriptors, whereas a peer receive window can permit a much larger burst; this supports the bounded-queue test scenario but is not direct hardware proof of the exact drop location. Final implementation and acceptance remain pending below.

Working evidence and test images: /tmp/hopos-pi-tcp-20260909; Lean deterministic-loss evidence: /tmp/hopos-lean-retrans-review-20260909. Kernels remain 2.2.4; no kernel FLIP, reboot, commit or publication is performed by this test work.

L70 completion: physical-route TCP now uses a small congestion window with slow start, ACK-based growth, conservative Tahoe-style reduction on fast loss/RTO and restart after idle. Existing ACKs and the existing retransmission timer drive it; no new worker, timer, lock or queue. Trusted in-memory routes retain their receive-window-only path. Recovery admits already-sent bytes within the reduced window without treating a rewound cursor as permission for a new unrestricted burst. References: [RFC 5681](https://www.rfc-editor.org/rfc/rfc5681) and [RFC 6928](https://www.rfc-editor.org/rfc/rfc6928); this is not a claim of complete TCP RFC coverage. The bounded 64-frame host fixture transfers 8 MiB with deliberate drops and about 3.6% replay overhead. Regression coverage includes duplicate-ACK latching, ACK progress, sequence wrap, peer windows, FIN, probes, RTO, idle restart and trusted-route selection. Full Lean host tests, focused race tests, ring tests, Vitals host tests and ARM64/RISC-V app builds pass. A separate code review finds no further concrete blocker in these changes.

Final hardware acceptance uses two dedicated cores and 128 MiB per Vitals app, with normal GC and statistics enabled. Six ordinary 64 MiB transfers pass: three Pi4-to-Pi5 at 21.45–24.27 MB/s and three Pi5-to-Pi4 at 11.60–12.99 MB/s. A private verification fixture additionally hashes every received byte: 128 MiB in each direction separately, then 128 MiB each way simultaneously, all match the independently computed SHA-256 9bbedc16d273e408106017b33ff8f97336613093990a756dc79a8135e24d2629. After settling, a ten-second observation records roughly 1–1.4 early RX wakes/s and 414–415 fallback pump ticks/s, no retransmission or TX-wait growth, responsive apps and no restarts. The full SMP idle-percentage limitation remains separate.

Remove both temporary jobs and verify each board again has only its original Welcome task, with zero restarts and exactly 64 MiB/one core claimed. Stop the temporary artifact server. Neither kernel was changed. Public evidence: [Pi TCP acceptance](release-evidence/2026-09-09-pi-tcp.json), including exact app hashes, relevant source hashes, transfer results, checksums and cleanup. The reported TCP correctness item is closed for these review builds. Final release packaging remains open: include the HopOS ring change, publish/pin the Lean change and retain the TamaGo runtime change, then rebuild the affected apps and final kernel candidates. Existing app binaries keep the runtime/netstack they were linked with. No commit, tag or publication.


### L71 — capture the tested TamaGo runtime in the existing patch set

Derek requests review of the TamaGo runtime and preservation of its changes in the two existing HopOS toolchain patches for later upstream submission. Compare the actual compiler checkout with its base commit and with tools/tamago-go. The complete local delta is four files: runtime idlehook_tamago.go, os_tamago.go, proc.go and net/net_tamago.go. Patch 1 lacks the L70 blocked-thread/running-P eligibility correction and the shared guard in RunIdleTimers; patch 2 already matches the working implementation.

Regenerate patch 1, explicitly including its untracked new runtime file. Keep the runtime and net changes as two separate patches, with English submission descriptions. Record base commit 07ec1e5beb4b5ac27420a8969db4334746878df5 in BASE_REVISION; its VERSION is go1.26.4. The previously documented toolchain tag is not verified by the upstream tag query, so the reproducible instructions use the actual commit. The English README states the caller contract, cooperative-preemption limitation and the scope of prior hardware evidence, including that Lean and ring changes also contributed to L70.

Correct apply.sh to preflight the complete pending patch set before applying it. The old loop could leave patch 1 applied when patch 2 conflicted, contrary to its comment. Preserve already-applied detection and accept Git-managed checkout layouts through git rev-parse. Do not reset user changes or rewrite an installed older patch automatically.

Validation on disposable repositories containing pristine base files: both patches apply and reproduce all four live toolchain files byte for byte; reapplication is a no-op; a conflicting second patch leaves the first unapplied; a checkout with patch 1 applied receives patch 2; an older patch-1 installation is rejected without changes. The real toolchain is recognized as already patched and is unchanged. Shell syntax and diff formatting pass. Exact file equality connects this packaging check to the ARM64/RISC-V builds and Pi4/Pi5 hardware evidence already recorded in L70; no redundant hardware run is claimed. [Patch-set verification](release-evidence/2026-09-09-toolchain-patches.json). No runtime implementation change, commit or upstream submission in this packaging step.


### L72 — Pi4, Pi5 and Radxa final lifecycle and sustained-use round (passed)

Current sign-off: **requested items 2 and 4 pass on Pi4, Pi5 and Radxa**. The earlier attempts below remain chronological evidence.

| Board | Lifecycle / three live FLIPs | Thirty-minute run / Vitals | Final state |
| --- | --- | --- | --- |
| Pi4 | Passed on production64–67; generations 2–4 retain three residents and TCP sockets | Passed | Original Welcome only; HTTP 200, zero restarts, 64 MiB / 1024 shares |
| Pi5 | Passed; final31–33 retain three residents and TCP sockets, followed by two UART-only FLIPs | Passed, with artifact scopes recorded below | Same original Welcome; temporary physical client removed; HTTP 200, zero restarts, 64 MiB / 1024 shares |
| Radxa | Passed; final31–33 retain three residents and TCP sockets | Passed | Original Welcome only; HTTP 200, zero restarts, 64 MiB / 1024 shares |

No M4/O6N/NVMe acceptance or published-dependency signoff is included. Pi5's full duration test ran on the earlier corrected image; later boot-path and seeded-port changes received the recorded live-FLIP, download and Vitals checks. Pi4/Radxa completed their full runs after their final live sequences. Exact source/artifact scopes, client-path failures and cleanup are in the [evidence](release-evidence/2026-09-09-board-acceptance.json).

Derek authorizes the remaining lifecycle and thirty-minute combined-use checks on Pi4 192.168.1.144, Pi5 192.168.1.193 and Radxa 192.168.1.241. M4 and O6N remain untouched for the later NVMe round, which will include their lifecycle and duration checks. Save the three original Welcome job definitions privately and record baseline tasks, capacity, identity and console. Build an isolated snapshot containing the current HopOS changes, the L70 Lean changes and the exact L71 patched TamaGo runtime. Record source and artifact hashes under /tmp/hopos-board-accept-20260909; these are review candidates, not a published release.

The changed ARM switch/stage-2 contract requires removing the old residents for the initial compatibility transition. That empty FLIP does not count among the three subsequent live FLIPs. Test twenty start/compute/stop/reuse cycles, sampled own-partition memory clearing, shared-neighbor survival, dedicated two-core execution/release, quiet wake, three consecutive actual kernel replacements retaining app sockets and task identity, and thirty minutes of compute plus network I/O with lifecycle actions. Check current Vitals capabilities afterward. No NVMe test is claimed on these boards. Record results and restoration below before signing off.

L72 first-candidate findings: Pi4 completes twenty checked compute/start/stop/reuse cycles and two sampled 3 MiB zero/paint/reuse checks. The mixed-layout harness then correctly rejects cage IDs 4, 5, 1. Source inspection finds that the published HOP v1.0.6 dependency still contains the old shared-cage offset and multi-cage SMP reservation; the L59 correction remains uncommitted in the sibling HOP checkout. The first L72 snapshot accidentally omitted that local dependency. Preserve its evidence as partial diagnostics and rebuild with an isolated copy of the intended HOP changes; publication/pinning remains open. Do not silently relax the cage-allocation assertion.

Pi5 reaches the new kernel generation but the boot-config Welcome reseed fails PSCI CPU_ON with ALREADY_ON (-4). Its partition is correctly quarantined; an empty agent task list does not prove physical release. Stop the test-job retries. Source inspection identifies an empty-FLIP initialization bug: the existing adoption flag only covers live residents, so scheduler initialization clears the mailbox of a physically parked core. Clearing its parked sentinel can both lose the already-on state and let the old park loop observe an invalid dispatch. Prepare a separate parked-core preservation condition without weakening live switch compatibility. Radxa's first FLIP is unconfirmed and the node subsequently returns through a cold boot; no zero-reboot success is claimed and the reset cause is not inferred solely from network observations. Pause the dependent acceptance and duration phases until these findings are addressed.

L72 correction: a valid ARM handoff now records that parked cores may exist independently of whether live app contexts are adopted. Empty adoption validates each app-core mailbox as cold or parked before rebuilding scheduler state, leaves its first word untouched throughout, and preserves the executing park-loop instructions. The remaining scheduler fields and new switch/trampoline code are initialized normally. Live-context adoption keeps the existing switch-code compatibility requirement; RISC-V behavior is unchanged. Three host regressions cover mixed cold/parked state, real cold reset, unconfirmed active state rejection before writes, and park-loop preservation while exception thunks are rebuilt. Stage2/kernflip tests and an independent source review pass. No extra worker, lock or mailbox field is introduced. Rebuild all three boards using the existing local HOP correction and this fix; retain the rejected first-candidate artifacts separately. Pi5 needs one operator powercycle to recover the state already lost by the first attempt; this recovery is not a zero-reboot acceptance result.

L72 hardware closeout for this attempt: the corrected Pi4 live transition loses all three pre-existing app sockets and never confirms the new marker/generation. The board later returns through its cold disk boot with a new Welcome task; this is a failed continuity test, not successful watchdog/zero-reboot acceptance. The corrected Radxa empty transition likewise remains unconfirmed and later returns cold with Welcome. Both Radxa attempts completed verified download and window preparation. The last buffered TCP-console line is "placing segments"; it cannot locate the failure relative to the actual jump because later log bytes may not be transmitted. No additional blind retry or reset is performed. Pi5 remains without jobs after stopping retries, pending the requested operator powercycle; its zero agent claims must not be mistaken for a recovered quarantined physical core.

The corrected source passes a separate actual-agent QEMU test with two shared apps on core 1 and one SMP app on cores 2–3. Checked compute, timer wake, live FLIP retaining task/boot identity and the existing SMP TCP socket, adopted stop, neighbor preservation, first-free cage reuse and final zero claims all pass. QEMU and its fixture server are stopped. This does not establish physical cache/firmware correctness or validate the empty-FLIP fix on these boards. Local evidence: /tmp/hopos-liveflip-qemu-20260909. Full HOP tests and a RISC-V kernel compile also pass on the isolated candidate.

Ask which board owns /dev/cu.usbmodem21302 before opening it, because Derek is separately working on O6N. Further physical FLIP localization requires UART across the jump/earliest bootstrap or a readable retained stage breadcrumb. The thirty-minute H6 run and final Vitals checks have not started; no board is signed off for the requested complete H0–H4/H6 sequence. [Board acceptance evidence](release-evidence/2026-09-09-board-acceptance.json) records source/artifact hashes, successful partial tests, failures and observed board state. M4 and O6N remain untouched. No commit, release, flash or manual reboot.

Cleanup: stop the temporary artifact server on port 18140. Pi4 and Radxa have their boot-config Welcome service again after cold recovery; Pi5 has no test-job retry loop. The prepared harnesses and private original job definitions remain available for resumption after hardware recovery/console identification.

L72 resumed at Derek's explicit request to complete items 2 and 4 successfully. He reports that powercycling Pi5 also reset Pi4. Confirm fresh Welcome tasks on both boards at 13:02:02 and 13:02:05 UTC. Pi4's diagnostic index-10 observation overlapped this operator reset and is excluded as an OS failure; earlier failures at approximately 10:17–10:21 UTC remain separate observations. Pi5's prior quarantined-core state is recovered by the operator reset. Resume the corrected empty transitions and then the full acceptance sequences. A passive read of the unclaimed USB modem at its existing settings returned firmware prompt text but did not establish board identity; do not change its settings or use it for these tests while O6N ownership is unknown.

L72 resumed hardware progress: after the operator reset, Pi5's full corrected candidate lands through an empty FLIP and its configured Welcome starts without CPU_ON/quarantine errors. Twenty start/stop cycles, two physical-claim reuse canary cycles, correct cages 1/2/3 for two shared apps plus a two-core resident, concurrent quiet wake, neighbor survival and complete SMP release pass. A fresh sequence performs three actual live FLIPs with the same three task IDs, boot IDs and TCP socket objects. One explicitly reported pre-download dial timeout is retried once while those sockets remain open; it is not a failed jump. Start H6 only after three confirmed jumps and fresh zero-restart fixture setup.

Pi4 diagnostic controls distinguish placement from bootstrap: an independently linked-at-destination candidate lands, while normal-address attempts remain unconfirmed. Exact-source three-link-variant checking and host replay of all placed/relocated bytes match the independently linked destination image byte for byte. Once the diagnostic kernel is running, flushed outgoing logs confirm all segments, relocations, state capture and HOPOS_FLIP_JUMP complete before the failed landing. Therefore earlier "placing segments" console tails did not locate the failure. These controls are diagnostics, not passing H4 evidence.

Radxa direct empty-transition controls, including zero relocation delta and paced download, do not establish a successful landing. An isolated diagnostic bridge retains the old 32-bit switch immediates and the existing live Welcome, so the old switch compatibility check remains valid; that live transition lands. No new apps are started on the bridge. Remove Welcome and use its instrumented outgoing path to install the full 39-bit corrected candidate; this empty FLIP lands as generation 2 and Welcome starts normally. Begin the full Radxa lifecycle and duration acceptance on this ordinary corrected candidate; the bridge is excluded from the release and from the three-live-FLIP count. No flash or manual reset is used for this bridge sequence.

L72 additional diagnosis: exact-source host relocation replay and a SHA-256 readback of every placed byte on Pi4 match, including relocation and RAM-symbol patches. The outgoing kernel confirms HOPOS_FLIP_JUMP, so the remaining Pi4 failure is bounded to CPU handover/early bootstrap; do not attribute it to ELF byte-copy corruption. Pi5 H6 is running with checked compute, bidirectional network payloads, timer wake and minute-spaced shared-app replacement; Radxa's full acceptance is progressing on the ordinary corrected build.

A separate Lean host regression demonstrates a cross-boot TCP tuple collision: every new stack previously began ephemeral allocation at port 49152, while the peer could retain TIME_WAIT and reject a lower new sequence number. Apple XNU's documented implementation ignores resets in TIME_WAIT and permits reopening without timestamps only with an advancing sequence number. Seed the initial ephemeral offset from the existing ISS seed; preserve the existing port range, wrap and occupied-port checks. Full Lean and focused race tests pass. This removes deterministic restart-port reuse without a new RNG or worker; an offset collision remains possible, and the particular Pi5 pre-download timeouts were not captured on the wire. The running acceptance snapshot is unchanged by this separate source correction; retain that distinction when identifying final release artifacts. Evidence: /tmp/hopos-lean-flip-port-review-20260909; source: https://github.com/apple-oss-distributions/xnu/blob/main/bsd/netinet/tcp_input.c.


L72 Pi4 root cause and final candidate: the HVC #2 kernel handover omitted local instruction-cache invalidation before branching into the placed image. All placed/relocated bytes matched host replay. A controlled comparison changes only the outgoing handover: adding the same three instruction-cache hygiene instructions already used by the other ARM entry paths makes the exact previously failing destination image land, retaining Welcome. Integrate IC IALLU, DSB SY and ISB immediately before BR in the generated handler. Existing branch destinations and register arguments are unchanged; the handler remains within its vector entry. The installed-handler regression, stage2/kernflip host tests and independent review pass. No new state, lock or worker is added.

Build the final review set with both the HVC correction and seeded Lean ephemeral allocation: four bundles per Pi4/Pi5/Radxa (indices 30–33), rebuilt observation/canary/Vitals apps, plus a RISC-V kernel compile check. Preserve source SHA manifests and bundle hashes. Pi4 runs its full remaining sequence with these final artifacts. Pi5 and Radxa finish their already running thirty-minute steady-operation tests on the earlier corrected candidate, then repeat actual live replacements and Vitals on the final artifacts; the evidence distinguishes these scopes. No shortened or interrupted run counts as thirty-minute acceptance.


L72 final30 counterexample: the Pi4 final artifact does not promptly confirm landing despite the outgoing hygiene correction and completed placement/rebase/jump logs. The successful same-image comparison establishes a real missing handover step, but does not establish that every final target now works. Pi4 acceptance remains blocked pending this additional discrepancy; do not generalize the earlier diagnostic success into a full fix. Pi5/Radxa duration tests continue independently.


L72 Pi4 further controls qualify the cache comparison: final30 and an otherwise equivalent production target retaining old Lean both remain unconfirmed. The outgoing handler words are read from their installed physical address and match IC IALLU / DSB SY / ISB / BR. Later, even the exact previously successful diagnostic image fails again. Thus the single paired success is insufficient to assign the observed landing failures solely to cache hygiene or linked layout. Keep the independently identified hygiene omission separate from the still-open physical cause. A packet capture records 192 byte-identical captured copies of each old console TCP packet around one failed transition, suggesting DMA descriptor replay. Investigate GENET stop/reset ordering before further layout variants; this is a concrete wire observation, not yet a proven driver diagnosis.


L72 Pi5 acceptance: H6 completes 1800.094 seconds with 2015 checked rounds, 528,220,160 bytes verified in each direction over the retained TCP connection and 28 shared-app replacements. No unexpected task restart, corruption, hang or claim growth; cleanup returns zero tasks, CPU shares and RAM claims. Install final30 as generation 7, then final31/32/33 as generations 8/9/10 with the same three resident task IDs, boot IDs and held TCP socket objects. All post-jump compute checks pass, followed by a newly downloaded shared replacement while its two neighbors survive. No final-series download retry is needed. The largest individual echo delay during consecutive FLIPs is 20.07 seconds; socket continuity is not a zero-latency claim. A harness initially checked the reseeded Welcome during its download; it subsequently reached running with zero restarts, and the harness continued without an extra kernel replacement.

The rebuilt final Vitals image passes on Pi5: two assigned cores reported, temperature read from the open node, explicit no-storage skips for disk/SQLite, successful HTTP syscall measurement, complete default 32 MiB download, SMP and timer tests. The Vitals task retains identity with zero restarts and releases all claims. Pi5's requested lifecycle/sustained-use/Vitals scope is passed on the precisely recorded review artifacts. Published dependency pinning and the separate M4/O6N storage round remain open.


L72 GENET correction candidate: stop MAC traffic and disable all DMA engines before MAC reset/PHY setup. Wait up to 5 ms for both hardware DMA-disabled status bits; propagate a timeout instead of rewriting active rings. Preserve the hardware-owned RX producer/TX consumer counters and align the corresponding software counters to their actual retained 16-bit values, matching U-Boot's GENETv5 initialization and existing modulo-256 descriptor selection. The old code assumed writes had reset those hardware counters to zero. Host tests cover cold/nonzero/wrapped retained values and refusal to reset while either engine remains active. Build Pi4 review artifacts 40–43 from the final snapshot with this driver/caller change; Pi5/Radxa and fixture binaries are unaffected. Hardware acceptance is pending. References: [U-Boot GENETv5](https://github.com/u-boot/u-boot/blob/master/drivers/net/bcmgenet.c), [Linux GENET](https://github.com/torvalds/linux/blob/master/drivers/net/ethernet/broadcom/genet/bcmgenet.c).


L72 Radxa H6 interruption: after the last saved 1521.65-second/1600-round checkpoint, one concurrent /compute request raises ConnectionResetError 54. The traceback does not identify which of the three ports; the original harness omitted endpoint context at this call site. All three tasks and boot IDs remain unchanged with zero restarts, the node is healthy and their /status endpoints respond. Preserve the failed attempt and start a bounded, captured reproduction with endpoint-level error evidence; do not accept this as a thirty-minute run or silently retry it.

L72 Pi4 index40 remains unconfirmed. A follow-up source review finds that the first GENET quiescence change combined MAC-first disabling with DMA completion polling. Linux's drain contract disables RX first and keeps TX enabled until DMA stops. Correct that ordering and its timeout test before another candidate; the failed 40 artifact remains immutable.


L72 Pi4 further qualification: corrected DMA-stop ordering candidate44 also remains unconfirmed, so driver source correctness alone has not located the failure. The shared Pi early-boot uputc helper can wait forever for UART TXFF to clear, unlike the already bounded earlier banner and runtime output. Bound that diagnostic-only loop using the same 100000-iteration convention and a verified unused scratch register; both Pi assembly builds pass. Candidate48 adds this fix without changing switch compatibility. UART evidence is requested separately because the available modem's O6N ownership must be respected. Each unconfirmed candidate remains an unconfirmed landing; no watchdog recovery substitutes for FLIP acceptance.


L72 Pi5 shared-UART follow-up: because the bounded early-output helper is shared by both Pi boards, build two further Pi5 artifacts (52/53) with that exact source change. Both actual live FLIPs land as generations 11/12 in approximately 8 seconds, preserving the same Welcome task and zero restarts. HTTP returns 200 after each and the final claims remain one core/64 MiB. This specifically validates the changed boot path; the earlier mixed lifecycle and thirty-minute steady-operation evidence remains identified by its own artifact hashes.


L72 Radxa second duration interruption is captured: after about 213 seconds a new SMP /nap connection fails. The Mac sends a bare reset before its valid handshake ACK and HTTP GET; Lean then correctly closes the embryonic connection and rejects the following packets. The reset's TOS 0x10, DF flag and sequence construction match Apple's PF handshake-state-mismatch path, unlike the surrounding native client packets. This is strong evidence of client-side packet-filter interference; the responsible rule/extension is not established. No firewall changes and no weakening of valid TCP-reset handling are made. Preserve both failed attempts. Run the unchanged strict acceptance harness from an isolated Linux Docker client, record its image/client provenance and retain the Docker Desktop host-network caveat. Source analysis: /tmp/hopos-radxa-h6-rst-review-20260909/assessment.md.

L72 Pi4 live 32-bit compatibility bridge also fails to confirm landing, so the initial empty transition alone does not explain the failures. Complete the GENET warm-teardown protocol against Linux v6.12: disable MAC receive; clear only global TX DMA enable and await its bounded acknowledgement while ring enables remain; allow 10 ms for queued traffic to drain; stop and acknowledge RX; only then clear ring enables and MAC transmit before reset. The earlier combined all-ring disabling was not an exact implementation of that drain protocol. Updated regressions verify timeout ordering and untouched active queues; build a fresh immutable candidate rather than overwriting the failed artifacts.


L72 client-path controls: Docker Desktop's test reaches the apps but later times out before emitting a target SYN; its packets retain the host TCP fingerprint, so it is not an independent physical client. A temporary one-core/64-MiB proxy test app on Pi5 then opens fresh physical Pi5→Radxa control connections, while the original held Mac→Radxa Echo socket remains unchanged. The first run through that proxy times out waiting for a response; all Radxa app identities still match and subsequent status replies are prompt. Preserve these attempts and add request-phase diagnostics before another duration run. Do not silently ignore resets, lengthen a passing timer or equate a healthy follow-up request with completed H6. Pi5's original Welcome remains running alongside the temporary test client, which must be removed at final cleanup.

L72 Pi4 execution boundary established without UART: diagnostic60 emits a received UDP packet from the start of Go's ProbeNIC/GENET Reset using the inherited active NIC. Thus this particular unconfirmed landing has already completed relocation, early bootstrap and runtime startup. Earlier absence of a new TCP console cannot establish failure before Go. No stop-error packet follows; distinguish a sleep/wake failure from a hardware-stop failure using a bounded busy-read transmit completion followed by one explicit sleep and a second diagnostic packet, before touching MAC/DMA state. These packets are isolated test instrumentation, not production networking or recovery paths.


L72 physical-client timeout resolved as fixture capacity: proxy tracing records request513 waiting in outbound dial, and Pi5 logs HOPOS_MASQ_SLOT_FULL for its 512-flow per-app quota. The deliberately fresh outbound HTTP connections consume the configured close-retention budget faster than expiry; this is not evidence of a NAT leak or a Radxa failure. Keep the production quota unchanged. Replace only the temporary proxy with normal pooled HTTP connections (MaxIdleConnsPerHost4), retaining request-phase tracing, deadlines, target allowlist, strict payload/task/boot checks and the direct held Echo socket. The new artifact is ffe230e896ed2d88317dc6d26619baa54a5c6f5f2274a5cf8529948364c7c2e9; earlier proxy attempts remain archived. A fresh full-duration attempt starts only after the pooled-client probe.

L72 Pi4 diagnostic61 emits both entry and sleep-ok about11.3 ms apart before MAC/DMA changes. Ordinary runtime sleep works at this point. To locate the subsequent network bring-up failure, diagnostic62 records a magic-tagged nonce and stage/register snapshot in the verified unused tail of the board-reserved8-MiB DMA region. A later diagnostic entry reads and sends the previous record over the still-working inherited NIC. Interpret it only after its nonce proves persistence across watchdog/cold recovery; absence of a valid record is inconclusive. The production driver contains no recorder, diagnostic packet or retry fallback.


L72 Pi4 hardware root cause: diagnostic63 receives the previous nonce62 record after watchdog/cold recovery, validating persistence. Its last stage is61: DHCP error, after successful reset, PHY scan, negotiation and ring initialization. TX's hardware pointer0x12 represents descriptor6, while software counter0x110 selects descriptor16: the difference10 exactly equals the initial retained counter0x10a modulo256. RX's hardware pointer0x20d represents descriptor175, while counter0x42d8 selects descriptor216: the difference41 exactly equals initial counter0x3f29 modulo256. The hardware pointers were reset to descriptor0 independently of the retained counters; software consequently reads/writes the wrong descriptors. Correct the ring initialization by seeding its pointers to (retained counter modulo256) ×3 descriptor words, matching the existing software index. No additional cursor, worker or lock is needed. Keep the controlled hardware record and verify the corrected ordinary candidate through the full acceptance sequence before signing off.


L72 Pi4 candidate64 lands as generation1 in9.47 seconds and starts Welcome with zero restarts and no quarantine. The complete H1–H3 sequence then passes on final fixtures: twenty checked start/stop/reuse cycles, two sampled memory-clearing canaries, first-free cages1/2/3 for two trusted shared apps plus a dedicated two-core resident, concurrent quiet timer wake, neighbor-preserving replacement and exact memory/core release. Three new mixed residents are handed to the actual live-FLIP sequence65/66/67. This validates the pointer correction beyond simple network reachability; H4/H6/Vitals remain pending at this checkpoint.


L72 Pi4 H4 passes: three actual production replacements65/66/67 land as generations2/3/4, preserving the same three task IDs, boot identities and held TCP socket objects. Checked compute passes after every replacement. The largest individual Echo exchange spans28.66 seconds across the consecutive replacements; this is connection continuity, not a claim of zero service pause. H0–H4 is now passed for all three boards in the requested scope. H6 and Vitals remain to be completed on Pi4/Radxa; Pi5 has already passed those checks.


L72 Radxa final acceptance: the pooled physical-client H6 run completes 1800.363 seconds, 1847 checked rounds, 484,179,968 verified bytes in each direction and 27 shared-app replacements. No payload/checksum error, unexpected restart or claim growth; cleanup returns zero tasks, CPU shares and RAM claims. Final Vitals passes two assigned cores, explicit no-storage skips for disk/SQLite, HTTP syscall, the complete default 32 MiB download, SMP and timer. Temperature remains unavailable on this board, as documented. Remove Vitals and restore the original Welcome: HTTP 200, one running task, zero restarts, 64 MiB and 1024 shares. Earlier incomplete client-path attempts remain separate evidence; they are not counted as completed duration runs.


L72 final closure: complete Pi4 H6 and Vitals, restore Welcome on all three boards, remove the temporary Pi5 physical client and stop owned test servers/captures. The final simplicity review finds no required implementation change: GENET reuses existing counters and a bounded stop helper; empty-FLIP preservation and bounded diagnostic output add no worker or lock. Clarify one adoption comment without changing executed code. Requested items 2 and 4 are closed for these three boards; release dependency pinning and the later M4/O6N storage round remain open.

| Board | Full H6 seconds | Checked rounds | Verified bytes per direction | Shared-app replacements |
| --- | ---: | ---: | ---: | ---: |
| Pi4 | 1800.684 | 2627 | 688,652,288 | 29 |
| Pi5 | 1800.094 | 2015 | 528,220,160 | 28 |
| Radxa | 1800.363 | 1847 | 484,179,968 | 27 |

These are combined compute/network, sleep/wake and replacement tests, not disk-throughput benchmarks. All accepted runs preserve checked content and stable resident identities without unexpected restarts or growing claims, then return zero test claims. Vitals verifies two assigned cores, open-node temperature on the Pi boards, explicit no-storage skips, HTTP syscall, the complete default 32 MiB download, SMP and timer. Radxa has no temperature reading in this board path. Earlier interrupted runs are not included in the totals.


### L73 — canonical HOP checkout corrected

Derek identifies `/Users/derek/Git/easy/hop` as the canonical HOP checkout. Transfer the eight staged review files from the mistakenly created `/Users/derek/Git/hop` using a three-way patch. The canonical checkout is four commits newer; retain its newer log-broadcaster handling and other changes. Runner and agent tests pass, and the HopOS API package compiles. Verify that the old checkout has no untracked/ignored files or unique ancestor commits to retain, then remove it. All eight review files remain staged in the canonical checkout; no commit or publication. Earlier L55/L56 paths identify the historical review location, not the location to use for future work. Do not recreate `Git/hop`; use `Git/easy/hop`.


### L74 — O6N boot and operation confirmed

On 17 September 2026, Derek confirms that the O6N boots and is fully operational. Close board bring-up and supersede the earlier L66 statement that no O6N hardware boot had occurred. Update the current support matrix, board reference, status, remaining-work plan and gethop.org hardware list. This records operator acceptance; no new hardware test is performed in this documentation update. Exact image identification, repeated lifecycle/live-FLIP evidence, NVMe integrity/performance and full-duration/Vitals measurements remain the separately planned release checks. Do not infer specific IRQ, DVFS, sensor, display or watchdog measurements from this general operational confirmation.

### L75 — O6N root cause, first-day fixes and the UEFI flip

The O6N silent core death was a dangling `TTBR1_EL1`: the boot left the firmware's value with `TCR.EPD1 = 0`, so speculative walks through the upper address range could stall the core on a non-responding bus address without any exception (the Cortex-A720 faster than the A520, EL2 immune because its regime has its own TTBR1). A/B on the board: the control image stops, the image with `TTBR1_EL1` pointing at a zeroed table runs. The fix is generic in `cpu/el2/boot.h` (`TTBR1_EMPTY_OFF`, every board boot and every app core). The O6N kernel runs VHE (`HCR_BASE` in `cpu/el2/sysreg.h`, `VHE=1` default for `BOARD=o6n` in `image/uefi-run.sh` and `image/flip-bundle.sh`), which is the combination proven on the board; nVHE with the fix is untested there.

First operating boot on 17 September (node `hopos-1`, 11 app cores, 32 GB): NVMe WD SN770 500 GB claimed whole, SCMI thermometer (four CPU sensors, 39 °C), SBSA watchdog armed with proven liveness, DHCP. Fixed the same day: the `_CPC` Generic Register address was read one byte early (`fw/acpi/cpc.go`), which turned `0x0659009c` into `0x659009c03` and produced the external abort in `StartClock`; `gicv3.FindRedistributor` stepped three frames per GICv4 redistributor instead of four, so the NIC line stayed polled; core classes are now clustered by `_CPC` highest performance with a 25 % gap (the four A720 pairs differ by binning and must all be `big`); `hopos.nvmewipe=1` zeroes both GPT copies so the firmware stops booting a leftover OS from the NVMe. The kernel flip now works on UEFI machines (see the kernel flip page); verified on QEMU with adoption, not yet on the O6N.

Open: the node stopped answering after its first application placement (Vitals via the agent API, 2 shares, 128 MB) and did not return; the next stick boot carries the previous console in the black box. The O6N flip, NIC interrupt path, corrected clock knob and class reporting are built into `hopos-o6n.img` and `hopos-o6n.flip` and await that boot. The observer diagnosis (a second core drawing the clock, PSCI state, PC samples and trap registers on the framebuffer) did its job and was removed from the tree on 18 September, together with the boot-diagnosis overlays, markers and traces; it lives on in this logbook and the O6N diagnosis notes.

### L76 — O6N flips on hardware, NIC interrupt verdict, first application

The kernel flip works on the O6N: the stick kernel flipped to a bundle served from the workstation, restored its firmware facts, adopted generation 1 with agent state and re-registered. Two flip crashes on the way had a build cause (the firmware-facts block moved when the config cap grew; the layout is now fixed and documented). The borrowed window is now the full 160 MB footprint and the previous kernel's carve stays reserved; a second consecutive flip on a moving-window board is refused with a message instead of a panic (root cause recorded in the code: the adopted kernel has no app-core region of its own yet). The NVMe wipe knob (`hopos.nvmewipe`) also has a build-time form for flip kernels; the NVMe no longer carries a bootable Linux, so a watchdog reset returns to HopOS on the stick in about 35 seconds.

Verdicts: the RTL8125 INTx line through the GICv3 (INTID 468) freezes the node at its first wait, twice; receive stays polled until that path is investigated. The `_CPC` clock knob is off by default pending its own test. The first application placement (Vitals, two shares) failed twice on slot 1 with an EL2 undefined-instruction fault at the end of the switch blob (`el2entry+0x52c`, the padding after the last RET) and then ran on slot 3 with its SMP core; the suite is running at the time of writing. Open: that fault, the restart loop that re-places a failed slot while another slot holds the published port, and the 4 KiB config cap that applies until the stick is reflashed.



### L77 — O6N physical acceptance with NVMe

Derek authorizes the planned O6N test round at 192.168.1.205 on 17 September 2026. Initial health is good; the node reports eleven app cores (seven big, four small) and 33,954,988,032 available partition bytes. Console identifies a WD_BLACK SN770 500GB NVMe, receive polling (`hopos.nicirq=0`), firmware-managed clocks and the SBSA watchdog. Existing Vitals has one unresponsive resident and a second task repeatedly failing to publish the same port8090; archive its definition, tasks, capacity and console before deleting that test job. Both tasks and all logical CPU/RAM claims are then released. This pre-existing state is not accepted test evidence. Reuse the prior strict lifecycle/sharing/wake/live-FLIP harness, add full source-plus-backup content checks beyond16GiB aggregate and actual disk I/O to the full thirty-minute workload. Storage timing covers completed writes and readback; the existing app/NVMe API has no flush operation, so no power-loss durability claim is made. M4 and other boards are outside this round. No commit or publication.


L77 first controlled start: ioobserve (one core,96MiB; SHA2562b36fcaa4de1a2564b4ae4b7162d5a0baa3646e5aa0c5665e2c36c5157997a39) receives an HTTP200 artifact response at18:24:48UTC; this proves the transfer started, not that it completed. Before the first status/compute/storage result, management8080, console5555 and fixture18090 become unreachable; ping also times out. No FLIP or storage workload has been requested. No automatic recovery is observed after more than two minutes; request operator recovery and screen observation. Preserve the initial console and failure checks. The two earlier Vitals SMP faults have ESR0x02000000 (EC0, unknown instruction), not a proven stage-2 abort; their switch+0x52c address exactly matches an Apple-only IPI register write in the current VHE O6N build. Separate Apple-IPI selection from VHE/E2H; retain normal non-Apple waiting. This is a concrete explanation for those SMP faults, not yet proof of the subsequent whole-node outage.


L77 receive-path qualification: the packet capture still shows fresh outbound SYNs from O6N to the artifact server every few seconds, plus timed retransmissions. The host responds with SYN-ACKs, but no corresponding ACK or management reply returns from O6N. Kernel timers and transmission are therefore still active; this is an RX/receive-processing loss of reachability, not evidence that all CPUs stopped. Keep this investigation separate from the positively located Apple-IPI unknown-instruction fault.


L77 source correction ready: gate the five Apple fast-IPI switch paths with APPLE_IPI rather than VHE. All Apple image/build entrypoints supply both defines, and Apple startup rejects omission of its IPI define. Generic VHE keeps E2H/_EL12 and uses WFE. Three-mode assembly regression plus complete Apple/O6N target builds pass; linked O6N code contains none of the proprietary Apple IPI instructions, while Apple retains them. Corrected O6N bundles4–7 and a standalone BOOTAA64 EFI are built and archived locally with hashes; no corrected kernel has been deployed. The switch hash changes, so the first live transition must have no apps. Firmware-fact compatibility also needs running-image provenance: an intermediate wipe.flip used a different memory-map offset, while retained wipe.img and current builds use the original compatible offset.

L77 pause for physical recovery: management and console remain unreachable. No FLIP, NVMe benchmark, large-copy or full-duration test has run; none is signed off. Root stops its artifact server and capture, preserves the original job definition and failure evidence, and prepares the corrected EFI for recovery while retaining the existing boot-medium configuration. The submitted baseline job cannot be removed and original Vitals cannot be restored until access returns. An RX-status acknowledgement discrepancy against the reference polled driver is retained as an unproven lead; no speculative NIC change is made. Resume with node recovery, confirmed boot-image/config identity, clean job state and the corrected kernel before repeating the planned tests.


L77 operator recovery: Derek powers the O6N back on and reiterates platform separation. Node returns healthy, with no jobs or claims. A paced2MiB/s artifact server limits burst load while testing the independent VHE correction. The empty FLIP to corrected bundle4 positively lands as generation1 in14.58s, retaining node identity and firmware NIC/clock settings. Begin full lifecycle acceptance on this corrected build. The inherited old cold configuration contains a truncated display-init JSON entry; the expanded loader cannot reconstruct already-truncated bytes. This round remains headless compute/storage, with no new GUI acceptance claim.


L77 recovered-run qualification: the first 48MiB lifecycle fixture loses its artifact connection before a completed transfer or application status; the watchdog returns the node to its cold kernel. One captured repetition with a96MiB fixture reproduces the loss after an empty generation1 landing (9.95s). The persistent console ends after dispatch, before a partition/start result. Management subsequently returns with no jobs or tasks. Neither run passes H1, and neither starts the NVMe acceptance workload.

L77 transition geometry: both captured landings expose Go RAM0x97e00000..0x9ffc0000 (129.75MiB), while this incoming UEFI layout starts its carve at Base+128MiB=0x9fe00000. Runtime RAM therefore overlaps the carve by1.75MiB; the full160MiB footprint ends at0xa1e00000, beyond the old outgoing reservation ending around0xa0000000. The incoming BoardFootprint hook cannot repair a reservation already chosen by the old outgoing kernel. This invalidates acceptance on that transition; a successful generation marker alone is insufficient. First-app/NIC overlap is not established, and this does not explain the earlier cold-kernel outage. Do not apply a speculative NIC fix.

L77 next gate: boot the prepared corrected EFI once, preserving the existing hopos.cfg. Its frozen O6N source installs uefi.Footprint, reserves160MiB and exposes128MiB to Go; artifact SHA256 db3b93933d47c6e8d9992c6eb3b1e8d9b65051d1ef9a6721fd347299a3baa4dc. Verify actual geometry before continuing application tests. The current moving-window guard also refuses a second consecutive UEFI FLIP; repeated live FLIP remains an explicit open requirement, not passed hardware coverage. No storage content/performance or full-duration test has completed. The saved original Vitals definition is retained, but not restarted while this failure remains unresolved. Stop owned test transport/capture while awaiting the boot medium.


L77 boot-medium update, 17 September20:05UTC: Derek connects the O6N USB stick (HOPOS, disk5s1). Back up its original EFI and HOPOS.CFG privately, then replace only EFI/BOOT/BOOTAA64.EFI with the prepared corrected artifact (SHA256 db3b93933d47c6e8d9992c6eb3b1e8d9b65051d1ef9a6721fd347299a3baa4dc). Flush, unmount, remount read-only and verify the new EFI hash and byte-identical configuration; eject successfully. Original EFI SHA256 b66a5df39269a60a643000c50bd1c3e1166a50c2c882902699fb82cfa81d67d2. Physical return and cold boot remain pending; no new application, FLIP or NVMe acceptance is implied.


L77 operator observation after corrected EFI cold boot: GUI starts, but Derek reports that USB was not recognized. Record an open O6N USB detection/input investigation; the specific peripheral and failing layer are not yet established. This is operator evidence, not a completed USB test or a change to the compute/NVMe acceptance scope.


L77 USB clarification: Derek confirms USB support code already exists, but neither mouse nor keyboard works on O6N. Investigate that implemented path through controller initialization, enumeration and HID delivery; do not classify this as an absent feature.


L77 corrected cold boot verified at20:06:52UTC: runtime128MiB, full configuration parsed, display and launcher start with zero restarts. Archive current boot job definitions separately from the earlier Vitals backup. H0 removes these jobs and releases claims. First48MiB ioobserve download at20:07:56UTC again loses RX reachability before full transfer or any compute result, without any FLIP. Passive115200 UART capture on usbmodem21302 records ongoing task-restart/download timeout messages; packet observation records outgoing SYNs with unanswered host SYNACKs and ARP requests. This confirms continued kernel execution, not a whole-kernel halt, and leaves an independent receive-path failure after the footprint correction. H1–H6 remain unpassed. Request physical recovery; prepare the same current rolling launcher artifact over local HTTP as a transport control, SHA2566ce0ec22448c273df9e2ceebd00979b2caeaaac38d18cdbac88086a42fd2118e (original running artifact content hash unavailable). No speculative NIC change.

L77 USB source inspection: shared xHCI/HID and usbin exist; usb_rpi4.go, usb_rpi5.go and usb_rk3566.go register hosts. No O6N/UEFI registration exists in this source tree. startUSBInput is consequently a no-op there. Add the platform-specific controller discovery/registration using existing shared drivers, then verify mouse and keyboard on hardware. This is missing platform wiring, not missing shared USB code.


L77 continuation18September05:42UTC: O6N is reachable after operator restart; display and launcher run with zero restarts. Save current job definitions. Replace only launcher with current rolling launcher bytes served locally at2MiB/s: complete3,279,685byte download and cage2 start pass. Replace with ioobserve32MiB in the same desktop sharegroup: complete6,179,761byte download, status and independently checked compute pass at PA0x7fc000000. Replace with the same fixture48MiB, same group/cage: RX reachability fails, UART reports stalled after2,011,028of6,179,761bytes after60s; allocationPA0x7fb000000. Kernel retry messages continue. This narrows the reproduction but does not establish memory size itself as cause: physical placement and total scrub duration also change. Prepare bounded RX descriptor/status diagnostics; no speculative production NIC edit. Full H1–H6 remains open, and physical recovery is requested. These are actual completed controls, not overnight/background tests.


L77 diagnostic candidate prepared18September: isolated RX observation build O6N-RXDIAG-20260918-8 compiles and produces6,676,168byte flip bundle, SHA256 deb5965be077bbac362764e3dd884250ca414e2f65f7f8118687f45ab9a75c7f. It reads descriptor ownership, RX head and NIC status/config after lack of RX progress, with bounded UART output; no acknowledgement, reset, new goroutine or ring mutation. Original fixed snapshot and product driver remain unchanged. Candidate is not deployed: node remains unreachable after the controlled48MiB failure, awaiting physical recovery. All full acceptance/performance phases remain open.


L77 operator restart follow-up18September: health briefly answers, then the node loses RX during automatic GUI downloads before even the first /v1/jobs request of diagnostic deployment. No diagnostic FLIP was requested or performed. UART reports launcher stalled1,096,738/3,279,685bytes and display15,158/3,806,026bytes, then DNS timeouts. Therefore neither localHTTP nor48MiB allocation is necessary to trigger this failure. Prepared the same read-only RX diagnostic as a standalone EFI for direct boot: SHA256 974da50235e6541cf4e29a52eed9bac0aa54671ef026157d56871f7ab1b11061. Boot-medium installation pending; retain actual HOPOS.CFG.


L77 diagnostic boot-medium installation: replace only EFI/BOOT/BOOTAA64.EFI on operator-provided HOPOS stick disk5s1 with RX diagnostic SHA256974da50235e6541cf4e29a52eed9bac0aa54671ef026157d56871f7ab1b11061. Preserve a private backup of old EFI/config. Verify replacement and byte-identical HOPOS.CFG after unmount/read-only remount, then eject successfully. Diagnostic hardware boot and RX observations remain pending.


L77 diagnostic-stick follow-up18September05:55UTC: cold boot completes GUI downloads;128MiB runtime and node API are healthy. The48MiB ioobserve in desktop sharegroup downloads all6,179,761bytes and is marked running, but /status times out while both display and fixture report100%CPU. Node management remains responsive at this point: distinguish this app/sharing failure from RX loss. Remove fixture and display; both logical claims return to zero. A dedicated48MiB fixture attempt then loses network reachability. No RTL_RX_OBSERVE line has yet been obtained; the USB modem opens at115200 but captured zero bytes, including ordinary start/stop logs. Ask operator to verify UART wiring or read the diagnostic line from the display, keeping the stalled state intact. Installed EFI hash is known, but no diagnostic marker has been observed from the running hardware; do not claim a register diagnosis. Full acceptance remains open.


L77 first hardware NIC snapshot after USB-UART replug and cold restart: during clean48MiB lifecycle start, RTL_RX_OBSERVE frames=8846 head=142 own=256/256 current=0x80000800 next=0x80000800 ISR=0x40f5 IMR=0 CMD=0x0c RXCFG=0x40000f0e ringHW=ringPA=0xb9200000 busOff=0. Every descriptor is returned to the NIC and RX/TX remain enabled, while RX overflow/FIFO-overflow bits0x50 are latched with interrupts masked. This supports testing a narrow observed-overflow W1C acknowledgement in the existing poll path; it does not yet prove that acknowledgement alone recovers DMA. Keep other status bits intact and verify resumed reception on hardware.


L77 bounded RTL candidate: Receive OWN branch reads ISR and W1C-clears only observed RxOverflow|RxFIFOOver (0x50), then commits through the existing helper. No additional state, loop, worker or lock in the product change. Existing host RTL receive/bounds/recycle tests pass. The diagnostic candidate additionally logs its first acknowledgement and first subsequent consumed descriptor. Cold EFI SHA256 1117da59baa52220471728ad289554c1b635b664675d33d4de22712fa9d81722 built and archived; not installed/tested yet. ISR0x4000 has the historical PCSTimeout name, but that name alone is not a proven cause; the candidate preserves that and all other non-overflow bits. Physical medium requested because RX is still stalled.


L77 RX-overflow acknowledgement candidate installed on operator-provided HOPOS bootstick disk5s1: only EFI/BOOT/BOOTAA64.EFI replaced, SHA2561117da59baa52220471728ad289554c1b635b664675d33d4de22712fa9d81722. Old EFI and HOPOS.CFG backed up privately. Verify new EFI and byte-identical configuration after read-only remount, then eject successfully. Hardware acknowledgement/reception recovery and acceptance still pending.


L77 hardware recovery observed on RXACK candidate at06:10UTC: first48MiB lifecycle start reports RTL_RX_ACK frames=8061 head=125 own=256/256 before=0xb5 write=0x10 after=0xa5, followed by RTL_RX_AFTER_ACK head=125 opts=0x340285ee elapsed_us=13086 ISR=0xa5. Full6,179,761byte download completes and checked compute/start/stop cycles begin passing. This supplies observed acknowledgement→reception→complete-download evidence absent from the unmodified driver runs. Full lifecycle/storage/soak acceptance still in progress.


L77 RXACK candidate H1–H3 complete:20checked start/compute/identity/stop cycles, two same-address sampled zeroing canaries, two shared residents plus a dedicated two-core SMP resident, ten concurrent timer/wake rounds, neighbor-preserving replacement and exact CPU/RAM release all pass. Artifacts remain the frozen acceptance fixtures, downloads temporarily paced2MiB/s; no unrestricted throughput claim yet. H5 now performs two9GiB source+backup passes, position-dependent full content verification and removal. Current write timing includes pattern generation/SHA and app/syscall overhead; do not label it rawNVMe performance. H4 repeatedFLIP and H6 full-duration remain open.


L77 H5 pass1 complete:9GiB source-write300.46s, full source-read/copy468.43s, full backup-read265.15s; all phases agree on SHA256336ffb78ce871ace34665a0b6a6df29ddad97a469772359abab32ee1f939c17e. Both9GiB files removed and absence verified. Pass2 starts with a different position-dependent seed; full H5 signoff awaits that pass. These are end-to-end verification timings, not rawNVMe throughput.


L77 compatibility review: the persistent-origin decoder rejects unversioned facts on new incoming kernels, but ABI2/switch hashes do not automatically reject an old target that ignores that extension. Following patched cold entry, select only UEFI/O6N bundles implementing persistent cage ownership; old kernels are cold-boot-only. This round uses exact hashed new candidates. No legacy migration/downgrade support is claimed or added, consistent with the requested first-public-release scope.


L77 repeated UEFI FLIP candidate: retain the original 32MiB carve as the single owner of cage execution state through a versioned firmware-facts extension. Incoming kernels keep private control/DMA areas in their own carve and exclude the original owner from allocation; intermediate kernel windows return fully. Host tests and four actual QEMU transitions pass, including a live 48MiB resident with unchanged identity, verified compute and held TCP, followed by parked-core reuse. Final cold EFI SHA256 e037a56b61ec4ea67e76b7d82a64870ab8d33af057f20beb677586a68eaef43b and marked bundles10–13 are archived. Physical H4 remains pending; this candidate requires one patched cold entry before its FLIP sequence. Continue current H5 without interruption.


L77 H5 PASS on RXACK candidate: two full 9GiB source + 9GiB backup passes complete in2068.05s, each with absolute-position content verification and matching SHA256 across write/source-read-copy/backup-read. Different pass seeds produce different hashes. Remove and absence checks pass twice; final task removal returns CPU shares and memory claims to zero. Completed write/readback coverage only: no power-loss durability, flush, or exact physical-block-reuse claim. Proceed to GUI/sharegroup regression and independent Vitals performance measurements.


L77 GUI-sharing regression FAIL on RXACK candidate: display256MiB plus ioobserve32MiB in desktop sharegroup passes status/compute. Replacing that fixture with48MiB completes the full download and reports running, but status times out; both app CPU readings reach100%, while node management remains responsive. Removing display does not restore the fixture within6s. Removing both releases all claims; a fresh dedicated48MiB fixture passes status and compute. This is distinct from the fixed NIC RX stall and prevents an overall sharing signoff. Source investigation proceeds while independent Vitals measurements run on11appcores.


L77 independent Vitals:14selected tests complete without error on10cores/512MiB, including120.13s burn (3898→3897Msteps/s,45C maximum), shared-heap SMP verification(7.99x), SQLite65.40s, networking/syscall/GC/timer. Disk256MiB with1MiBcalls:write46.48MB/s,read57.02MB/s,4KiBwrite19.99MB/s,statp50 76.14us. These measure the full application I/O path, not rawNVMe. All11cores could not be allocated after the GUI regression;10could. Correct the cleanup interpretation: API claims0 reflects task accounting, not proof that the physical allocator released the stuck core. Source quarantine retains ownership after failed Stop. GUI issue and physical full-capacity recovery remain open.


L77 ordering audit: insert ISB between VTTBR_EL2 write and VMALLS12E1 TLBI in primary-app, secondary-app and node-SMP entry (three instructions, two assembly files). Xen p2m_force_tlb_flush_sync documents and implements this context-synchronization requirement (https://github.com/xen-project/xen/blob/master/xen/arch/arm/mmu/p2m.c). Actual assembly checks for nVHE/VHE/Apple and full generic VHE build pass. Both unmodified and patched QEMU pass three heterogeneous256+32→48MiB replacement rounds, so QEMU does not reproduce or prove resolution of the hardware hang. Root applies the reviewed minimal ordering correction; physical A/B remains required. This changes trampoline compatibility; prepare a new cold candidate and matched bundles rather than attempting resident migration from the diagnostic kernel.


L77 final controls before new cold entry: generic ioobserve256MiB resident + fresh48MiB shared fixture passes; replacing48 with32MiB hangs before status. This reproduces without GUI/framebuffer. A display-without-FB control could not start because quarantined cage1 still owns ports80/7878; do not infer anything from that unexecuted control. All visible jobs removed, but physical cleanup remains unproven. LAN64MiB measurement:45.4MB/s download,39.3MB/s upload (1MiB requests). New cold EFI SHA2561f0cc4ce709b899dd67fe90f390e0e949516c9eb2dfff4a3ec8e9b1617676084 and bundles14–17 archive the persistent-origin, RTL RX acknowledgement and3ISB ordering fixes; matchedswitchhash0x021789b0ae1900e7, linked opcode checks and targeted tests pass. No hardware deployment yet. Next: install EFI preserving configuration; cold boot; first verify heterogeneous32/48replacement and complete core/port reclamation, then three liveFLIPs and full1800sH6. Restore display/launcher after recovery. USB input remains separate open platform wiring.


L77 ordering candidate boot-medium installation,18September07:04UTC: operator supplies HOPOS USB stick disk5s1. Back up original RXACK EFI and HOPOS.CFG privately, replace only EFI/BOOT/BOOTAA64.EFI with SHA2561f0cc4ce709b899dd67fe90f390e0e949516c9eb2dfff4a3ec8e9b1617676084, flush and remount read-only. Replacement hash and byte-identical configuration verified; stick ejected successfully. Hardware cold boot and heterogeneous replacement regression remain pending.


L77 ordering candidate cold boot confirmed18September07:12UTC after operator returns prepared EFI. Original display/launcher start without restarts. Previously failing GUI control now passes32→48→32→48MiB with retained display and checkedcompute. Generic256MiB resident plus48→32→48→32→48MiB replacement also passes, including neighbor compute each round. Physical reclamation verified by a single512MiB fixture using all11cores with correct compute, then display restart reclaiming ports80/7878. Final removal yields zero visible tasks and claims. Record these as physical regression passes, not full acceptance: fresh20cycle/canary lifecycle, sharing/wake, three liveFLIPs and1800s combined test now follow on the exact installed candidate.


L77 physical H1–H4 PASS on ordering candidate:20cycles,2sampled-zeroing canaries, mixedshared/SMP residents,10wake rounds and exactrelease pass. Three marked liveFLIPs14/15/16 land as generations1/2/3 with all3original task/boot identities retained, independently checkedcompute and the same three TCPsocket objects throughout. Echo maxima7.99/8.84/9.18s during transitions: connection survival is proven, uninterrupted response latency is not. Handoff reports3/3residents and0/0NATflows; do not label absentNATflows as exercised. Full1800s combined workload begins on the surviving residents.


L77 H6 first attempt FAIL after one completedround. Shared48MiB storagefixture restarts; nodeandneighborsremainhealthy. Reproductionwithliveapplogcaptures runtime outofmemory:cannotallocate16777216byteblock,29130752inuse,atacceptance/ioobserve/main.go110. Fixtureallocatesanew16MiBpayloadforeachIOrequest. Preparefixture-onlyboundedbufferreuseundertheexistingactionCAS; noOSchangeorlowerIOsize. Preservefailureevidence and restartthefull1800sdurationonfreshresidents; previousattemptisnotacceptance. Visiblejobsremovedandlogicalclaimszero.


L77 fixture correction built separately:ioobserve-soak.elf SHA256ac971bda64642f4ee76bce2f229371890d2e551fff52c82f86c1e155665cd7fc reuses16MiBdataand1MiBreadbuffersundertheexistingCAS. Frozenproductdependencies/toolchain matchtheoriginalfixture; oldartifactpreserved. Fresh48/48/96MiBresidents restartfull1800stestandpassinitialrepeatedIO/appreplacementrounds. Fullhost/import/targetgate alsoPASS onanisolatedcopyofthefrozenorderingcandidate,includingAppleandRISC-V,noskips; originalsnapshotunchanged. Logsarchiveunderordering-full-gate.


L77 H6 PASS on ordering generation3 with the corrected bounded-buffer test fixture: 1800.8579 seconds, 1034 rounds, 28 shared-app replacements, 271056896 echoed bytes in each direction on one retained socket, and 17347641344 bytes written plus fully read/verified on NVMe. Every round checks independent compute, content/hash, cleanup, wake and constant capacity. No unexpected restarts or content errors. After removal: zero tasks, CPU shares and memory claims. The failed first fixture-OOM attempt remains separate evidence. Final11core Vitals/reclamation and the separately planned controlled watchdog fallback follow.


L77 final Vitals PASS on ordering generation3: all14selected tests on11cores/512MiB, no unexpected restarts. The120s burn measures4397→4403Msteps/s and44C maximum. Three256MiB/1MiB-call disk repetitions measure writes46.00–46.37MB/s and reads55.53–56.91MB/s. Three64MiB LAN repetitions measure download44.24–45.06MB/s and upload35.65–37.88MB/s. Metadata/4KiB subtests and full results are in the compact evidence. These establish current-candidate spread; earlier10core RXACK readings are not a matched three-run before/after performance comparison. Final removal returns zero tasks and claims; all11cores were exercised after the full duration test. Controlled failedFLIP18 now runs with no residents and unchanged watchdog policy; UART confirms armed guard and injected pre-agent wait.


L77 controlled watchdog fallback PASS: with no resident apps, diagnostic bundle18 deliberately waits after the normal boot guard is armed and before starting the agent. UART records HOPOS_FLIP_JUMP, the exact injected marker, and HOPOS_BOOT_GUARD_EXPIRED. Without manual intervention, firmware cold-boots the installed ordering EFI and the normal agent returns after146.39s from the request. This is watchdog recovery from an intentionally failed update, separately recorded from successful zero-reboot FLIPs. Original display/launcher start from the unchanged boot configuration. No production watchdog policy or timeout was altered.

L77 closing scope: the recorded O6N lifecycle, heterogeneous shared-memory replacement, SMP/wake, three liveFLIPs, all-core/port reclamation, full30min compute/network/NVMe run, final14-test Vitals and controlled watchdog fallback pass. Large two-pass9GiBsource+9GiBbackup integrity evidence belongs to the preceding RXACK candidate; subsequent ordering changes affect EL2 synchronization and persistent UEFI cage ownership. Full host/target gate passes on the frozen ordering source. Current-candidate disk/network measurements have three repetitions; a matched three-run before/after comparison is not claimed. USB keyboard/mouse registration, physical networkIRQ and performance/power improvements remain in the board follow-up list. M4 and release packaging remain separate work. No commit or release was made in this round.


L77 final state verified: only display and launcher, unchanged task identities across the final observation, zero restarts, one shared physical core and288MiB claimed memory; node health OK and displayHTTP302 responds. Original boot configuration is unchanged. Temporary artifact server and UART capture stopped; other devices untouched. Final artifacts and source remain local/uncommitted for review.


### L77 follow-up — NVMe performance and USB input (18 September)

Derek reopened NVMe performance and physical USB input after the preceding acceptance round. Integrity acceptance did not establish adequate throughput, and the original O6N build had no USB host registration. All work below is local and uncommitted. Compact records: [NVMe comparison](release-evidence/2026-09-18-o6n-nvme.json) and [USB/input acceptance](release-evidence/2026-09-18-o6n-usb.json). Full local traces and frozen sources are under `docs/v1/reviews/2026-09-18-o6n/`.

1. **NVMe baseline and cache correction.** Diagnostic19 runs the existing raw and HopFS benchmarks. At 1 MiB per command, raw write/read is 49.1/59.1 MB/s; HopFS is 48.6/59.1 MB/s. The bottleneck is already below HopFS. Candidate20 separates a whole 2 MiB write-back data block from uncached queues/PRPs, reusing the existing Push/Pull cache ownership operations and the M4 approach. The reservation stays within the existing carve. Host NVMe/HopFS and DMA-boundary tests pass; independent review finds no buffer-ownership blocker.

2. **Matched physical comparison.** Cached raw write/read is 3056.8/2705.6 MB/s; HopFS is 2970.1/2716.7 MB/s. Those are single 16 MiB boot-benchmark runs. Three matched 256 MiB app runs, with the same 512 MiB/eleven-core Vitals fixture, move from write45.61–46.40/read55.37–56.56 to write556.83–625.43/read727.49–796.83 MB/s. LAN results, hashes and exact conditions are in the NVMe record. Initial harness attempts encountered automatic GUI reseeding after an empty FLIP and a transient DELETE502; their logs remain, and only the subsequent complete matched runs count.

3. **Independent cached-data verification.** A separate fixed-buffer fixture completes two 64 MiB passes with distinct position-dependent patterns, mixed512B/4KiB/64KiB/1MiB requests, overlapping boundary writes, immediate surrounding readback, full readback, exact size/EOF and removal checks. It verifies144writes/136603648bytes and532data reads/136617984bytes. It passes first on diagnostic21, again on normal23, and finally after the two accepted GUI FLIPs on30. No unexpected fixture restart. These are completed-write/readback checks, not flush or power-loss durability claims.

4. **Actual firmware discovery.** Read-only diagnostic21 captures the complete71,872-byte DSDT through UART, verified SHA25632366cf689b027494a1cd97df9f77d717c9d114080edb25b7d40c70300d4eff2. GNVA is0xfffe0000, length0x4d; enable/role bytes at0x12 are01010101010101010101000000. Only checksum-validated ACPI RAM is read. The initial interpretation of CIX's `USB_EHCI_HOST` macro names was wrong: actual DSDT PNP0D10 nodes and the pinned Sky1 primary sources identify all ten controllers as xHCI. The source fixture README records provenance. No separate EHCI driver was needed.

5. **USB wiring and first failures.** USB DMA uses the final2MiB of the original persistent32MiB carve, with no added reservation. Firmware parsing validates controller resources, enable and host-role conditions; all1024 enable masks retain stable host positions. Before clearing any DMA slice, all enabled controllers are reset; a failure prevents all starts. Existing xHCI allocation now reserves at most one device slot per root port, matching its direct-device-only support. Candidate22 boots but fails before enumeration because its input listener explicitly binds the internal gateway VIP. Candidate23 reuses the system service's wildcard node-stack bind while retaining the framebuffer-holder peer check. Six controllers and the display stream then start; this was partial coverage, not a mouse pass.

6. **Receiver detection and unnecessary polling.** Candidate24 starts all ten controllers and detects Logitech keyboard+mouse046d:c548 on USB0port1, full-speed. The SuperSpeed device on XHC4port2 is not boot-HID; initial AddressDevice timeout recovers through the existing reset path. Candidate26 remembers this connected non-HID device in the existing port map until unplug/recovery, removing repeated enumeration and log churn without another worker, map or lock. A fresh display attaches successfully. Physical cursor/click confirmation was requested separately.

7. **Live-FLIP input recovery failures.** Surf's input Scanner originally waited indefinitely when the node-side TCP state disappeared. The node now sends an empty line every5s in its existing input worker; Surf refreshes a15s read deadline and uses its existing2s reconnect loop. Updated display ELF SHA25634684f1d0ccb79998d63aa5b2130bae5c7f504a01243ae6c4ec3284c0be9c729 is installed once with unchanged256MiB allocation, ports and desktop sharegroup. Candidate28 retains all three then-present apps and the fixture TCP connection, but input reconnections are rejected: framebuffer-owner bookkeeping was not adopted. Derek also observes the boot console drawing over SURF. This failed GUI acceptance remains recorded.

8. **Grant ownership and console correction.** Restore the framebuffer owner only from the inherited kernel-owned stage-2 mapping, validating the complete current window, attributes and owned table pointers. Absent mapping means no grant; malformed, partial, broader or duplicate ownership fails adoption. No app-writable environment is trusted, no mapping is rebuilt and no FLIP ABI field is added. Restore grant bookkeeping before slot services. UEFI still maps GOP memory on every boot but initializes its console early only on cold boot. Main defers the FLIP console until adoption, then starts it only when no display owns the framebuffer. This preserves both cold/empty-node console behavior and a resident display's pixels.

9. **Final physical regression PASS.** Distinct bundles29→30 land as generations10→11. All five residents retain exact task IDs: updated display, launcher, Derek's newly started clock/browser and the independent compute/TCP fixture. Checked compute and the same TCP socket survive both transitions; three NAT flows adopt3/3. Input automatically reattaches after22.62/19.28s from the request. Neither boot initializes the framebuffer console. A35s quiet check keeps one input attachment without reconnect churn. The final mixed-size NVMe content test passes. Test jobs are removed; display, launcher, clock and browser remain, with node health OK. These are recovery delays, not zero-interruption input latency. Physical cursor/click, keyboard and replug confirmation remain separate from enumeration and automated stream checks.

10. **Validation and final artifacts.** Full host/import/target gate passes without skips. Grant race tests pass with checkptr disabled for existing MMIO fixtures; an earlier unadjusted xHCI/checkptr failure is retained separately. A late one-file correction preserves high-GOP mapping on warm boot; its exact delta and successful UEFI GUI/O6N GUI/O6N VHE GUI builds supplement the full pre-delta gate. Final source/dependency manifest SHA2568bbfcab45d3061240186c0a31891593cefdc1afbcb40f28e08a57d7a9f84c99d. Matching coldEFI30 SHA2567c7e6026878004a5da0f0f25535fff6f7afa2ad755806e9f076546067ebb4551 is prepared, not flashed, and supersedes cold27. Existing device boot configuration is unchanged. The running display references the local test artifact server; retain that server for app restarts until the updated display is published. Product changes are in HopOS and canonical `hop-os-surf`; no commit or release was made.

Harness limitations are preserved in the raw records: large firmware output and repeated non-HID logs displaced prior generation markers from the TCP replay ring. UART/previous hardware evidence confirmed those transitions; they were not counted as held-socket passes. The later checker can take an explicitly evidenced prior generation. A same-bundle re-FLIP was correctly rejected without jumping. Another preflight stopped before mutation when Derek's clock/browser increased the resident count from three to five; final acceptance included all five.


**Post-test GUI stability issue (open):** at09:35:41UTC, several minutes after the accepted FLIP/quiet/NVMe checks, display task00TWAH0RC7JC8S6H exits2 and automatically restarts asNG1JBTQDX543K8YY (restart1). Node health and other apps remain active. Derek is using the desktop and opening/closing applications; exact trigger requested. The recovered log is only a partial runtime stack, so no cause is asserted yet. Preserve the completed bounded checks above, but do not claim overall GUI stability or a restart-free final observation. Live display logging and source investigation follow.

Post-failure source review found no demonstrated cause in the retained goroutine tails. Ten repeated host test runs reproduced no display crash or race report; one WebSocket input test timed out and is retained separately, not attributed to the hardware failure. Evidence: `reviews/2026-09-18-o6n/display-crash-source-review/`. A subsequent task snapshot still shows the same replacement display task with restart count1. No speculative crash fix was applied.

### L77 follow-up — non-disruptive M4 storage measurement (18 September)

Derek authorized storage tests on192.168.1.122 while retaining all existing residents. A temporary one-core/512MiB Vitals job performed three256MiB file write/read tests with1MiB calls in its own filesystem namespace. Writes measured1598.14–1656.67MB/s; reads1063.90–1072.05MB/s, including repeated-pattern readback checks. All runs passed. The test job was removed, resource claims returned to the original values, and Spin/cloudflared retained their task IDs, start times and zero restart counts. No kernel update, FLIP, reboot, existing job change or raw-device benchmark was performed. The raw driver benchmark is boot-only; it was not invoked against live storage. These app-path results do not independently confirm the earlier approximately4000MB/s M4 figure. [Compact evidence](release-evidence/2026-09-18-m4-nvme.json).

### L78 — M4 round started: boot benchmark and lifecycle pass, node dark after the candidate FLIP (18 September)

Derek authorizes the complete M4 round on 192.168.1.122, including FLIPs and removal of the running apps. Both original job definitions (cloudflared, Spin) are saved privately and deleted; restore order remains cloudflared first, Spin second. Evidence: [compact record](release-evidence/2026-09-18-m4-round.json), raw records in `reviews/2026-09-18-m4/`.

1. **Bundle 1 FLIP and boot benchmark.** Four FLIP bundles are built from the current hop-os working tree with `hopos.nvmebench=1` embedded in the configuration of the first three. The empty node adopts bundle 1 as generation 6 in 9.71 s; the embedded configuration then autostarts the Welcome job. The raw driver benchmark at boot reads, for 1 MiB commands, 5389.3 MB/s write and 1971.3 MB/s read; HopFS with 1 MiB calls gives 5499.8/1781.2 MB/s, with 64 KiB calls 4411.8/475.7 MB/s. 4 KiB raw commands give 1022.1/453.9 MB/s. These are single boot runs of 16 MiB per size on the tail of the HopOS window; completed I/O only, no flush or power-loss claim. This is the first recorded raw M4 figure with conditions; it is one run, not the three the plan asks for.
2. **H1 lifecycle PASS on generation 6.** Twenty start/compute/stop cycles produce twenty distinct task IDs and boots; placement of the 5 MB fixture takes 124–137 ms; CPU and memory claims are zero after every cycle. Two memory-canary cycles arrive zeroed and repaint fully.
3. **H2 sharing FAIL, cause established.** The two sharegroup fixtures are placed as cages 10 and 11 and the SMP fixture as cage 1; the harness expects cages 1, 2, 3 (L59). The M4 bundle links the published hop v1.0.6, whose runner still puts shared cages above the physical core range. The L59 cage/core separation exists only in the local hop working tree, and the O6N candidates were built from a snapshot with replace directives to the local hop and lean trees. Bundles 1–4 are marked NOT_DEPLOY. Bundles 5–8 are rebuilt with the same snapshot recipe (local hop, local lean, tamago `hopos-highram`); the snapshot manifest records every tree's HEAD, diff hash and status.
4. **Bundle 5 FLIP: node dark.** With the test residents removed and capacity clean, POST /flip for bundle 5 returns 202 at 13:13:15 UTC. From then on 192.168.1.122 answers neither ARP, the TCP console nor the agent; a /24 scan for ports 8080/5555 finds only the router and the LicheeRV. After more than twenty minutes the node has not cold-recovered, whereas the 9 September watchdog recovery took 167 s. No debug USB (kis) link is attached to the mini, so there is no console and no remote reset. The only difference from bundle 1 is the local hop (runner allocator, agent handoff signature) and local lean (ephemeral port seed, per-peer congestion control). The O6N and Pi rounds entered the local-hop kernel by cold boot, never by a FLIP from a hop-v1.0.6 kernel; that transition is now the prime suspect, but no cause is established.

Not done: H2/H3 on a correct candidate, H4 live FLIPs with held TCP, two further boot benchmark runs, the 9 GiB source-plus-backup HopFS check, the thirty-minute soak, the fourteen Vitals tests with disk/network repetitions, the final candidate FLIP, and restoring cloudflared and Spin. Next step requires Derek: power-cycle the mini and, preferably, attach the debug USB-C cable so the dockchannel console shows the next attempt. No commit or release was made.

### L79 — M4 round completed except the thirty-minute soak; FLIP departure bug found and fixed (18 September)

Continuation of L78 after two power cycles. Evidence: [compact record](release-evidence/2026-09-18-m4-round.json); raw records, harness and bundles in `reviews/2026-09-18-m4/`.

1. **Root cause of the dark node and the clean boot.** After the first power cycle the installed kernel adopted bundle 5 (local hop and lean) as generation 1 without trouble, so the candidate itself boots. The next FLIP, 5→6 with three residents, jumped and the new kernel started clean over them: `No committed state found — clean boot`, residents lost, and the following boot's recorder said `HOPOS_FLIP_STALLED step 12/11`. Cause: the uncommitted kernflip change from the O6N work relocates the handoff pointer/magic pair to the borrowed window offset because the UEFI boot scratch lies inside the kernel window. Apple, Pi, RK3566, QEMU and LicheeRV have a fixed cpuinit scratch, so the incoming kernel read the fixed address and found nothing. Every FLIP that departed from a kernel of this tree failed on the M4; departures from older kernels still worked, which hid it. Fix: `kernflip.BoardScratchInWindow`, set only by the two UEFI board wirings; the pair moves only there (flip.go, board_uefi.go, board_o6n.go). The QEMU flip regression passes with the fix: two residents survive three kernel flips, NAT and pool checks pass. A later demo step in that run, `FAIL smc-kooi: slot 2 still owns memory`, is unrelated to the flip and was not investigated. Two clean boots over live parked cores left the M4 unable to start a second app core (`apple: cpu2 did not start`, `HOPOS_PART_QUARANTINE`); a second power cycle cleared that state, and nothing from it is counted.
2. **Boot benchmark, three runs (H5).** Raw NVMe driver, 1 MiB commands, 16 MiB per size, tail of the HopOS window, completed I/O only: write 5389.3 / 5755.8 / 5672.4 MB/s, read 1971.3 / 1846.5 / 1824.1 MB/s. HopFS through 1 MiB calls: 5499.8 / 5631.6 / 5480.1 write, 1781.2 / 1816.1 / 1801.5 read. 4 KiB raw commands: 1022–1189 write, 454–459 read. This replaces the earlier developer-reported approximately 4000 MB/s figure; it is a driver benchmark, not a durability or sustained-throughput claim.
3. **H1, H2, H3, H4 on the corrected candidate (bundles 11–14).** Bundle 11 adopted from the installed kernel as generation 1 (boot guard armed, no stall). Lifecycle: twenty cycles, twenty distinct task IDs and boots, clean claims after each, two zeroed canaries. Sharing: cages 1, 2, 3 as L59 requires, ten concurrent nap/wake rounds, SMP release returns 2048 shares and its memory, neighbour replacement keeps the survivor. Live FLIPs 11→12→13 land in 9.67 and 9.73 s with 3/3 residents adopted, identical task IDs, and the three held HTTP sockets unchanged. The final candidate, bundle 14, lands as generation 4 in 9.12 s with 3/3 residents and held sockets; longest exchange pause 12.8 s.
4. **H5 storage.** Two passes of 9 GiB source plus 9 GiB backup on HopFS pass in 457 s: source-write 72 s, source-read-plus-copy 82 s, backup-read 74 s per pass, both files removed and reallocated, seeds differ. This closes the physical large-copy item from L62 for the M4. Completed writes and readback only; no flush or power-loss claim.
5. **Vitals.** All fourteen tests pass on three big cores with 512 MiB: cpu 723 Msteps/s, smp speedup 2.8× on 3 cores, burn 0.0 % degradation, membw copy 35.8 GB/s, disk 1725/1137 MB/s with repeats 1713/1126 and 1742/1120, sqlite insert 537 MB/s, storm 9398 conn/s with p99 1.5 ms, rtt p50 39 µs, timer oversleep p99 19 µs. Network 64 MiB: download 44.7–48.3, upload 38.7–41.4 MB/s over the 1 Gbit tg3 link. A first attempt with nine cores dispatched only two secondary cores, without any error line, and the HTTP state poll starved during the cpu test; that observation is recorded, not explained.
6. **H6 soak: cause found, fixed in the app toolchain, then PASS.** Four attempts failed first. The first, with residents adopted through two FLIPs, lost the 48 MiB ioobserve fixture to its known memory exhaustion (the L77 fixture correction) and does not count. Attempts two and three with the corrected fixture reached 155 and 77 rounds and died at the first or second shared-cage replacement: the freshly started shared app faulted right after `HARDWARE_APP_BOOT` with a stage-2 data abort, ESR 0x93c18006, FAR 0x4ffffff0 (sixteen bytes below its partition), pc 0x50085898 in `runtime.findTimer`. The same ESR and FAR appear in the M4 console of 6 September on slot 1 (cloudflared) after a rolling update of Spin. Forty-eight isolated replacements in six variants did not reproduce it. The linker dependency dump (`-ldflags=-dumpdep`) found the path no source grep showed: `cpu/idle.serveDoor` registers the doorbell through `os/signal.Notify`; TamaGo's arm64 `handleInterrupt` calls `os/signal.Relay` on every interrupt, and Relay is `B runtime·WakeG` on the signal-loop goroutine. `findTimer` searches that goroutine's cached timer in the timer heap by walking from the last element downwards with address zero as its only bound. When the doorbell fires while the signal loop is not sleeping (just woken, or a fresh app before its first sleep), the timer is not in the heap and the walk leaves the slice, runs down through the partition and faults at its first unmapped address, sixteen bytes below the partition base. Only the M4 has the vFIQ doorbell; the other boards poll, which is why only the M4 shows it. Fix: [patch 0003](../../../tools/tamago-go/0003-runtime-findtimer-heap-bound.patch) bounds the walk at `heap[0]` on arm64 and riscv64; a miss returns failure and the level interrupt fires again once unmasked. The soak fixture rebuilt on the patched toolchain (`ioobserve-soak2.elf`, SHA256 de6b56f0b8cb1fadc55f8d99c9992d9bc2d4e4d924902907689daf10ebb851d4) then passes the full run: 1800.8 s, 1775 rounds, 29 shared replacements, 465 MB echoed each way on one held socket, 29.8 GB written to and read back from HopFS with matching SHA-256, zero unexpected restarts, all claims released. Every application binary must be rebuilt with the patched toolchain before the release; existing binaries carry the unbounded walk.
7. **Restore.** cloudflared and Spin are back from the saved definitions, in that order, both running with zero restarts on generation 4 (bundle 14). No commit or release was made.

### L80 — O6N network interrupt: freeze explained, storm removed, on by default (18 September, evening)

Derek asks to finish the NIC interrupt path now, for the O6N and later Ampere, instead of leaving it for after the release. Evidence: [compact record](release-evidence/2026-09-18-o6n-nicirq.json); bundles, harness and raw results in `reviews/2026-09-18-o6n-irq/`. Every FLIP in this round kept Derek's display and launcher residents; the O6N was cold-booted once by the watchdog and re-seeded them.

1. **The L76 freeze.** With `DefaultNICIRQ=auto` the node went dark within a minute (bundle 31) and cold-booted from the stick in 35 s; the same tree with the interrupt off adopted normally (bundle 32, control). The dispatcher in `cpu/irq` claimed until the controller had nothing more, but a level line that stays asserted after the device ack comes back on every claim: one isr pass never ends, with I masked, and the SBSA watchdog resets after 12 s. Bundle 33 makes the dispatcher disable an unregistered line at once and a line that keeps returning after 256 claims in one pass, one console line each; the node then stayed up and said `INTID 477 keeps asserting after 256 acks in one pass — line disabled`. The RTL8125B behind root port bus 0x30 is INTID 477, not the 468 of the first LAN port.
2. **Why the line stayed high.** r8169 masks the interrupt in its hard handler, lets NAPI drain the ring and unmasks afterwards; HopOS only acked. `RxDescUnavail` latches again immediately while the ring is full, and the pump never got the core. Bundle 34: `AckIRQ` writes IMR 0 before the ack, and `WaitNIC` rearms IMR after the pump round. No stray line since.
3. **The storm.** With the interrupt wired, rtt p50 was about 1 ms against 160 µs polled (control bundle 32, same tree and evening). A counter showed why: about 36,000 claims per second while the pump saw a frame in only one of about 340 wakes, a storm of empty interrupts, not goroutine latency (bundle 42 histograms: ack→frame under 100 µs whenever a frame was there). Two diagnostic bundles that did a little extra work between reading `IntrStatus` and writing it back did not storm; the isolated ingredients (a `time.Now`, an atomic) did. Acking with a write-one-to-clear of all bits, as `rtl8169_irq_mask_and_ack` does, removes the storm (bundle 44: 1,813 claims in 40 s including an rtt run). Which bit survived "write back what you read" is not identified; the pump reads the ring, not the status bits, and `Receive` clears the overflow latches itself, so acking everything loses nothing.
4. **Final numbers (bundle 45, clean tree, `DefaultNICIRQ=auto` is now the O6N default).** Generation 19, 2/2 residents adopted. rtt p50 156, 201 and 200 µs (polled 160 and 168 µs); storm 844 conn/s, p99 19.6 ms (polled 780); rx 25.5 and 29.4 MB/s from the internet, which bounds it. A wake every 300 µs becomes one per received frame plus a 10 ms guard.
5. **Also in this round.** `WFESleep` now leaves its WFE loop when the earliest runtime timer is due, so an interrupt taken during the sleep returns the core to the scheduler instead of waiting for the next event-stream tick; on its own it did not change the rtt figure, but it is the correct behaviour once interrupts exist. The generic UEFI board (Ampere) still has no NIC line lookup (DSDT `_PRT` or MSI) and keeps polling. No commit or release was made.

### L81 — M4 network interrupt: AIC3 works, the NIC line does not yet (18/19 September, night)

Derek asks for the M4 to be finished including its network interrupt, like the O6N. Bundles 15–23 in `reviews/2026-09-18-m4/`; every step ran as a live FLIP from the previous kernel, with the node emptied first because the installed kernel refuses a FLIP over a resident when the switch code differs.

1. **Read-only probe (bundle 15).** AIC3 (`aic,3`, version 0xa) at 0x381000000 with 2048 of 4096 interrupt numbers, config words at +0x10000, event register at +0x40000, global config at +0x14; iBoot leaves the controller **off** (bit 0 clear), every mask set and no targets configured. MSI facts: doorbell 0xfffff000, vector offset 1506, port 2 vector base 64 (interrupts 1570–1601), the NIC's 64-bit MSI capability at 0x58 off.
2. **What works (bundles 16–22).** New `driver/aic` (AIC2/AIC3 layout as m1n1 derives it from the ADT; the controller masks a claimed interrupt itself, `Complete` unmasks), `idle.ServeIRQ` (tamago's service loop with only the I mask opened, F stays closed for the Apple timer FIQ), the tg3 MSI and interrupt-mailbox helpers, and the Apple board wiring. A software-raised interrupt on line 1570 reaches HOP's core (MPIDR 0x10100) with AIC target 0, through tamago's vector, `os/signal.Relay` and the `cpu/irq` dispatcher; the node keeps running with I unmasked. The port lookup from the ADT works for a flipped kernel that finds PCIe already up.
3. **What does not work yet.** MSI: no port register at +0x2000, +0x2800 or +0x160 holds the doorbell address, and the firmware's MSI map entry at +0x3000 reads 0x80000200; bundle 18, which cleared the tg3 `MASK_PCI_INT` and enabled MSI without a known doorbell decode, took the node down (watchdog recovery, no console: the wiring ran before the TCP console existed). INTx: the apcie node lists one line per port (1231, 1240, 1249; port 2 takes 1249), the port's INTMSK is 0xfffffff0 and INTSTAT shows only bit 12 (link bandwidth), but the tg3 never raises INTA#: no port status bit and no interrupt within four seconds after opening the interrupt mailbox with `MASK_PCI_INT` cleared (bundle 22). Bundle 23, which briefly enabled lines 1231–1260 to catch a differently ordered port line, went dark right after landing, before its first console line.
4. **With the debug cable (bundles 24–25, after midnight).** Derek attached the USB-C debug cable; `macvdmtool debugusb` exposes the kis channels and `image/apple/console.py` reads the dockchannel, so a crash now leaves its last lines. Bundle 24 shows the whole picture: after `MASK_PCI_INT` is cleared and the mailbox opened, the tg3 status word reads 0x3 (updated) and the PCI status register has bit 3 set: the chip asserts INTA#. The root port raises AIC interrupt **1253**, four above the port's first line 1249, so the port owns a block of nine lines and INTA is the fifth. The crash of bundle 23 was the ack: the port's INTSTAT (+0x100) is readable but a write-one-to-clear to it is a synchronous external abort (ESR 0x96000410, FAR 0x492028100), followed by an SError storm. The write is unnecessary: the line is level and drops when the tg3 interrupt mailbox is masked. Bundle 25 (scan the port's nine lines with a counting ack, ack = mailbox mask only, rearm from `WaitNIC`) is built; before it could be tested the node hung after the bundle 24 panic in a state that still answers ping but neither agent nor console, the Apple watchdog did not reset it (the exit path appears to disarm it), and `macvdmtool reboot` did not bring it back; the debug channel is lost on every reboot and must be re-enabled per attempt. A power cycle is needed to continue.
5. **State.** The M4 stays on polling; `DefaultNICIRQ` for Apple is "0" and the wiring only runs with `hopos.nicirq=auto` or the build-time knob. The new code stays in the tree as work in progress, with the ESR/FAR/PC exception report for HOP's core now in the Apple agent. Next: power-cycle, flip bundle 25 with the console attached, confirm the interrupt drops after the mailbox mask, then the rtt and idle comparison against polling as on the O6N.

### L82 — M4 network interrupt on by default (19 September, morning)

After the power-cycle question resolved itself (`macvdmtool reboot` took effect in the morning), bundle 25 landed and did what L81 predicted: the port's nine lines scanned with a counting ack, INTA of root port 2 on AIC interrupt 1253, the ack only masks the tg3 interrupt mailbox, the line drops (PCI status bit 3 clear), `WaitNIC` rearms after the pump round. No stray line, node stable. Evidence: [compact record](release-evidence/2026-09-19-m4-nicirq.json); bundles 15–26 and the harness in `reviews/2026-09-18-m4/`.

**Same-morning A/B, Vitals with two cores, from this workstation over 1 Gbit:**

| | Interrupt (bundle 25) | Polled (bundle 14) |
| --- | --- | --- |
| rtt p50 | 50 / 62 / 66 µs | 48 / 63 / 54 µs |
| storm | 6379 conn/s, p99 1.9 ms | 6537 conn/s, p99 1.9 ms |
| 64 MiB upload into the node | 46.7 / 43.3 / 43.3 MB/s | 36.9 / 36.6 / 38.3 MB/s |
| 64 MiB download from the node | 47.7 / 48.2 / 52.3 MB/s | 46.9 / 44.1 / 44.7 MB/s |
| 32 MB from the internet | 42.7 MB/s | 41.6 MB/s |

Latency and connection rate are equal within noise; inbound throughput is about 15 % higher, and the node no longer wakes 3,333 times per second to look at an empty ring. `DefaultNICIRQ` for Apple is now "auto"; `hopos.nicirq=0` or the build knob returns to polling. Bundle 26 (the clean tree with that default) runs on the M4 as generation 3 with cloudflared and Spin restored, zero restarts. What remains open on the M4 is recorded in the evidence: no idle wake-rate measurement yet, one unattributed "unexpected EOF" on an internet download of bundle 25 that the repeat did not reproduce, the AIC target semantics measured rather than documented, and the watchdog that did not reset the node after the bundle 24 panic. No commit or release was made.

### L83 — No network is not a dead node; Ampere finds its own NIC line (19 September)

Derek's two requirements of the morning: a node that boots without an external network must keep trying instead of parking ("if everything starts without network I do not want a hundred dead nodes"), and a running node that loses its DHCP lease during an outage must do the same. Plus the Ampere at 192.168.1.178 (127 app cores, 411 GB, Intel I210 behind root port bus 0, no NVMe yet) with the same interrupt path as the O6N. Evidence: `reviews/2026-09-18-m4/` (bundles 27–28) and `reviews/2026-09-19-ampere/`.

1. **Boot without network.** Until today `main.go` parked the node forever after a failed `hopnet.Up` ("headless — no external network, agent/leader not started, HOPOS_NODE_HEADLESS"). Now it retries the whole bring-up with a pause growing from 5 to 30 s, one console line at the first attempt and every tenth (`HOPOS_NET_RETRY`), and only then starts the agent. The cold boot guard pets the watchdog blindly meanwhile; a flipped kernel loses its two-minute grace and comes back cold, where the same loop waits. Tested on the M4 with a build-time knob that fails the first two probes on purpose (`hopos.dhcptest=2`, Apple board only): two `dhcp: test failure` rounds, then `HOPOS_NET_UP`, the interrupt wiring once, and `HOPOS_AGENT_UP`; the tg3 bring-up ran three times on live hardware without complaint. The Apple board now reads its `hopos.*` knobs through the kernel's config reader (`hop.Config`); before, `apple.BootParamAll` only saw a loader's parameter block, which the installed boot object and every FLIP lack, so `hopos.nicirq=` never did anything on the M4.
2. **Lease lost while running.** `leandhcp.KeepAlive` gives up when a lease expires without an answer, is refused, or moves ("a reboot acquires a new address"). `hopnet.keepLease` now wraps it: expired without answer means rebinding by broadcast with a pause growing to a minute, forever, keeping the address in use (a network without a server is not an address conflict); the same address confirmed means carrying on with the fresh lease (`HOPOS_DHCP_REACQUIRED`); a DHCPNAK or a different address means the address is gone, and the stack cannot change address, so `hopnet.AddressLost` asks for a controlled reset: the node canary withholds its pets and the hardware watchdog restarts the node, which then waits for the network in the boot loop above. `leandhcp.ErrRefused` is exported for this (local lean, to publish).
3. **Ampere.** The generic UEFI board now carries the NIC interrupt path that L80 built for the O6N: `igb` has the same three verbs as the Realtek (`EnableIRQ`, `AckIRQ` = mask plus read-to-clear, `RearmIRQ`), the GICv3 driver can read the distributor's pending bits (`GICD_ISPENDR`, which show a level line even while the interrupt is disabled), and `uefi/hop` discovers the line without enabling anything: snapshot the pending SPIs, open the chip's mask, wait for the one that comes up. The O6N table is only a hint, and only on Cix silicon; bundle 46 showed why, it took the O6N's 468 on the Ampere. Bundle 47 on the Ampere: `NIC line discovered: SPI 52 (INTID 84)`, wired to HOP's core, display and launcher adopted through the FLIP. Measurements follow below.

**Ampere measurements, same Vitals fixture (2 cores, 512 MiB), 64 MiB per transfer, three repeats; rx = 32 MB from the internet.**

| bundle | NIC path | upload into the node | download from the node | rx |
|---|---|---|---|---|
| 47 | interrupt, line = first new pending SPI (52), igb ack = mask then read | 2.8 / 2.3 / 2.5 MB/s | 25.4 / 25.3 / 24.7 MB/s | 2.6 MB/s |
| 48 | polled (`DefaultNICIRQ="0"`) | 4.6 / 5.0 / 5.2 MB/s | 41.9 / 43.2 / 42.3 MB/s | 7.8 MB/s |
| 49 | interrupt, line = first new pending SPI (this time 124), igb ack = read then mask | 36.9 / 38.8 / 40.3 MB/s | 48.1 / 50.1 / 45.7 MB/s | 26.4 MB/s |

rtt p50 was 80–180 µs and storm 590–640 conn/s in all three; those do not separate the variants. What followed cost the morning and ends with the interrupt off on the Ampere:

4. **Line discovery was a guess, now it proves.** "The first SPI that becomes pending after the chip opens its mask" gave 52 on one boot and 124 on the next: from cold, SPI 52 (INTID 84) comes up first and does not drop when the chip is masked, so it is not the NIC, and bundle 47 had wired it and lived on the 10 ms guard, which is where its 2.5 MB/s came from. The discovery now collects a whole 2 s window, and for every candidate closes the chip's mask (the line must drop) and reopens it (it must return), remembering rejects; bundles 55–57 report `SPI 124 (INTID 156) follows the chip's mask (rejected [], 3 SPIs already pending)`. The GICv3 driver can also list and quiet every SPI the firmware left enabled (none on the Altra) and read a line's router.
5. **The igb acknowledge order.** Bundle 49 reads ICR before closing the mask (a masked ICR bit is not cleared by the read); bundle 47 did it the other way round. Whether that order matters was not established: 47's slowness was the wrong line. The comment in the driver says so; bundles 51/53 (old order, verified line) were built for the A/B and never ran because of point 6.
6. **On the Altra the NIC interrupt kills the node.** Every kernel that wired INTID 156 and received interrupts hung HOP's core within seconds, except bundle 49: 50 (from 49, dead about a minute after landing), 52 and 57 (wired during the probe, never reached the network console), 54, 55 and 56 (wired 10 s after the lease: the console shows the wiring line and then nothing). Bundle 56 proves the delivery path is sound: `irq: claim #1 is INTID 156`, `#2`, `#3` within one second of wiring, and five seconds later the node is gone: no agent, no console, no ping, until the SBSA watchdog resets it and the firmware brings the cold kernel back some three minutes later. The firmware left no SPI enabled, so it is not a stray line landing on a parked core. Early versus late wiring makes no difference (57 versus 54–56). The single survivor, 49, ran interrupts for minutes at 40 MB/s and was flipped from a polled kernel of this tree rather than from the cold kernel; that is the only difference on record and it is not understood. What is needed next is the hung core's state (ELR/ESR from the header UART, or an EL2 last-words path), not more flips.
7. **Default for unknown UEFI boards: poll.** `DefaultNICIRQ` is now `known`: the O6N wires its table line as before (L80 evidence stands), a board without a known line polls and says so, and `hopos.nicirq=auto` is the opt-in for discovery. The Ampere stays polled at 5 MB/s inbound (older than today; the O6N polled 37) until point 6 is understood. The wiring can also be deferred 10 s after the lease (`IRQLate`, off) so the network console is up before discovery; that is how 54–56 were observed.
8. **USB on the Ampere.** Derek: "USB does not work, try it while you are at it." The xHCI driver and the input manager exist (L77, O6N native hosts); the Altra's controllers are PCIe functions (class 0c.03.30). `usb_uefi.go` now registers them from the PCIe hierarchy when the O6N table gave nothing. The first attempt (bundle 58, scan inside an `init()` before hwinit) never reached the network console; the scan now runs from `startUSBInput` and the whole path is opt-in (`hopos.usbpci=1`) until someone at the machine sees a cursor. Unproven.
9. **Flip harness lessons.** The network console only exists after ProbeNIC, so anything that dies before it looks like a placement failure from outside; `consolefollow.py` keeps a reconnecting reader on tcp/5555 (a flip kills the old socket silently, reconnect after 5 s of silence), and `ampereab.py` runs the fixture. Flip-events for bundles 46–58 are in `reviews/2026-09-19-ampere/flip-events.jsonl`; the node ends the day on its cold kernel, which is stable. The O6N was off during all of this (host down since the morning); the M4 ran bundle 28 with cloudflared and spin at 0 restarts throughout.

**Afternoon, with the header UART on the Mac (Derek's FTDI cable, 115200; `reviews/2026-09-19-ampere/uartfollow.py`).** Derek reaffirmed the goal (interrupt and USB on the Ampere too), so the hang was chased to its mechanism with a ladder of one-change bundles, 59–76 in `bundles.json`, each flipped onto the empty node with the watchdog off so the last words stay on the wire:

10. **Instruments first.** The o6n flavour carried a link-time early UART (the Orion's 0x040d0000), and while that is non-zero hwinit1 never takes the SPCR console, so on the Altra HopOS printed into nothing and the cable showed only the UEFI stub. The early UART now applies only on CIXTEK firmware (`EarlyUARTOEM`); the Altra prints on its SPCR PL011. `hopos.wd=off` now really disarms the SBSA watchdog a previous kernel armed (`WatchdogOff`, WCS readback). A last-words record (EL1 exception → ESR/FAR/PC + console tail at a fixed carve address, `lastwords_uefi.go`) proved that DRAM does not survive the reset on this box (ECC scrub: three addresses read zero), so RAM post-mortems are out here. The DSDT and SSDTs can be dumped as base64 over the console (`hopos.acpidump=1`) for iasl.
11. **The line is right.** The decompiled DSDT `_PRT` says segment 5 (PCI7) root ports route INTA→GSI 156, INTB→157, INTC→158, INTD→159; the I210 sits on that segment. INTID 156 is exactly what Linux would program from ACPI. INTID 84, the one that rises first from cold, is in no `_PRT`.
12. **The death is not on HOP's core.** Bundle 63 (tamago's handler): three claims, two empty passes, silence, SoC reset 35 s later with the watchdog off. Bundle 64 (a flag-only IRQ vector with the governor waking the ISR, no runtime code in exception context): dead before the first claim. Bundles 66/67 (I never unmasked, no exception ever taken): dead ~3 s after the mask opens. Bundle 72 (line routed to another core): dead in 150 ms. Bundle 73 (priority 0xff, never signalled to any CPU interface): dead in 200 ms. Bundle 74 (edge-triggered): 19 interrupts claimed and acked in 200 ms, then dead. In every case the last console line is normal, no EL1 exception is taken, the heartbeat goroutine stops, ping stops, and the firmware reboots the SoC about 35 s later. The one ingredient every fatal run shares: GICD_ISENABLER set for INTID 156 while the endpoint asserts INTx. With the same INTx asserted for seconds and the line *disabled* (the discovery), nothing happens. Bundles 65 and 68 died from my own EL2-only register reads (EC=0 at EL1) and taught that HOP runs at EL1 here, with our EL2 above it; the chainload's E2H remark is about the landing context.
13. **Reading.** The Altra's PCIe erratum in the open (82288 / "PCIE_65": bad addresses for MMIO writes with discontiguous byte enables from write-combining mappings) does not describe our 32-bit Device-mapped register writes. Linux never exercises INTx on this NIC: igb takes MSI-X through the GIC ITS. The only interrupt path Linux proves on the Altra is therefore the ITS (LPIs), which HopOS does not have yet: ITS init from the MADT, IORT for the requester-ID mapping, LPI tables in the redistributor, MAPD/MAPC/MAPTI/INV/SYNC, and MSI-X on igb. That is the road for "interrupt on the Ampere"; it is days, not hours, and it is also the road for NVMe interrupts later. INTx on the Altra stays off (`DefaultNICIRQ=known`).
14. **USB.** With `hopos.usbpci` forced on, the PCIe scan registers `xhci@03:00.0 1912:0014`, a Renesas uPD720201/2 behind a root port, BAR0 at 0x2bfff0300000. Bundle 75 aborted on the first register read; bundle 76 enabled the bridge chain first and aborted the same way: `ESR 0x96000004`, a level-0 translation fault on the BAR, although `MapHigh` reported success. The 48-bit mapper had never been exercised for a BAR above 512 GB in a flipped kernel (the I210's BAR is low); next step is printing TTBR0/TCR and the L0 entry inside Prepare. The Renesas also needs its firmware upload (Linux `xhci-pci-renesas`) unless the card carries a ROM; unknown yet. USB stays opt-in.
15. **State.** The Ampere ends on its cold kernel with display and launcher, stable. The bundles, build logs, flip events, A/B results, the UART log and the decompiled DSDT are all in `reviews/2026-09-19-ampere/`. Gate re-run after the day's tree changes.
16. **Scope decision (Derek, end of the day).** HopOS is for the efficient end: nodes of up to about 20 cores, where the idle wake rate is a real share of the power budget (the M4's interrupt is worth about 1 W of 4). The Altra is a reference platform that runs polled; the ITS/MSI-X route is not pursued. The INTx findings stay documented so nobody repeats the six flips.
17. **The polled inbound gap was the ring.** igb had 64 receive descriptors; at 1 Gbit and a 300 µs poll that is less than one poll round of frames, so the ring overflowed, frames dropped and TCP collapsed to 5 MB/s. With 256 descriptors (bundle 80, same Vitals fixture, polled, 64 MiB × 3): upload 36.8 / 34.8 / 36.2 MB/s (was 4.6–5.2), download 42.8 / 42.5 / 42.1 (unchanged), rx from the internet 36.5 (was 7.8; that figure is bounded by the line). The Realtek already had 256, the dwmac 128; the Pi and Radxa inbound figures of 3–7 MB/s (L-earlier, v2.1 first run) deserve the same look. Derek's target for the small boards is 100 MB/s on a 1 Gbit link; the remaining ceiling of about 40 MB/s in both directions is shared by the M4 (tg3) and the O6N (rtl8126) and therefore lives above the drivers: the stack, the pump and the slot LAN, not the NIC.
18. **Where the 40 MB/s ceiling lives (bundle 81, idle statistics forced).** A 600 MiB upload into the Vitals app ran at 29–32 MB/s, an 800 MiB download at 53 MB/s. HOP's own TCP counters barely moved (about 130 segments in the whole run): traffic to a published app port is switched as frames NIC ↔ slot ring, HOP's stack is not in the path. HOP's core was 5–33 % busy in the 10 s samples, so the limit is not HOP's CPU either. What remains is the per-frame path: 1500-byte frames handed one by one through the slot ring with 300 µs polls on both ends, and the app's own leannet (window scaling yes, SACK no). 100 MB/s on 1 Gbit means about 70 000 frames a second through that path; reaching it is batching (several frames per ring handoff and per wake), coalescing toward the jumbo slot MTU, and the app pump's cadence — a project for the target boards, measured on M4 and O6N, not an Ampere item.
19. **Doorbell batching (night of 19/20 September).** `netdev.Flusher`: igb and tg3 defer their doorbells (RDT, TDT, the tg3 mailboxes) and no longer read TDH per transmitted frame; the RX pump flushes when the ring is empty, the drivers flush themselves every 32 received frames, the switch loop flushes the uplink once per round, HOP's own stack flushes per frame, and batching only switches on after the network is up. Two lessons cost a bundle each: with deferral on from boot, DHCP has nobody to flush for it and the node never gets a lease (bundle 30 on the M4, bundle 82 on the Ampere: watchdog, cold boot, and on the M4 the loss of cloudflared and spin until restored from the saved job specs); and `Uplink.Receive` loops through a whole inbound burst without returning to the pump, so a pump-only RX flush starved the NIC of descriptors (bundle 83: 1 MB/s inbound). Bundle 84 on the Ampere, polled: short upload 36.7 / 34.9 / 37.6 MB/s, download 42.3 / 43.4 / 41.8, rx 37.9; long upload 35.7 (was 29–32), long download 53.3 (unchanged). So the PCIe register traffic per frame was not the ceiling; the code stays because it is correct and cheaper, but the 100 MB/s lives elsewhere: the app-side pump and its stack (ACK cadence, window), and the sender's view of it. Next: a packet capture on the Mac during an upload (window, ACK pattern, retransmits), the app core's busy share, and the M4 on bundle 32.
20. **The measuring Mac is on Wi-Fi (found 02:00, 20 September).** Every throughput and latency figure taken from 192.168.1.208 in this logbook went over `en0`, which is the Mac's Wi-Fi: the gateway itself pings at 3–93 ms from here, the M4 at 4–95 ms, the Ampere at 2.6–66 ms, and a single raw TCP stream into any node tops out around 47 MB/s in and 54 out. That is the link, not HopOS; it is also why the O6N's polled-versus-interrupt figures wandered by 10 MB/s and why nothing on the node was busy while transfers "stalled". The night's ladder after the ring fix — doorbell batching, the switch kick from the RX pump, the app pump's linger and yield, the byte-count growth trigger in leannet, a finer event stream — showed no gain and cannot show one over this link; the 82 µs event stream made everything worse (more wakes, no benefit) and was taken out again. What stands: the igb ring (5 → 36 MB/s over the same Wi-Fi, so real), and the Wi-Fi ceiling as the thing to eliminate first. The 100 MB/s question is open until it is measured from a wired host; the Mac has Ethernet ports without a cable.
21. **Node to node, wire only (02:30).** Derek's correction: measure between the test machines, not from the Mac. Vitals on the M4 and on the Ampere, each pulling 400 MB from the other over the wired switch: 43.8 / 44.0 MB/s (M4 sends), 44.5 / 44.5 (Ampere sends), header in 16–21 ms, no hang (the v2.1 node-to-node hang is gone). So the ~44 MB/s ceiling is HopOS's after all, and its shape is latency: a connect-request-response cycle between the two nodes takes 21 ms p50 whether the target is the app or HOP's own stack (storm from either side, 240–365 connections/s). Two HopOS nodes therefore add about 5–7 ms of round trip, and during a pull half of the app pump's wakes came from its timer rather than the doorbell (pump_early 5711, pump_timer 5846). The throughput is a window divided by that round trip. The levers are the wake paths, per hop: the RX poll without an interrupt (1.3 ms event-stream tick on the Ampere), the switch loop falling back to its 1 ms failsafe when the door is missed, and the app pump's missed doorbells. That is target-board work (M4 and O6N have the NIC interrupt; the rest of the chain is the same), to be instrumented hop by hop with timestamps before anything is changed again.
22. **Streaming mode and the GIC kick (03:00–04:00).** Two builds on the Ampere against the M4 on bundle 28. Bundle 88: HOP's RX pump lingers 500 µs after a burst polling the NIC (yielding in between), the switch loop lingers 300 µs after work; node-to-node stayed at 44.7 / 44.4 MB/s and the connection cycle at 21 ms, so those two sleeps were not where the round trip goes. The in-node numbers then pointed at the app side: on the M4 an app reaches its own HOP over the slot LAN in 0.63 ms and a hairpin (app → HOP → NAT → back to the app) in 2.0 ms; on the Ampere 2.3 and 32 ms, with the system-call round trip at 390 µs p50 / 6 ms p99 against 60 / 300 µs. The reason is structural: `board.Cores.Kick` exists only on Apple (the fast IPI), so on a GIC board a resident that yields to EL2 sleeps in WFE until the event-stream tick; HOP cannot wake it. Bundle 89 adds the kick for GICv3: the UEFI board reports `IdleMode = IdleYield` and `Kick = SGI 1` to the core's MPIDR (`gicv3.SendSGI`, redistributor prepared once per core with `PrepareSGI`), the switcher under `GIC_IPI` sets up the core's CPU interface, sleeps in WFI and acknowledges the SGI through IAR1/EOIR1, and the waker comes on (`HOPOS_WAKER_UP`; idlestat shows 533 rounds/s and 364 kicks/s). A freshly placed app then measures rtt 340 µs p50 / 0.8 ms p99, app → HOP 1.33 ms, hairpin 21–23 ms; node-to-node 45.0 / 45.0 MB/s. Better on the tail, not on the median, and the hairpin is still ten times the M4's. The next thing to see is where a hairpin spends its 21 ms — timestamps at the ring writes and the switch pass — before touching anything else. `GIC_IPI` is now in the o6n, uefi and QEMU (uefi-run.sh) builds; the O6N must be re-verified with it, and the M4 keeps `APPLE_IPI`.
23. **Where the hop cost sits, and a bundle chain that ended in a stuck node (04:30–06:00).** With a fresh single-core app the Ampere's hairpin is 2.4 ms (M4: 2.0), so the per-hop yield cost is not what limits throughput; the two-core app still hairpins in 21–26 ms because the sibling wake (HVC #4) had no GIC kick — bundle 94 adds an SGI there under `GIC_IPI`, untested. The app-side timer oversleeps a fixed 310 µs and a system call costs 340 µs (M4: 60), which is the EL2 switcher running with instruction fetches uncached (SCTLR_EL2.I=0, the boot code clears M, C and I). Bundles 91–93 tried enabling the EL2 I-cache from the park path; under 92 every app died shortly after start (HOP writes thunks into the plan region after a core has started; stale instructions), 93 added `ic iallu` per park but could not land: a resident that failed and left its port and framebuffer grant on slot 1 makes every later flip refuse with `slot 1: reserved owner is not ready for flip`. The I-cache change is out again; enabling it needs a coherence design (invalidate on the app core after every thunk write, or no writable code in the plan region), not a park-time toggle. The node-to-node counters also showed the actual throughput mechanism: the sending app's RX pump (the ACKs coming back) wakes mostly on its timer (M4 sending: 981 door, 6821 timer; Ampere sending: 976 / 6381), about once per 1.3–1.4 ms, so the window advances in ~58 KB steps per tick: 45 MB/s. The direct RX kick from the switch (`wakeRX`) was never reached in those runs (its skip counters stayed at zero), so the "there is traffic" signal to the app is the lever Derek named, and it is the next thing to make work on both boards. The Ampere ends the night stuck on bundle 92 with the slot-1 zombie: it needs a power cycle; the M4 is untouched on bundle 28.
24. **The Ampere after the power cycle, and a verdict that has to be withdrawn (08:50–11:00, 20 September).** The node came back on its disk kernel, which is an 18 September build (polled igb ring of 64, no waker, the O6N name on the console), and the 9.3 / 8.3 MB/s node-to-node pulls measured on it are that kernel, not a regression. Bundle 94 (GIC kick, sibling SGI, kick counters, no I-cache change) refuses over the two desktop residents because its switch code differs; with them stopped it landed as generation 1 in 12 s, and then every app placed afterwards never came back to HOP: the reseeded display on core 1 "never yielded" for the launcher, a fresh one-core Vitals on core 2 never answered HTTP and "did not park even after stage-2 revocation", the two-core app the same on core 4. That is the same picture as bundle 92 in point 23, and both cases share one condition: the cores had been parked by a kernel with the old switch bytes (this morning: the disk kernel, after the cold-kernel measurement) and were then dispatched by a kernel with new ones. The I-cache verdict in point 23 is therefore unproven either way. What is proven is the switcher itself: the same `GIC_IPI` build on QEMU (EDK2, neoverse-n1, GICv3, four cores) runs a one-core and a two-core Vitals, both answer, yield and park cleanly on delete (rtt 517 / 524 µs p50, hairpin storm 876 / 847 conn/s — TCG figures). The discriminating run on the Ampere is the next power cycle with nothing placed on the cold kernel first: stop the desktop jobs, flip 94, and compare the reseeded display on the cold-parked core 1 with a fresh app on core 2. Until then the Ampere holds bundle 94 with its jobs deleted (they were restart-looping into `HOPOS_CORE_RECLAIM`), and needs the power cycle. The 02 September memory on poisoned parked cores says a parked core needs no reset because the park loop bytes never change; the mailbox protocol is indeed stable, so if the Ampere run confirms the poisoning the mechanism is still open.
25. **Last-words probe fixed.** `lastwords_uefi.go` still probed two Altra control addresses; on QEMU the third (0x8007ffff000) is unbacked and the EL1 exception handler faulted inside its own record write, recursed and ran the heap out (`fatal error: out of memory`). The probe now writes only the carve address every board has, and the handler refuses to re-enter.
26. **The 45 MB/s ceiling is the round trip, not the app side (M4 bundle 33, 11:00).** Bundle 33 is the current tree with idlestat on; it flipped over cloudflared, spin and the Vitals fixture (generation 2, 3 of 3 handed over). Two Vitals on the same M4 pulling 400 MB from each other through the switch: **769 and 764 MB/s** either way, with the app pumps almost never sleeping (about 260 wakes per 400 MB). Node to node the same path does 45 MB/s. So the difference is the wire's round trip: leannet's receive ring starts at 16 KiB and grows, but a window in the 64 KB range divided by a 1.3–1.4 ms round trip is 45–50 MB/s, and the same window over the in-node round trip is the 765. The counters also settle point 23's question: the "there is traffic" kick does work — with the Mac pulling from a one-core app HOP sent 196 direct RX kicks per second and the pump's remaining timer wakes are its own 300 µs poll floor between ACK bursts (`RXPOLL` lo), not missed doorbells; "not due" skips are a pump that was awake. Mac pulls (Wi-Fi) 56.3 MB/s from a one-core app and 36.7 from a two-core one. Consequence: the lever is the round trip, and on the target boards the round trip is the NIC interrupt on both ends (M4 and O6N have it; the Ampere polls at the 1.3 ms event-stream tick, which is exactly the RTT observed). The measurement that decides the 100 MB/s question is M4 ↔ O6N, both interrupt-driven; the O6N is off. If the window turns out to be the second limit, leannet already advertises a window scale, and the ring growth rule is the knob.
27. **100 MB/s over the wire, and the round trip explained (12:00–12:30, 20 September).** Reasoning first, at Derek's request: throughput is window ÷ round trip, and both are measurable. The connection cycle (two round trips) is 0.7 ms in-node and 21.0 ms over the wire (p90 21.2, p99 21.5 — a timer, not jitter), so the wire round trip is 10.5 ms; 45 MB/s × 10.5 ms = 472 KB, which is leannet's per-connection cap of 480 KB, and in-node 765 MB/s × 0.6 ms is the same 460 KB. The window is at its cap on both paths; the receive-ring growth trigger of point 20 was aimed at a limit that does not exist. The 10 ms is not physical (every pump in the chain wakes within 1.3 ms) but the only 10 ms constant above the drivers: `WaitNIC(10 ms)`, the failsafe of HOP's RX pump when it waits for the NIC interrupt. One node waiting 10 ms per received frame gives a symmetric 10 ms round trip whichever side starts; two would give 41 ms cycles. M4↔O6N and M4↔Ampere both measure 21 ms with different kernels on the O6N and the Ampere, so the common element is the M4: its tg3 interrupt does not fire for a lone frame (it does under bulk, 7,588 claims/s during a pull), and each SYN or small-window ACK waits for the failsafe. The L82 verdict "interrupt equal or better than polling" was measured over Wi-Fi and could not see this. A/B: bundle 34 is bundle 33 with `hopos.nicirq=0` (the M4 polls, `0 irq/s, 1005 empty rx rounds/s`), flipped over cloudflared, spin and the fixture (generation 3). Against the O6N on its polled stick kernel: cycle 10.9 / 9.6 ms p50 (was 21.0 both ways), and 400 MB pulls **103.4 and 114.2 MB/s** (O6N from M4) and **101.2 and 101.7 MB/s** (M4 from O6N), header in 5 ms (was 18–21). That is the 1 Gbit wire. The remaining 10 ms per round trip is on the O6N's stick kernel side (18 September build) and is the next thing to look at, together with the tg3 interrupt itself: an interrupt path that only fires under bulk must not be the default until it wakes the pump for every frame, so the M4 stays polled for now.
28. **The tg3 interrupt fixed the Linux way; the M4 back on its interrupt at 110–118 MB/s (12:10–12:15).** The O6N flipped to bundle 45 (its L80 interrupt kernel, three residents handed over); against the polled M4 the cycle dropped to 2.4 / 1.5 ms and the pulls read 100.6 / 105.7 (O6N from M4) and 118.2 / 118.2 MB/s (M4 from O6N), so the remaining 10 ms of point 27 was the O6N's stick kernel, not the M4. The tg3 cause: our `RearmIRQ` only reopened the interrupt mailbox; work that arrived while it was closed (the ack masks it, the pump drains, then reopens) does not make the chip raise the line again, so a lone frame waited for the 10 ms failsafe while bulk kept going on the next status update. Linux handles exactly this in `tg3_int_reenable`: after reopening, check for work and set `HOSTCC_MODE_NOW` so the chip produces a status update and an interrupt at once. `RearmIRQ` now does the same (return-ring producer against our consumer index). Bundle 35 (that fix, interrupt on by default, idlestat) flipped over cloudflared, spin and the fixture as generation 4. Against the O6N on bundle 45: cycle **2.0 / 1.3 ms** p50 (was 21.0 on the interrupt without the fix), app system-call rtt 250 µs p50, hairpin 2.1 ms, and 400 MB pulls **109.3 / 112.2 MB/s** (O6N from M4) and **118.3 / 118.3 MB/s** (M4 from O6N) with the first byte in 1.2–1.5 ms; idlestat shows 121–130 interrupts/s at idle and up to 38,795/s during a pull. That is the 1 Gbit wire on both target boards with both NIC interrupts on, and Derek's 100 MB/s is met. The measuring fixtures on both nodes are removed; the M4 runs bundle 35 with cloudflared and spin, the O6N bundle 45 with display and launcher.
29. **Two-core apps over the wire, and what the tg3 fix cost on the way (12:15–13:00).** The first two-core run under bundle 35 ended with the M4 cold-booting from its disk kernel (watchdog; only welcome seeded, cloudflared and spin gone, spin refused on that kernel: 2048 MB > its 768 MB slot cap). The cause was the placement of the fix, not the fix: the waiter goroutine ran "reopen, check for work, force a status update" before every wait while the pump was still draining, one forced interrupt per frame on top of the pump. It now sits where Linux has it (`napi_complete` → `tg3_int_reenable`): the RX pump rearms at its own sleep point, after an empty round, through `netdev.IRQRearmer` passed down the uplink and local-device wrappers; `WaitNIC` only waits. Two more things learned on the way: clearing the status block's UPDATED bit in the ack, as `tg3_interrupt` does, silenced the interrupt for good on the M4 (bundle 37: 0 irq/s, cycle back to 21 ms) and is out again with the reason left open; and the disk kernel → bundle 34 flip with zero residents placed cloudflared and spin on cores the old kernel had parked, and both run, so on the M4 a switch-code change over parked cores is fine (the Ampere's deaths after bundles 92 and 94 need another explanation, which points at the `GIC_IPI` switcher on Altra silicon). Bundle 38 (rearm at the sleep point, NOW work check, no UPDATED clear, NIC diag word in idlestat) over cloudflared and spin: one-core pulls 117.0 / 108.6 MB/s, cycle 1.9 / 1.3 ms; **two-core apps on both nodes 111.5 (O6N from M4) and 114.4 MB/s (M4 from O6N)**, first byte 1.0 ms, app-to-app storms both ways without errors (775 and 1,069 conn/s), M4 two-core rtt 57–240 µs p50, hairpin 2.1–2.3 ms; the interrupt rate returns to 65–94/s after every run (25,634/s during one). The O6N on bundle 45 has no sibling kick, and it shows: its two-core hairpin is 17 ms and a two-core app there answered a storm from the M4 with 48 refusals once (bundle 36 run, not reproduced on 38); a two-core app on the M4 exited once right after its memory-limit line (exit code 0) and ran on the next placement. Both are on the board. Gate green. M4 on bundle 38 with cloudflared and spin (restart count 7 from the port-80 clash with the reseeded welcome, since removed); O6N on 45 with display and launcher; no fixtures left.
30. **Tree hygiene (13:00–13:25).** Everything that was a measurement and not a mechanism is out: the ACPI hex dump and the last-words record (an EL1 exception on HOP's core still prints ESR/FAR/PC, `cmd/hopos/exception_uefi.go`), the Ampere USB opt-in (`hopos.usbpci`, `uefi/hop/usb.go`, the PCIe bridge readers), the IRQ experiment knobs on the UEFI board (late wiring, routing to another core, priority, edge, heartbeat, line-state prints) and the GICv3 readers they used, the Apple IRQ read probe, the DHCP failure knob, the event-stream period knob, the first-five "rx: wake" lines, the NIC diagnostic word in idlestat, and the two lingers that showed no gain (HOP's RX pump and the switch loop; the app pump keeps its 200 µs linger and the yield per 16 frames, which cured a measured wake storm). What stays is what the boards run on: the tg3 and igb interrupt paths, `hopos.nicirq` with `auto` discovery on UEFI, the GIC kick and `GIC_IPI` switcher, the interrupt door, the waker and rx-kick counters behind `hopos.idlestat`, `hopos.wd=off`, `hopos.nvmewipe`. Gate green. The release checklist and the board are updated; nothing committed.
31. **O6N on the current tree: the `GIC_IPI` switcher fails on Cix silicon exactly as on the Altra (13:29–13:40).** Bundle 46 (today's tree, config without the NVMe wipe that the 18 September configs still carry — bundle 45 re-zeroed the O6N's partition tables at 12:12 for that reason) landed as generation 2 with the desktop residents stopped. The launcher started on core 1; the display, sharing that core, never got it: `core 1 never yielded for slot 1`, quarantine, port 80 held by the dead slot, and on delete `slot 2: not dead after stage-2 revocation (shared core 1)`. That is the Ampere picture of point 24 on a second GICv3 board with a different firmware, while the M4 (Apple IPI switcher) survives the same sequence; so the suspect is the `GIC_IPI` sleep or wake path itself on real hardware (QEMU accepts it), not parked cores. Bundle 47 is the same tree built without `-D=GIC_IPI` (WFE sleep; HOP's SGI kick wakes a WFE just as it did on the Ampere bundles 85–90) as the A/B, and it cannot land: `slot 1: reserved owner is not ready for flip`. The O6N needs a power cycle; the clean run is then: stick kernel, stop display and launcher before anything else is placed, flip 47, and see the residents come up. One more observation from bundle 46 to keep: `slot 1: fb grant requested, board has no framebuffer` — after a zero-resident flip from bundle 45 the new kernel had no framebuffer fact, which is either the carve's firmware-facts layout having moved between the 18 September and today's `uefi.go` or a lost GOP fact; it decides whether the desktop can come back on a flipped kernel and is on the board.
32. **O6N after the power cycle: the switcher is pinned, the rest is not yet (14:00–15:30).** Bundle 47 (today's tree without `GIC_IPI`) landed on the fresh stick kernel with the desktop stopped first, and display and launcher came up and stayed up — so the `GIC_IPI` switcher is the only thing that kills residents on real GICv3 boards, and `flip-bundle.sh` now builds `uefi` and `o6n` without it. But the current tree on the O6N is back at the 21 ms cycle and 45 MB/s each way, one- and two-core alike, where bundle 45 (the 18 September tree) did 1.3–2.4 ms and 118 MB/s this morning; the two-core hairpin is 14–18 ms and a two-core O6N app refused 64 connections in a storm. Bundle 48 (47 with tamago's interrupt service instead of the flag-only door) measured the same, so the door is not it. The remaining differences to bundle 45 are the rearm moved to the pump's sleep point and the doorbell batching; for the RTL8125 the first has a plausible mechanism: its interrupt is an edge on an ISR bit being set under an open mask, so a bit that latched while the mask was closed (a frame the pump read anyway) never fires again after the unmask, and every later frame waits for the 10 ms failsafe. Bundle 49 carries the fix (rearm = clear ISR, then unmask; the pump reads the ring once more after the rearm) together with the firmware-facts repair of the framebuffer (`bootELVal` had been inserted at +48 on 19 September, shifting `gopInfo` and `cfgLen` for every kernel flipped from a stick that wrote the older layout; it now sits at +120, past the cage facts). Bundle 49 did not land: twice the node went dark on the flip request and came back on its stick kernel two and a half minutes later, so it dies in its own boot before the console is up — a UART capture on the O6N header is the next step, not another blind flip. Two things the cold boots taught: the O6N stick config still carries `hopos.nvmewipe=1`, so every cold boot (and bundle 45's config) zeroes the NVMe partition tables again, which must come out of the stick's `hopos.cfg`; and "fb grant requested, board has no framebuffer" also appears on the stick kernel's own boot, so that message needs a second look before the fwFacts repair is credited with anything. State: O6N on its stick kernel (polled, desktop up), M4 on bundle 38.
33. **The afternoon's O6N chase was wrong-footed by the M4, and the M4 bug is real (15:30–17:00).** With the O6N back on bundle 45 (the kernel that did 118 MB/s at 12:12) the wire was still 21 ms and 26–45 MB/s. The M4's own counters then said it: on bundle 38 its NIC interrupt was dead, 0 claims/s at idle and under a storm, every frame on the 10 ms failsafe, hours after it had measured 117 MB/s. The mechanism is the stray-line guard in `cpu/irq`: a served line that comes back more than 256 times in one dispatch pass was disabled for good, and a tg3 under a 1 Gbit pull (a status update per frame, plus the NOW retrigger) does exactly that. The guard now ends the pass instead and leaves a served line enabled (unknown lines are still disabled); bundle 39 carries it and the M4 keeps 93 claims/s idle and 2,000–2,700/s under pulls. That invalidates points 31–32's "current tree is slow on the O6N": bundles 47, 48, 51, 53 and 55 (rearm timing, interrupt door, SGI kick, level trigger) were all measured against a dead M4 and changed nothing because nothing on the O6N was the cause. What remains true and open: after the O6N's power cycle every kernel on it, bundle 45 included, sits at the failsafe. A split from the Mac settles the side: connection cycle to the M4's HOP 8.5 ms p50 (the router itself 9.9 over Wi-Fi), to the O6N's HOP 21.0 ms. So the O6N's RX is at the failsafe today with the same kernel that was not this morning; the difference is the boot and flip sequence (this morning: stick, then bundle 45 over two live residents; this afternoon: stick, residents stopped, zero-resident flips through 47–55, then 45) or the LAN (the Ampere stopped answering pings this afternoon on bundle 94). One controlled repeat decides it: power cycle, flip 45 over the live residents, measure; then the current tree the same way. The stick still wipes the NVMe at every cold boot (`hopos.nvmewipe=1`, point 32). Bundle 49 dying in its own boot was the firmware-facts "repair": the stick kernel writes `bootELVal` at +48 already, so the tree keeps that layout; the framebuffer question stays open. State: M4 on bundle 39 with cloudflared and spin; O6N on bundle 45 with display and launcher; Ampere dark.
